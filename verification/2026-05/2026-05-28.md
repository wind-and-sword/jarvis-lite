# Jarvis Lite 验证记录：2026-05-28

> 日期：2026-05-28
> 执行者：Codex

## 2026-05-28 LLM fallback 近期上下文增强验证

### 验证范围

- LLM fallback 继续保持本地优先之后才触发。
- fake provider 收到的 context 包含记忆摘要和最近资料。
- 当存在近期上下文时，LLM context 追加已有建议系统生成的 `下一步建议`。
- 不依赖真实 API key，不触发真实网络调用。

### RED：LLM context 缺少下一步建议

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_llm_fallback_context_includes_recent_next_actions -v
```

结果：

- 目标测试先失败。
- provider 收到的 context 只有 `记忆摘要：用户偏好：中文简洁回答` 和 `最近资料：data/note.txt`。
- 缺少 `下一步建议：继续处理最近资料：/read note.txt；/tag note.txt 标签...`。

### 专项 GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_llm_fallback_context_includes_recent_next_actions -v
```

结果：

- 目标测试通过。

### 专项回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent -v
.\.venv\Scripts\python.exe -m unittest tests.test_llm -v
```

结果：

- `tests.test_agent`：通过，`Ran 157 tests`，结果 `OK`。
- `tests.test_llm`：通过，`Ran 25 tests`，结果 `OK`。

### 收尾验证

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
git diff --check
Markdown 本地链接检查脚本
敏感信息扫描
```

结果：

- 全量 `unittest`：通过，`Ran 332 tests`，结果 `OK`。
- 桌面 smoke：通过，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- `git diff --check`：退出 0，仅提示 LF/CRLF 工作区换行警告。
- Markdown 本地链接检查：通过，输出 `Markdown local links OK`。
- 敏感信息扫描：未命中真实 API key、用户提供的具体网关域名或 key 片段。

## 2026-05-28 LLM fallback 最近搜索结果上下文验证

### 验证范围

- `/ask` 命中多份资料后会写入最近搜索结果。
- LLM fallback context 追加最近搜索结果数量和最多 3 条编号路径。
- fake provider 可在下一次 fallback 中收到这些 context 行。
- 本地知识库问答仍优先，不会因为启用 LLM provider 而跳过本地答案。

### RED：LLM context 缺少最近搜索结果

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_llm_fallback_context_includes_recent_search_results -v
```

结果：

- 目标测试先失败。
- provider 收到的 context 包含 `记忆摘要`、`最近资料` 和 `下一步建议`。
- 缺少 `最近搜索结果：2 条`、`1. data/memory.md` 和 `2. data/runtime.md`。

### 专项 GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_llm_fallback_context_includes_recent_search_results -v
```

结果：

- 目标测试通过。

### 专项回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent -v
.\.venv\Scripts\python.exe -m unittest tests.test_llm -v
```

结果：

- `tests.test_agent`：通过，`Ran 158 tests`，结果 `OK`。
- `tests.test_llm`：通过，`Ran 25 tests`，结果 `OK`。

### 收尾验证

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
git diff --check
Markdown 本地链接检查脚本
敏感信息扫描
```

结果：

- 全量 `unittest`：通过，`Ran 333 tests`，结果 `OK`。
- 桌面 smoke：通过，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- `git diff --check`：退出 0，仅提示 LF/CRLF 工作区换行警告。
- Markdown 本地链接检查：通过，输出 `Markdown local links OK`。
- 敏感信息扫描：未命中真实 API key、用户提供的具体网关域名或 key 片段。

## 2026-05-28 LLM 调用收口与 0.1.1 打包验证

### 验证范围

- Agent 执行 LLM `command` intent 前必须再次校验命令白名单。
- `/llm-context-preview` 只展示 fallback context，不调用 provider。
- `/llm-status` 展示 API key 是否已配置、是否会触发网络调用和 SDK Base URL。
- OpenAI provider 401、429 等异常转为可读提示，且不泄露 API key。
- Windows 安装脚本明确覆盖安装行为和用户数据保留策略。
- 项目版本提升到 `0.1.1`，并构建新的 Windows 安装器。

### RED：目标行为缺失

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_help_command_lists_llm_usage_command tests.test_agent.AgentTests.test_llm_context_preview_reports_context_without_calling_provider tests.test_agent.AgentTests.test_llm_command_intent_rejects_unknown_command_before_agent_execution tests.test_llm.LLMTests.test_openai_provider_formats_401_error_without_leaking_api_key tests.test_llm.LLMTests.test_openai_provider_formats_429_error_as_rate_or_quota_issue tests.test_llm.LLMTests.test_router_uses_openai_provider_from_settings tests.test_llm.LLMTests.test_router_describes_openai_missing_configuration tests.test_llm.LLMTests.test_router_describes_fake_provider_as_local_no_network tests.test_windows_installer.WindowsInstallerTests.test_install_script_explains_cover_install_and_keeps_user_data -v
```

结果：

- 9 个目标测试先失败。
- `/help` 未列出 `/llm-context-preview`，命令返回未知命令。
- LLM 返回 `/unknown` 时 Agent 继续交给 `handle()`，导致出现未知命令执行路径。
- Provider 错误仍直接返回原始异常文本，401/429 不可读且可能包含 key。
- `/llm-status` 未展示 API key 状态和网络调用状态。
- 安装脚本未提示覆盖安装和用户数据保留。

### GREEN：目标测试通过

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_help_command_lists_llm_usage_command tests.test_agent.AgentTests.test_llm_context_preview_reports_context_without_calling_provider tests.test_agent.AgentTests.test_llm_command_intent_rejects_unknown_command_before_agent_execution tests.test_llm.LLMTests.test_openai_provider_formats_401_error_without_leaking_api_key tests.test_llm.LLMTests.test_openai_provider_formats_429_error_as_rate_or_quota_issue tests.test_llm.LLMTests.test_router_uses_openai_provider_from_settings tests.test_llm.LLMTests.test_router_describes_openai_missing_configuration tests.test_llm.LLMTests.test_router_describes_fake_provider_as_local_no_network tests.test_windows_installer.WindowsInstallerTests.test_install_script_explains_cover_install_and_keeps_user_data -v
```

结果：

- 9 个目标测试通过，结果 `OK`。

### 专项回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_llm -v
.\.venv\Scripts\python.exe -m unittest tests.test_agent -v
.\.venv\Scripts\python.exe -m unittest tests.test_windows_installer tests.test_desktop_packaging tests.test_project_metadata -v
```

结果：

- `tests.test_llm`：通过，`Ran 28 tests`，结果 `OK`。
- `tests.test_agent`：通过，`Ran 160 tests`，结果 `OK`。
- 安装器、桌面打包和项目元数据专项：通过，`Ran 18 tests`，结果 `OK`。

### 本地包版本刷新

命令：

```powershell
.\.venv\Scripts\python.exe -m pip install -e .
```

结果：

- editable 安装从 `jarvis-lite 0.1.0` 更新为 `jarvis-lite 0.1.1`。

### 全量与 smoke

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe src\app.py --once "/llm-context-preview"
.\.venv\Scripts\python.exe src\app.py --once "/llm-status"
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
git diff --check
Markdown 本地链接检查脚本
敏感信息扫描
```

结果：

- 全量 `unittest`：通过，`Ran 339 tests`，结果 `OK`。
- 源码桌面 smoke：通过，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- 打包后 exe smoke：通过，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- `/llm-context-preview`：通过，输出 context preview，并说明不会调用 provider。
- `/llm-status`：通过，默认 off 状态输出 `API key：未配置` 和 `网络调用：否（LLM 未启用）`。
- `git diff --check`：退出 0，仅提示 LF/CRLF 工作区换行警告。
- Markdown 本地链接检查：通过，输出 `Markdown local links OK`。
- 敏感信息扫描：未命中真实 API key、用户提供的具体网关域名或 key 片段。

### Windows 打包

命令：

```powershell
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Existing app files|User data kept|taskkill|USER_DATA_DIR'
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.1.1.exe -Force
```

结果：

- 安装器生成成功：`E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 版本化副本生成成功：`E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.1.1.exe`。
- 打包后 exe smoke：通过，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- `JarvisLite.exe` 大小：55,978,843 bytes。
- `JarvisLiteSetup.exe` 大小：55,373,824 bytes。
- `install.cmd` 包含 `taskkill /IM JarvisLite.exe /F`、`DisplayVersion /d "0.1.1"`、`Existing app files were replaced if present` 和 `User data kept at "%USER_DATA_DIR%"`。

### 安装升级结论

- 已安装旧版时，优先直接运行新安装包覆盖安装。
- 覆盖安装会关闭旧进程并覆盖 `%LOCALAPPDATA%\Programs\Jarvis Lite` 下的程序文件与卸载脚本。
- 用户数据目录 `%LOCALAPPDATA%\Jarvis Lite` 不会被删除。
- 只有安装目录、快捷方式或卸载注册表异常时，才建议先卸载再安装；卸载脚本也会保留用户数据目录。

## 2026-05-28 用户日志自然语言识别修复与 0.1.2 打包验证

### 验证范围

- 用户真实日志中的 `早上好` 不再落到长期记忆兜底。
- 用户真实日志中的 `你叫什么名字` 直接回答助手身份。
- 用户真实日志中的 `帮我把桌面上的比特云手机和比特浏览器的快捷方式删掉` 能识别为桌面快捷方式删除任务。
- 快捷方式删除只处理桌面目录下明确点名的 `.lnk` 文件，并报告未找到项。
- 桌面 bridge 复用同一套自然语言意图。
- 安装脚本、IExpress 完成弹窗和包版本升级到 `0.1.2`，方便用户确认新安装包生效。

### RED：用户日志样例仍走兜底

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_natural_language_morning_greeting_uses_user_name_without_memory_fallback tests.test_agent.AgentTests.test_natural_language_assistant_name_question_answers_assistant_identity tests.test_agent.AgentTests.test_natural_language_deletes_named_desktop_shortcuts_only tests.test_agent.AgentTests.test_natural_language_desktop_shortcut_delete_reports_missing_names tests.test_agent.AgentTests.test_greeting_does_not_use_plain_memory_summary_fallback tests.test_desktop_bridge.DesktopBridgeTests.test_send_routes_greeting_through_local_natural_language_brain tests.test_windows_installer.WindowsInstallerTests.test_install_script_uses_supplied_project_version tests.test_windows_installer.WindowsInstallerTests.test_iexpress_sed_points_to_external_installer_output_and_packaged_exe -v
```

结果：

- 8 个目标测试先失败。
- `早上好`、`你叫什么名字` 和桌面快捷方式删除请求都返回 `Jarvis Lite 已读取长期记忆...`。
- 安装脚本未输出版本号，IExpress `FinishMessage` 未包含当前版本。

### GREEN：目标测试通过

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_natural_language_morning_greeting_uses_user_name_without_memory_fallback tests.test_agent.AgentTests.test_natural_language_assistant_name_question_answers_assistant_identity tests.test_agent.AgentTests.test_natural_language_deletes_named_desktop_shortcuts_only tests.test_agent.AgentTests.test_natural_language_desktop_shortcut_delete_reports_missing_names tests.test_agent.AgentTests.test_greeting_does_not_use_plain_memory_summary_fallback tests.test_desktop_bridge.DesktopBridgeTests.test_send_routes_greeting_through_local_natural_language_brain tests.test_windows_installer.WindowsInstallerTests.test_install_script_uses_supplied_project_version tests.test_windows_installer.WindowsInstallerTests.test_iexpress_sed_points_to_external_installer_output_and_packaged_exe -v
```

结果：

- 8 个目标测试通过，结果 `OK`。

### 专项回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent tests.test_desktop_bridge tests.test_windows_installer tests.test_desktop_packaging tests.test_project_metadata -v
```

结果：

- 相关回归通过，`Ran 188 tests`，结果 `OK`。

### 全量与 smoke

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe src\app.py --once "早上好"
.\.venv\Scripts\python.exe src\app.py --once "你叫什么名字"
.\.venv\Scripts\python.exe -c "from jarvis_lite.desktop.windows_installer import render_iexpress_sed, windows_installer_paths; print(render_iexpress_sed(windows_installer_paths()).split('FinishMessage=', 1)[1].splitlines()[0])"
```

结果：

- 全量 `unittest`：通过，`Ran 345 tests`，结果 `OK`。
- 源码桌面 smoke：通过，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- `早上好`：输出 `早上好。我是 Jarvis Lite，可以继续帮你处理知识库、最近上下文、桌面目录和本地任务。`
- `你叫什么名字`：输出 `我叫 Jarvis Lite，是你的本地 PC 助手...`
- 安装完成消息：`Jarvis Lite 0.1.2 installation finished. Start Jarvis Lite from desktop shortcut to use this version.`

### Windows 打包

命令：

```powershell
.\.venv\Scripts\python.exe -m pip install -e .
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'Jarvis Lite 0.1.2 installed|DisplayVersion|Existing app files|User data kept|taskkill'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'FinishMessage|TargetName'
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.1.2.exe -Force
```

结果：

- editable 安装从 `jarvis-lite 0.1.1` 更新为 `jarvis-lite 0.1.2`。
- 安装器生成成功：`E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 版本化副本生成成功：`E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.1.2.exe`。
- 打包后 exe smoke：通过，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- `JarvisLite.exe` 大小：55,981,820 bytes。
- `JarvisLiteSetup.exe` 大小：55,377,920 bytes。
- `install.cmd` 包含 `taskkill /IM JarvisLite.exe /F`、`DisplayVersion /d "0.1.2"`、`Jarvis Lite 0.1.2 installed to "%INSTALL_DIR%"`、`Existing app files were replaced if present` 和 `User data kept at "%USER_DATA_DIR%"`。
- `JarvisLiteSetup.sed` 包含 `FinishMessage=Jarvis Lite 0.1.2 installation finished. Start Jarvis Lite from desktop shortcut to use this version.`

## 2026-05-28 InnerBrain 本地内脑第一版验证

### 验证范围

- InnerBrain 输出 `intent`、`slots`、`confidence`、`missing`、`source`、`reason` 和执行策略。
- 既有自然语言规则保留为 `source=legacy_rule`。
- 内置 seed 样本能识别知识库摘要和桌面快捷方式删除表达变体。
- 运行态 `data/inner-brain/training/*.jsonl` 样本可扩展本地意图。
- 低置信度输入继续进入 data/LLM fallback，不直接执行。

### RED：InnerBrain 模块缺失

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain -v
```

结果：

- 目标测试先失败。
- 失败原因为 `ModuleNotFoundError: No module named 'jarvis_lite.inner_brain'`。

### GREEN：InnerBrain 单元测试通过

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain -v
```

结果：

- `tests.test_inner_brain`：通过，`Ran 6 tests`，结果 `OK`。

### RED：Agent 尚未接入 InnerBrain

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_seed_variant_executes_without_llm tests.test_agent.AgentTests.test_inner_brain_runtime_sample_executes_without_llm tests.test_agent.AgentTests.test_inner_brain_low_confidence_still_uses_llm_fallback -v
```

结果：

- 2 个目标测试先失败。
- `麻烦看一下知识库摘要` 走了 fake LLM，返回 `LLM 外脑：不应使用外脑`。
- `请打开我的资料库` 被 data 问答弱匹配拦截，没有走运行态样本。
- 低置信度 LLM fallback 保持通过。

### GREEN 与专项回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_seed_variant_executes_without_llm tests.test_agent.AgentTests.test_inner_brain_runtime_sample_executes_without_llm tests.test_agent.AgentTests.test_inner_brain_low_confidence_still_uses_llm_fallback -v
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v
```

结果：

- 3 个 Agent 目标测试通过，结果 `OK`。
- `tests.test_inner_brain tests.test_agent`：通过，`Ran 174 tests`，结果 `OK`。

### 收尾验证

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe src\app.py --once "麻烦看一下知识库摘要"
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
git diff --check
Markdown 本地链接检查脚本
```

结果：

- 全量 `unittest`：通过，`Ran 354 tests`，结果 `OK`。
- `麻烦看一下知识库摘要`：输出 `知识库摘要`，说明 seed 样本命中后执行 `/kb-summary`。
- 源码桌面 smoke：通过，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- `git diff --check`：退出 0，仅提示 LF/CRLF 工作区换行警告。
- Markdown 本地链接检查：通过，输出 `Markdown local links OK`。

## 2026-05-28 InnerBrain preview/status 验证

### 验证范围

- `/inner-brain-status` 展示 legacy 规则、seed/runtime 样本数量、置信度阈值和训练目录。
- `/inner-brain-preview 文本` 展示策略、意图、来源、置信度、槽位和候选命令。
- Preview 只预览，不执行本地动作。

### RED：命令和格式化函数缺失

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent.AgentTests.test_help_command_lists_llm_usage_command tests.test_agent.AgentTests.test_inner_brain_status_command_reports_samples_and_thresholds tests.test_agent.AgentTests.test_inner_brain_preview_command_reports_result_without_execution tests.test_agent.AgentTests.test_inner_brain_preview_does_not_delete_desktop_shortcut -v
```

结果：

- `tests.test_inner_brain` 因 `describe_inner_brain_result` 不存在而导入失败。
- `/help` 未列出 InnerBrain 命令。
- `/inner-brain-status` 和 `/inner-brain-preview` 均返回未知命令。

### GREEN 与专项回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent.AgentTests.test_help_command_lists_llm_usage_command tests.test_agent.AgentTests.test_inner_brain_status_command_reports_samples_and_thresholds tests.test_agent.AgentTests.test_inner_brain_preview_command_reports_result_without_execution tests.test_agent.AgentTests.test_inner_brain_preview_does_not_delete_desktop_shortcut -v
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v
```

结果：

- 目标 12 个测试通过，结果 `OK`。
- `tests.test_inner_brain tests.test_agent`：通过，`Ran 179 tests`，结果 `OK`。

### 收尾验证

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe src\app.py --once "/inner-brain-status"
.\.venv\Scripts\python.exe src\app.py --once "/inner-brain-preview 麻烦看一下知识库摘要"
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
git diff --check
Markdown 本地链接检查脚本
```

结果：

- 全量 `unittest`：通过，`Ran 359 tests`，结果 `OK`。
- `/inner-brain-status`：输出 legacy 规则启用、seed 样本 9 条、runtime 样本 0 条、阈值和训练目录。
- `/inner-brain-preview 麻烦看一下知识库摘要`：输出策略 `execute`、意图 `knowledge.summary`、来源 `seed_sample`、置信度 `1.00` 和命令 `/kb-summary`，并说明不执行命令。
- 源码桌面 smoke：通过，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- `git diff --check`：退出 0，仅提示 LF/CRLF 工作区换行警告。
- Markdown 本地链接检查：通过，输出 `Markdown local links OK`。

## 2026-05-28 InnerBrain runtime 样本采纳验证

### 验证范围

- `/inner-brain-adopt 文本` 将当前 InnerBrain 可识别的非 `unknown` 结果保存为运行态 JSONL 样本。
- 保存路径固定为 `data/inner-brain/training/runtime.jsonl`。
- 重复样本不重复写入。
- 保存动作只写训练样本，不执行识别出的命令或本地动作。
- `unknown` 结果不保存，避免污染训练样本。

### RED：保存 API 和 Agent 命令缺失

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v
```

结果：

- `tests.test_inner_brain` 因 `save_runtime_training_sample` 不存在而导入失败。
- `/help` 未列出 `/inner-brain-adopt`。
- `/inner-brain-adopt` 返回未知命令，runtime 样本文件未创建。

### GREEN 与专项回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v
```

结果：

- `tests.test_inner_brain tests.test_agent`：通过，`Ran 186 tests`，结果 `OK`。
- 新增测试覆盖 JSONL 写入、去重、`unknown` 拒绝、保存后 status 刷新，以及采纳桌面快捷方式删除表达时不删除 `.lnk` 文件。

### 收尾验证

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
临时目录 /inner-brain-adopt smoke
git diff --check
Markdown 本地链接检查脚本
敏感信息扫描
```

结果：

- 全量 `unittest`：通过，`Ran 366 tests`，结果 `OK`。
- 源码桌面 smoke：通过，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- 临时目录 `/inner-brain-adopt 帮我看看资料库状态`：输出已保存 runtime 样本、`意图：knowledge.status`、`runtime_sample：1 条`，未污染当前仓库 `data/`。
- `git diff --check`：退出 0，仅提示 LF/CRLF 工作区换行警告。
- Markdown 本地链接检查：通过，输出 `Markdown local links OK`。
- 敏感信息扫描：通过，输出 `Sensitive scan OK`。

## 2026-05-28 InnerBrain 人工标注样本验证

### 验证范围

- `/inner-brain-label 文本 => intent [slot=value ...]` 可保存人工标注样本。
- unknown 输入可通过人工标注写入 `runtime.jsonl`，保存后当前 Agent 立即可用。
- `items`、`tags` 和逗号分隔值会保存为 JSON 列表。
- 缺少 `=>`、缺少 intent 或 slot 不是 `key=value` 时返回明确错误。
- 标注命令只保存样本，不执行被标注出的命令或桌面动作。

### RED：人工标注 API 和命令缺失

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v
```

结果：

- `tests.test_inner_brain` 因 `save_labeled_runtime_training_sample` 不存在而导入失败。
- `/help` 未列出 `/inner-brain-label`。
- `/inner-brain-label` 返回未知命令，runtime 样本文件未创建。

### GREEN 与专项回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v
```

结果：

- `tests.test_inner_brain tests.test_agent`：通过，`Ran 192 tests`，结果 `OK`。
- 新增测试覆盖人工标注写入、列表 slot 保存、unknown 样本保存后本地执行、无效格式拒绝，以及标注桌面快捷方式删除样本时不删除 `.lnk` 文件。

### 收尾验证

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
临时目录 /inner-brain-label smoke
git diff --check
Markdown 本地链接检查脚本
敏感信息扫描
```

结果：

- 全量 `unittest`：通过，`Ran 372 tests`，结果 `OK`。
- 源码桌面 smoke：通过，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- 临时目录 `/inner-brain-label 可以看看资料库吗 => knowledge.status command=/kb`：输出已保存人工标注样本，随后同一句自然语言返回个人知识库状态，`runtime_sample：1 条`，未污染当前仓库 `data/`。
- `git diff --check`：退出 0，仅提示 LF/CRLF 工作区换行警告。

## 2026-05-28 InnerBrain 口语化教学入口验证

### 验证范围

- `/inner-brain-teach 文本 => /命令` 可把自然语言短句保存为 runtime 样本。
- “以后我说“文本”就是 /命令”“把“文本”记成 /命令”可走同一保存链路。
- 保存后当前 Agent 立即刷新 InnerBrain，跟进输入可直接命中本地内脑。
- 教学动作只保存样本，不执行目标命令。
- 未知目标命令不写入训练样本。

### RED：教学入口缺失

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_help_command_lists_llm_usage_command tests.test_agent.AgentTests.test_inner_brain_teach_command_saves_command_sample_and_refreshes_current_agent tests.test_agent.AgentTests.test_inner_brain_teach_natural_sentence_saves_command_sample tests.test_agent.AgentTests.test_inner_brain_teach_does_not_execute_target_command_when_saving tests.test_agent.AgentTests.test_inner_brain_teach_rejects_unknown_target_command -v
```

结果：

- `/help` 未列出 `/inner-brain-teach`。
- `/inner-brain-teach` 返回未知命令，runtime 样本文件未创建。
- 口语教学句式未被识别，无法保存样本。

### GREEN 与专项回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_help_command_lists_llm_usage_command tests.test_agent.AgentTests.test_inner_brain_teach_command_saves_command_sample_and_refreshes_current_agent tests.test_agent.AgentTests.test_inner_brain_teach_natural_sentence_saves_command_sample tests.test_agent.AgentTests.test_inner_brain_teach_does_not_execute_target_command_when_saving tests.test_agent.AgentTests.test_inner_brain_teach_rejects_unknown_target_command -v
```

结果：

- 目标 5 个测试通过，结果 `OK`。
- 覆盖 help 文案、slash 教学、口语教学、保存后立即生效、保存不执行 `/daily-report`、未知目标命令拒绝。

### 收尾验证

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
临时目录 /inner-brain-teach smoke
git diff --check
Markdown 本地链接检查脚本
敏感信息扫描
```

结果：

- 全量 `unittest`：通过，`Ran 376 tests`，结果 `OK`。
- 源码桌面 smoke：通过，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- 临时目录 `/inner-brain-teach 可以看看资料库吗 => /kb`：输出已保存教学样本，随后同一句自然语言返回个人知识库状态，`runtime_sample：1 条`，未污染当前仓库 `data/`。
- `git diff --check`：退出 0，仅提示 LF/CRLF 工作区换行警告。
- Markdown 本地链接检查：通过，输出 `Markdown local links OK`。
- 敏感信息扫描：通过，输出 `Sensitive scan OK`。

## 2026-05-28 InnerBrain 0.1.3 安装包验证

### 验证范围

- 项目版本提升到 `0.1.3`。
- `pyproject.toml` 与 `jarvis_lite.__version__` 保持一致。
- 安装脚本和 IExpress 完成消息使用 `0.1.3`。
- 安装包包含 InnerBrain v1、preview/status、runtime 样本采纳、人工标注和口语教学入口。
- 覆盖安装策略不变，用户数据目录仍保留。

### RED：项目版本仍为 0.1.2

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：

- 测试失败，`pyproject.toml` 中版本仍为 `0.1.2`，期望 `0.1.3`。

### GREEN 与打包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_project_metadata tests.test_windows_installer tests.test_desktop_packaging -v
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
..\jarvis-lite-dist\dist\JarvisLite\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'Jarvis Lite 0.1.3 installed|DisplayVersion|Existing app files|User data kept|taskkill'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0.1.3 installation finished'
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.1.3.exe -Force
git diff --check
Markdown 本地链接检查脚本
敏感信息扫描
```

结果：

- 项目元数据、Windows 安装器和桌面打包专项测试通过。
- 全量 `unittest`：通过，`Ran 377 tests`，结果 `OK`。
- 源码桌面 smoke：通过，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- 打包脚本生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 打包后 `JarvisLite.exe --smoke` 通过，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- 版本化副本生成成功：`E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.1.3.exe`。
- `install.cmd` 包含 `taskkill /IM JarvisLite.exe /F`、`DisplayVersion /d "0.1.3"`、`Jarvis Lite 0.1.3 installed to "%INSTALL_DIR%"`、`Existing app files were replaced if present` 和 `User data kept at "%USER_DATA_DIR%"`。
- `JarvisLiteSetup.sed` 包含 `FinishMessage=Jarvis Lite 0.1.3 installation finished. Start Jarvis Lite from desktop shortcut to use this version.`
- `git diff --check` 退出 0，仅提示 LF/CRLF 工作区换行警告。
- Markdown 本地链接检查通过。
- 敏感信息扫描未命中。

## 2026-05-28 LLM 本地配置入口验证

### 验证范围

- `ProjectPaths` 会创建 `config` 运行态目录。
- `LLMSettings.from_sources()` 可读取 `config/llm.local.json`。
- 环境变量可覆盖本地配置文件中的同名字段。
- `JarvisAgent` 初始化时按当前项目路径读取本地 LLM 配置。
- `/llm-enable` 和“开启外脑”展示配置状态、配置路径和 smoke 命令，不输出 API key 内容。
- 已运行会话中写入 `llm.local.json` 后，说“开启外脑”会重新加载 LLM Router。

### RED：本地配置入口缺失

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_config tests.test_llm.LLMTests.test_settings_read_local_config_file tests.test_llm.LLMTests.test_environment_overrides_local_config_file tests.test_llm.LLMTests.test_build_router_reads_local_config_file tests.test_agent.AgentTests.test_agent_reads_llm_local_config_file_on_startup tests.test_agent.AgentTests.test_llm_enable_command_reports_local_config_path_without_api_key tests.test_agent.AgentTests.test_natural_language_enable_llm_uses_inner_brain_entry -v
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_enable_llm_reloads_local_config_during_running_session -v
```

结果：

- `ProjectPaths` 缺少 `config_dir`。
- `jarvis_lite.llm` 缺少 `llm_local_config_path` 和本地配置读取入口。
- `JarvisAgent` 启动后仍只读取环境变量。
- “开启外脑”落入长期记忆兜底。
- 已运行会话写入 `llm.local.json` 后，`/llm-enable` 未重新加载 Router。

### GREEN 与专项回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_config tests.test_llm.LLMTests.test_settings_read_local_config_file tests.test_llm.LLMTests.test_environment_overrides_local_config_file tests.test_llm.LLMTests.test_build_router_reads_local_config_file tests.test_agent.AgentTests.test_agent_reads_llm_local_config_file_on_startup tests.test_agent.AgentTests.test_llm_enable_command_reports_local_config_path_without_api_key tests.test_agent.AgentTests.test_natural_language_enable_llm_uses_inner_brain_entry tests.test_agent.AgentTests.test_enable_llm_reloads_local_config_during_running_session tests.test_agent.AgentTests.test_help_command_lists_llm_usage_command tests.test_agent.AgentTests.test_inner_brain_status_command_reports_samples_and_thresholds -v
.\.venv\Scripts\python.exe -m unittest tests.test_config tests.test_llm tests.test_inner_brain tests.test_agent -v
```

结果：

- 目标 11 个测试通过，结果 `OK`。
- 相关回归通过，`Ran 233 tests`，结果 `OK`。
- 覆盖本地配置文件、环境变量覆盖、Agent 启动读取、运行中重载、API key 不出现在状态文本、InnerBrain seed 样本数量同步。

### 收尾验证

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe src\app.py --once "开启外脑"
git diff --check
Markdown 本地链接检查脚本
敏感信息扫描
```

结果：

- 全量 `unittest`：通过，`Ran 384 tests`，结果 `OK`。
- 源码桌面 smoke：通过，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- `/llm-enable` 自然语言 smoke：通过，输出 `外脑启用入口`、`配置文件：config/llm.local.json` 和 `模板文件：config/llm.example.json`。
- `git diff --check`：退出 0，仅提示 LF/CRLF 工作区换行警告。
- Markdown 本地链接检查：通过，输出 `Markdown local links OK`。
- 敏感信息扫描：通过，输出 `Sensitive scan OK`。
- `git ls-files config/llm.local.json` 未输出 tracked 文件，确认真实本地配置不会进入 Git。

## 2026-05-28 Agent 联网搜索第一版验证

### 验证范围

- 联网搜索作为 SearchRouter 工具能力接入，不替代 LLM 外脑。
- `config/search.local.json` 本地配置文件与环境变量覆盖可用。
- 默认 off 状态不触发网络请求。
- fake provider 可本地测试搜索链路。
- Tavily provider 通过官方 Python SDK 接入，缺少 SDK 或 API key 时返回可读错误。
- Agent 新增 `/search-status`、`/search-enable`、`/search-config-example [provider]` 和 `/search 关键词`。
- InnerBrain 可把“联网查一下 Python 版本”映射到 `/search Python 版本`，不会落到 LLM 兜底。
- 状态、日志和回复不输出真实 API key，`config/search.local.json` 不进入 Git。

### RED：SearchRouter 模块和 Agent 搜索入口缺失

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_search -v
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_search_status_command_reports_default_disabled_state tests.test_agent.AgentTests.test_search_command_returns_fake_provider_results tests.test_agent.AgentTests.test_search_command_reports_disabled_provider tests.test_agent.AgentTests.test_natural_language_web_search_uses_inner_brain_entry_without_llm tests.test_agent.AgentTests.test_search_enable_command_reports_local_config_path_without_api_key -v
```

结果：

- `tests.test_search` 因 `ModuleNotFoundError: No module named 'jarvis_lite.search'` 失败。
- Agent 搜索入口测试同样因缺少 `jarvis_lite.search` 无法导入。

### GREEN：目标测试通过

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_search -v
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_help_command_lists_llm_usage_command tests.test_agent.AgentTests.test_search_status_command_reports_default_disabled_state tests.test_agent.AgentTests.test_search_command_returns_fake_provider_results tests.test_agent.AgentTests.test_search_command_reports_disabled_provider tests.test_agent.AgentTests.test_natural_language_web_search_uses_inner_brain_entry_without_llm tests.test_agent.AgentTests.test_search_enable_command_reports_local_config_path_without_api_key tests.test_agent.AgentTests.test_inner_brain_status_command_reports_samples_and_thresholds tests.test_inner_brain.InnerBrainTests.test_describe_status_reports_sample_counts_and_thresholds -v
```

结果：

- `tests.test_search`：通过，`Ran 10 tests`，结果 `OK`。
- Agent/InnerBrain 目标测试：通过，`Ran 8 tests`，结果 `OK`。

### 专项回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_search tests.test_inner_brain tests.test_agent -v
```

结果：

- `tests.test_search tests.test_inner_brain tests.test_agent`：通过，`Ran 215 tests`，结果 `OK`。

### 依赖安装验证

命令：

```powershell
.\.venv\Scripts\python.exe -m pip index versions tavily-python
.\.venv\Scripts\python.exe -m pip install -e .
```

结果：

- `tavily-python` 当前可用版本包含 `0.7.24`。
- editable 安装成功，安装 `tavily-python 0.7.24`、`tiktoken 0.13.0`、`requests 2.34.2` 等依赖。

### 收尾验证

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe src\app.py --once "/search-status"
$env:JARVIS_LITE_SEARCH_PROVIDER='fake'; $env:JARVIS_LITE_SEARCH_FAKE_RESULTS='[{"title":"Python release","url":"https://python.example/release","snippet":"Python 版本摘要"}]'; .\.venv\Scripts\python.exe src\app.py --once "/search Python 版本"; Remove-Item Env:JARVIS_LITE_SEARCH_PROVIDER; Remove-Item Env:JARVIS_LITE_SEARCH_FAKE_RESULTS
git diff --check
Markdown 本地链接检查脚本
敏感信息扫描
git ls-files config/search.local.json
```

结果：

- 全量 `unittest`：通过，`Ran 399 tests`，结果 `OK`。
- 源码桌面 smoke：通过，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- `/search-status`：通过，输出默认 `联网搜索：未启用`、`Provider：off`、`网络调用：否（联网搜索未启用）`。
- fake provider `/search Python 版本`：通过，输出编号结果、URL 和摘要。
- `git diff --check`：退出 0，仅提示 LF/CRLF 工作区换行警告。
- Markdown 本地链接检查：通过，输出 `Markdown local links OK`。
- 敏感信息扫描：通过，输出 `Sensitive scan OK`。
- `git ls-files config/search.local.json` 未输出 tracked 文件，确认真实搜索配置不会进入 Git。

## 2026-05-28 InnerBrain 样本分类器优先迁移验证

### 验证范围

- 高频自然语言不再优先返回 `source=legacy_rule`。
- 问候、知识库摘要、最近上下文、最近文件、常用目录、更新、经验、确认/取消执行等高频意图由 seed 样本分类器命中。
- 未迁移完的复杂槽位规则继续可用，但标记为 `source=legacy_fallback`。
- `/inner-brain-status` 显示样本分类器优先和 legacy fallback 迁移期兼容状态。

### RED：旧规则仍作为主入口

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v
```

结果：

- 失败原因符合预期：`早上好`、`总结知识库`、`查看最近上下文` 等仍返回 `source=legacy_rule`。
- `/inner-brain-status` 仍显示 `legacy_rule：启用`，缺少样本分类器优先状态。
- 复杂槽位回归 `给 note.txt 打标签 项目` 返回 `legacy_rule`，未标记为 `legacy_fallback`。

### GREEN 与专项回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v
```

结果：

- `tests.test_inner_brain tests.test_agent`：通过，`Ran 207 tests`，结果 `OK`。

### 全量回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：

- 全量 `unittest`：通过，`Ran 401 tests`，结果 `OK`。

### 收尾验证

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe src\app.py --once "/inner-brain-status"
.\.venv\Scripts\python.exe src\app.py --once "/inner-brain-preview 早上好"
git diff --check
Markdown 本地链接检查脚本
敏感信息扫描
git ls-files config/llm.local.json config/search.local.json
```

结果：

- 全量 `unittest`：通过，`Ran 401 tests`，结果 `OK`。
- 源码桌面 smoke：通过，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- `/inner-brain-status` smoke：通过，输出样本分类器优先、`legacy_fallback` 迁移期兼容、`seed_sample：51 条`。
- `/inner-brain-preview 早上好` smoke：通过，输出 `意图：assistant.greeting`、`来源：seed_sample`、`策略：execute`。
- `git diff --check`：退出 0，仅提示 LF/CRLF 工作区换行警告。
- Markdown 本地链接检查：通过，输出 `Markdown local links OK`。
- 敏感信息扫描：通过，输出 `Sensitive scan OK`。
- `git ls-files config/llm.local.json config/search.local.json` 未输出 tracked 文件。

## 2026-05-28 InnerBrain 编号槽位迁移验证

### 验证范围

- 读取当前资料、读取编号资料、查看编号最近文件、导入编号最近文件、查看编号搜索结果、查看编号建议和执行编号建议不再依赖 `legacy_fallback` 主识别。
- 样本分类器负责识别意图，编号只作为 `result_index` 槽位抽取。
- `JarvisAgent` 继续复用既有最近资料、最近文件、搜索结果和经验建议执行链路。
- 未迁移的复杂槽位动作继续保留 `legacy_fallback`。

### RED：编号动作仍走 legacy fallback

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_numbered_object_intents_use_sample_classifier_slots -v
```

结果：

- 目标测试先失败，8 个样例均返回 `legacy.*` 意图。
- `读取这个资料` 返回 `legacy.read_recent_document`。
- `读取第二份资料` 返回 `legacy.read_numbered_recent_document`。
- `查看第二条结果` 返回 `legacy.read_numbered_search_result`。
- `执行第二条建议` 返回 `legacy.prepare_numbered_advice_suggestion_execution`。

### GREEN 与专项回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_numbered_object_intents_use_sample_classifier_slots -v
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain -v
.\.venv\Scripts\python.exe -m unittest tests.test_agent -v
```

结果：

- 目标测试通过，第一批编号动作均返回 `source=seed_sample`。
- `tests.test_inner_brain`：通过，`Ran 16 tests`，结果 `OK`。
- `tests.test_agent`：通过，`Ran 192 tests`，结果 `OK`。

### 收尾验证

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe src\app.py --once "/inner-brain-status"
.\.venv\Scripts\python.exe src\app.py --once "/inner-brain-preview 读取第二份资料"
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
```

结果：

- 全量 `unittest`：通过，`Ran 402 tests`，结果 `OK`。
- 源码桌面 smoke：通过，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- `/inner-brain-status`：通过，输出样本分类器优先、`legacy_fallback` 迁移期兼容和 `seed_sample：66 条`。
- `/inner-brain-preview 读取第二份资料`：通过，输出 `意图：document.read_numbered_recent`、`来源：seed_sample`、`策略：execute`、`编号：2`。
- `git diff --check`：退出 0，仅提示 LF/CRLF 工作区换行警告。
- Markdown 本地链接检查：通过，输出 `Markdown local links OK`。
- 敏感信息差异扫描：通过，输出 `Sensitive diff scan OK`。
- `git ls-files config/llm.local.json config/search.local.json` 未输出 tracked 文件。

## 2026-05-28 SearchRouter + LLMRouter 0.1.4 安装包验证

### 验证范围

- 项目版本提升到 `0.1.4`。
- `pyproject.toml`、`jarvis_lite.__version__` 和版本测试保持一致。
- README 覆盖安装说明更新为 `0.1.4`。
- 后续构建安装器并验证安装脚本、完成消息和打包后 smoke。

### RED：版本仍停留在 0.1.3

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：

- 测试按预期失败，`pyproject.toml` 中版本为 `0.1.3`，期望 `0.1.4`。

### GREEN：版本一致性通过

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：

- 1 项通过，结果 `OK`。
- `pyproject.toml` 与 `jarvis_lite.__version__` 已更新为 `0.1.4`。

### 全量、源码 smoke 与打包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'Jarvis Lite 0.1.4 installed|DisplayVersion|Existing app files|User data kept|taskkill'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0.1.4 installation finished|TargetName'
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.1.4.exe -Force
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.1.4.exe
```

结果：

- 全量 `unittest discover`：通过，`Ran 411 tests`，结果 `OK`。
- 源码桌面 smoke：通过，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- 安装器构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 打包后 `JarvisLite.exe --smoke`：通过，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- `install.cmd` 包含 `taskkill`、`DisplayVersion /d "0.1.4"`、`Jarvis Lite 0.1.4 installed`、覆盖安装提示和用户数据保留提示。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.1.4 installation finished` 和 `TargetName=E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 版本化副本生成成功：`E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.1.4.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.1.4.exe` 大小均为 57,057,280 bytes。
- `git diff --check`：退出 0，仅提示 LF/CRLF 工作区换行警告。
- Markdown 本地链接检查：通过，输出 `Markdown local links OK`。
- 敏感信息差异扫描：通过，输出 `Sensitive diff scan OK`。
- `git ls-files config/llm.local.json config/search.local.json` 未输出 tracked 文件。

## 2026-05-28 InnerBrain 标签槽位迁移验证

### 验证范围

- 当前资料/结果打标签、编号资料打标签、编号搜索结果打标签不再依赖 `legacy_fallback` 主识别。
- 标签组读取、标签组批量打标签预览、编号批量标签历史读取不再依赖 `legacy_fallback` 主识别。
- 样本分类器负责识别语义，解析函数只抽 `tags`、`alias`、`result_index`。
- 显式文件名标签命令继续保留 `legacy_fallback`，避免混淆文件名/路径槽位迁移。

### RED：标签动作仍走 legacy fallback

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_tag_intents_use_sample_classifier_slots -v
```

结果：

- 目标测试先失败，7 个样例均返回 `legacy.*` 意图。
- `给这个资料打标签 项目 Python` 返回 `legacy.tag_recent_document`。
- `给第二份资料打标签 项目 Python` 返回 `legacy.tag_numbered_recent_document`。
- `给第二条结果打标签 运行环境` 返回 `legacy.tag_numbered_search_result`。
- `给项目标签资料都打标签 归档` 返回 `legacy.preview_tagged_documents_tagging`。
- `读取项目标签资料` 返回 `legacy.read_tagged_documents`。
- `读取第一条标签历史资料` 返回 `legacy.read_tagged_documents_history_documents`。

### GREEN 与专项回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_tag_intents_use_sample_classifier_slots -v
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain -v
.\.venv\Scripts\python.exe -m unittest tests.test_agent -v
```

结果：

- 目标测试通过，标签类迁移样例均返回 `source=seed_sample`。
- `tests.test_inner_brain`：通过，`Ran 17 tests`，结果 `OK`。
- `tests.test_agent`：通过，`Ran 192 tests`，结果 `OK`。

### 收尾验证

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe src\app.py --once "/inner-brain-status"
.\.venv\Scripts\python.exe src\app.py --once "/inner-brain-preview 给第二份资料打标签 项目 Python"
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
```

结果：

- 全量 `unittest`：通过，`Ran 403 tests`，结果 `OK`。
- 源码桌面 smoke：通过，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- `/inner-brain-status`：通过，输出样本分类器优先、`legacy_fallback` 迁移期兼容和 `seed_sample：75 条`。
- `/inner-brain-preview 给第二份资料打标签 项目 Python`：通过，输出 `意图：document.tag_numbered_recent`、`来源：seed_sample`、`标签：项目、Python`、`编号：2`。
- `git diff --check`：退出 0，仅提示 LF/CRLF 工作区换行警告。
- Markdown 本地链接检查：通过，输出 `Markdown local links OK`。
- 敏感信息差异扫描：通过，输出 `Sensitive diff scan OK`。
- `git ls-files config/llm.local.json config/search.local.json` 未输出 tracked 文件。

## 2026-05-28 InnerBrain 文件路径、目录和经验槽位迁移验证

### 验证范围

- 显式文件读取、显式文件/目录导入知识库不再依赖 `legacy_fallback` 主识别。
- 打开盘符、打开/整理常用目录、打开/整理最近目录不再依赖 `legacy_fallback` 主识别。
- 经验记录、经验搜索和经验建议不再依赖 `legacy_fallback` 主识别。
- 样本分类器负责识别语义，解析函数只抽 `path`、`source`、`alias`、`experience` 和 `query` 槽位。

### RED：文件路径、目录和经验动作仍走 legacy fallback

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_file_path_intents_use_sample_classifier_slots -v
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_directory_intents_use_sample_classifier_slots -v
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_experience_intents_use_sample_classifier_slots -v
```

结果：

- 文件路径目标测试先失败，`读取 manual.md`、`查看 data/manual.txt`、`把 "C:/work/outside natural.md" 导入知识库` 和 `导入 E:/docs/manual.pdf 到资料库` 均返回 `command`/`legacy_fallback`。
- 目录目标测试先失败，`打开D盘`、`打开项目目录`、`整理项目目录`、`打开这个目录` 和 `整理这个目录` 均返回 `legacy.*`。
- 经验目标测试先失败，`记住这个经验：导入资料后先打标签`、`搜索经验 导入`、`经验查询 日报`、`我该怎么导入资料` 和 `导入资料有什么建议` 均返回 `command`/`legacy_fallback`。

### GREEN 与专项回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_file_path_intents_use_sample_classifier_slots -v
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_directory_intents_use_sample_classifier_slots -v
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_experience_intents_use_sample_classifier_slots -v
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v
```

结果：

- 三个目标测试均通过，新增迁移样例均返回 `source=seed_sample`。
- `tests.test_inner_brain tests.test_agent`：通过，`Ran 212 tests`，结果 `OK`。

### 收尾验证

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe src\app.py --once "/inner-brain-status"
.\.venv\Scripts\python.exe src\app.py --once "/inner-brain-preview 导入 E:/docs/manual.pdf 到资料库"
.\.venv\Scripts\python.exe src\app.py --once "/inner-brain-preview 打开项目目录"
.\.venv\Scripts\python.exe src\app.py --once "/inner-brain-preview 我该怎么导入资料"
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
```

结果：

- 全量 `unittest`：通过，`Ran 406 tests`，结果 `OK`。
- 源码桌面 smoke：通过，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- `/inner-brain-status`：通过，输出样本分类器优先、`legacy_fallback` 迁移期兼容和 `seed_sample：90 条`。
- `/inner-brain-preview 导入 E:/docs/manual.pdf 到资料库`：通过，输出 `意图：knowledge.import`、`来源：seed_sample`、`命令：/import "E:/docs/manual.pdf"`。
- `/inner-brain-preview 打开项目目录`：通过，输出 `意图：directory.open_alias`、`来源：seed_sample`、`别名：项目`。
- `/inner-brain-preview 我该怎么导入资料`：通过，输出 `意图：experience.advice`、`来源：seed_sample`、`命令：/experience-advice 导入资料`。
- `git diff --check`：退出 0，仅提示 LF/CRLF 工作区换行警告。
- Markdown 本地链接检查：通过，输出 `Markdown local links OK`。首次运行脚本因根目录 Markdown 文件 parent path 为空而报错，修正脚本后复跑通过。
- 敏感信息差异扫描：通过，输出 `Sensitive diff scan OK`。
- `git ls-files config/llm.local.json config/search.local.json` 未输出 tracked 文件。

## 2026-05-28 SearchRouter + LLMRouter 搜索总结组合验证

### 验证范围

- `/search` 成功返回联网搜索结果后写入最近联网搜索上下文。
- 最近联网搜索上下文会持久化，并出现在“查看最近上下文”和 `/llm-context-preview`。
- `/search-summary 关键词` 与自然语言“联网查一下...并总结”先搜索，再把来源、URL 和摘要交给 LLM 外脑总结。
- 普通搜索仍不自动调用 LLM。

### RED：搜索结果未进入上下文，查一下并总结被普通搜索吞掉

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_web_search_summary_uses_sample_classifier_slot -v
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_search_command_records_web_results_in_recent_context_and_llm_context tests.test_agent.AgentTests.test_natural_language_web_search_summary_uses_search_results_as_llm_context -v
```

结果：

- InnerBrain 目标测试先失败，`联网查一下 Python 版本并总结` 返回 `web.search` 而不是 `web.search_summarize`。
- Agent 目标测试先失败，`/search Python 版本` 后“查看最近上下文”仍显示还没有记录。
- Agent 目标测试先失败，`联网查一下 Python 版本并总结` 只执行普通搜索，未追加 `LLM 外脑总结`。

### GREEN 与专项回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_web_search_summary_uses_sample_classifier_slot -v
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_search_command_records_web_results_in_recent_context_and_llm_context tests.test_agent.AgentTests.test_natural_language_web_search_summary_uses_search_results_as_llm_context -v
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v
```

结果：

- 目标测试通过，`web.search_summarize` 映射到 `/search-summary Python 版本`。
- `/search` 写入最近联网搜索上下文，重启 Agent 后 `/llm-context-preview` 仍包含 URL 和摘要。
- `/search-summary` 组合流程把搜索来源放进 LLM context，并展示 `LLM 外脑总结：...`。
- `tests.test_inner_brain tests.test_agent`：通过，`Ran 215 tests`，结果 `OK`。

### 补充 RED/GREEN：LLM 关闭和教学 intent 一致性

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_search_summary_reports_llm_disabled_without_crashing tests.test_agent.AgentTests.test_inner_brain_teach_search_summary_uses_search_summary_intent -v
```

RED 结果：

- `/search-summary Python 版本` 在 LLM 关闭时触发 `AttributeError: 'NoneType' object has no attribute 'usage'`。
- `/inner-brain-teach 查版本 => /search-summary Python 版本` 保存的 runtime intent 是 `web.search_summary`，与 seed intent `web.search_summarize` 不一致。

GREEN 命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_search_summary_reports_llm_disabled_without_crashing tests.test_agent.AgentTests.test_inner_brain_teach_search_summary_uses_search_summary_intent tests.test_inner_brain.InnerBrainTests.test_web_search_summary_uses_sample_classifier_slot -v
```

GREEN 结果：

- 3 项通过，结果 `OK`。
- LLM 关闭时 `/search-summary` 保留搜索来源并提示 `/llm-status`、`/llm-enable`。
- `/search-summary` 教学样本保存为 `web.search_summarize`，并通过 command slot 执行教学目标。

### 收尾验证

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe src\app.py --once "/inner-brain-status"
.\.venv\Scripts\python.exe src\app.py --once "/inner-brain-preview 联网查一下 Python 版本并总结"
.\.venv\Scripts\python.exe src\app.py --once "/search-summary Python 版本"
fake 搜索 + fake LLM 环境变量 smoke：联网查一下 Python 版本并总结
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
```

结果：

- `tests.test_inner_brain tests.test_agent`：通过，`Ran 217 tests`，结果 `OK`。
- 全量 `unittest discover`：通过，`Ran 411 tests`，结果 `OK`。
- 源码桌面 smoke：通过，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- `/inner-brain-status`：通过，输出样本分类器优先、`legacy_fallback` 迁移期兼容和 `seed_sample：92 条`。
- `/inner-brain-preview 联网查一下 Python 版本并总结`：通过，输出 `意图：web.search_summarize`、`来源：seed_sample`、`命令：/search-summary Python 版本`。
- 默认 `/search-summary Python 版本`：通过，搜索关闭时输出联网搜索未启用、`/search-status` 和 `/search-enable`。
- fake 搜索 + fake LLM 自然语言 smoke：通过，输出 `联网搜索：Python 版本`、URL 来源和 `LLM 外脑总结：Python 3.13 是当前发布线。`。
- `git diff --check`：退出 0，仅提示 LF/CRLF 工作区换行警告。
- Markdown 本地链接检查：通过，输出 `Markdown local links OK`。
- 敏感信息差异扫描：通过，输出 `Sensitive diff scan OK`。
- `git ls-files config/llm.local.json config/search.local.json` 未输出 tracked 文件。
