# 2026-06-09 验证记录

> 日期：2026-06-09
> 执行者：Codex

## 0.140.0 偏好应用确认记录与撤销第一阶段

- 目标：在 `/preference-apply-confirm [输入文本]` 显式确认成功后写入运行态确认记录，新增 `/preference-apply-history` 查看最近记录，新增 `/preference-apply-undo 编号或ID` 只撤销确认记录。当前阶段不把偏好自动接入普通聊天、LLM prompt、SearchRouter、InnerBrain、路由或执行决策；撤销不会停用偏好、删除偏好或回滚已经展示的输出。
- 环境：系统 Python 为 `3.13.13`；创建 `.venv` 后执行 `.\.venv\Scripts\python.exe -m pip install -e ".[desktop-build]"` 安装源码和桌面打包依赖。
- 基线：`.\.venv\Scripts\python.exe -m unittest tests.test_project_metadata -v` 通过；`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 通过，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- RED：目标测试先失败；失败点为缺少偏好应用确认历史 helper、Agent `/preference-apply-history` 和 `/preference-apply-undo` 返回未知命令、版本仍为 `0.139.0`。
- GREEN：目标测试通过，覆盖确认成功写历史、无启用偏好或冲突拒绝时不写历史、撤销只改确认记录状态、Agent 命令入口、配置管家入口、LLM provider instructions 不包含新增历史/撤销命令和版本一致性。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_preferences tests.test_memory_config_candidates tests.test_memory_config_manager tests.test_agent tests.test_llm tests.test_project_metadata -v`，`Ran 453 tests`，`OK`。
- 调试留痕：Agent 测试首次发现 `_runtime_context()` 会覆盖 helper 已写入的运行态字段；修复为保存时保留 `persisted_context.recent_preference_applications`。
- 断线恢复复核：`日志.txt` 尾部显示上次断在命令行 smoke，原因是临时脚本误导入不存在的 `load_preferences`；本轮改用真实公开 API 复跑 smoke。
- 追加 RED：补充 `test_confirmed_preference_application_keeps_same_second_duplicate_history`，固定 `_now_iso()` 后连续两次相同输入确认先失败，失败点为运行态确认历史只剩 1 条。
- 追加 GREEN：确认 ID 生成在已有历史存在同 ID 时追加序号参与哈希；同秒重复确认保留两条记录，ID 分别保持 `prefapp-...` 形态。
- 目标复验：`.\.venv\Scripts\python.exe -m unittest tests.test_preferences tests.test_agent tests.test_llm tests.test_project_metadata -v`，`Ran 438 tests`，`OK`。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，`Ran 770 tests`，`OK`。
- 命令行 smoke：临时项目内保存并启用偏好，执行 `/preference-apply-confirm 帮我总结知识库`、`/preference-apply-history`、`/preference-apply-undo 1`，再用同秒重复确认和确认 ID 撤销复验；输出 `preference-apply-audit-smoke OK`，同秒两条确认 ID 为 `prefapp-eef3c515fe` 与 `prefapp-8e505eac4e`，撤销后偏好仍保持启用。
- 源码桌面 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 退出码 0，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- Windows 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 成功，生成 `E:\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；复制版本化安装包 `E:\ai\jarvis-lite-dist\JarvisLiteSetup-0.140.0.exe`，大小 `59,715,584` 字节。构建日志包含既有 `Hidden import "tzdata" not found!` 警告和第三方 `pyautogui` 的 `SyntaxWarning: invalid escape sequence '\e'`。
- 安装包元数据：`E:\ai\jarvis-lite-dist\windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.140.0"` 和 `Jarvis Lite 0.140.0 installed`；`E:\ai\jarvis-lite-dist\JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.140.0 installation finished`；`JarvisLite.version.txt` 的 `filevers/prodvers` 为 `(0, 140, 0, 0)`；`desktop-exe\JarvisLite.exe` 的 `FileVersion/ProductVersion` 均为 `0.140.0`。
- 打包后 smoke：`E:\ai\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 重定向复验退出码 0，stdout 包含 `desktopPetWindow`，stderr 为空，无残留 `JarvisLite` 进程；PowerShell 表格中中文 stdout 有编码显示问题，不影响 smoke 结论。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 提示；Markdown 本地链接 674 项通过；严格真实密钥形态扫描 722 个文本文件无命中；`config/llm.local.json`、`config/search.local.json`、`word/inner-brain-evaluation-report.md` 不存在；README BOM 为 `EF-BB-BF`。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 提示；Markdown 本地链接 666 项通过；严格真实密钥形态扫描 25 个公开变更/新增文件无命中；`config/llm.local.json`、`config/search.local.json`、`word/inner-brain-evaluation-report.md` 不存在；README BOM 为 `EF-BB-BF`。

## 0.141.0 偏好进入普通回复上下文第一阶段

- 目标：只把最近一条仍有效的偏好应用确认记录接入普通 LLM fallback 上下文和 `/llm-context-preview`。有效条件为状态仍是 `confirmed`、当前已启用偏好 ID 和文本与确认记录完全一致、且当前已启用偏好无明显冲突。当前阶段不把偏好管理命令加入 LLM 白名单，不改变 SearchRouter、InnerBrain、路由、授权层或桌面执行决策。
- RED：目标测试先失败；失败点为缺少 `describe_preference_reply_context()`、`/llm-context-preview` 不展示偏好确认上下文、普通 LLM fallback 未收到偏好确认上下文、版本仍为 `0.140.0`。
- GREEN：新增 `describe_preference_reply_context()` 和最新有效确认记录选择逻辑；`JarvisAgent._llm_context_lines()` 追加有效偏好确认上下文；撤销确认、停用偏好、删除偏好或启用集合变化后旧确认自动失效。
- 目标复验：`.\.venv\Scripts\python.exe -m unittest tests.test_preferences tests.test_agent.AgentTests.test_llm_context_preview_includes_confirmed_preference_application tests.test_agent.AgentTests.test_llm_fallback_receives_confirmed_preference_context_until_undone tests.test_llm.LLMTests.test_openai_provider_instructions_list_supported_agent_commands tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v`，`Ran 23 tests`，`OK`。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_preferences tests.test_memory_config_candidates tests.test_memory_config_manager tests.test_agent tests.test_llm tests.test_project_metadata -v`，`Ran 458 tests in 6.834s`，`OK`。首次因更新 manifest 夹具仍为 `0.140.1` 失败，提升到 `0.141.1` 后复跑通过。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，`Ran 774 tests in 8.623s`，`OK`。
- 命令行 smoke：临时项目内保存并启用偏好，确认前 `/llm-context-preview` 不包含 `已确认偏好应用`；执行 `/preference-apply-confirm 请回答普通问题` 后，`/llm-context-preview` 和普通 LLM fallback context 均包含 `prefapp-...` 与偏好文本；执行 `/preference-apply-undo 1` 后，后续 preview 和普通 LLM fallback context 不再包含确认上下文；输出 `preference-reply-context-smoke OK`。
- 源码桌面 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 退出码 0，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`，无残留 `JarvisLite` 进程。
- Windows 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 成功，生成 `E:\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；复制版本化安装包 `E:\ai\jarvis-lite-dist\JarvisLiteSetup-0.141.0.exe`，大小 `59,711,488` 字节，时间戳 `2026-06-09 17:31:22`。构建日志包含既有 `Hidden import "tzdata" not found!` 警告和第三方 `pyautogui` 的 `SyntaxWarning: invalid escape sequence '\e'`。
- 安装包元数据：`E:\ai\jarvis-lite-dist\windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.141.0"` 和 `Jarvis Lite 0.141.0 installed`；`E:\ai\jarvis-lite-dist\JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.141.0 installation finished`；`JarvisLite.version.txt` 的 `filevers/prodvers` 为 `(0, 141, 0, 0)`；`desktop-exe\JarvisLite.exe` 的 `FileVersion/ProductVersion` 均为 `0.141.0`。
- 打包后 smoke：`E:\ai\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 重定向复验退出码 0，stdout 包含 `desktopPetWindow`，stderr 为空，无残留 `JarvisLite` 进程；PowerShell 表格中中文 stdout 有编码显示问题，不影响 smoke 结论。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 提示；Markdown 本地链接 671 项通过；严格真实密钥形态扫描 14 个公开变更/新增文件无命中；首次密钥扫描被历史方案文件名中的 `sk-...-plan` 误报，收窄到真实 key 形态后复跑通过；`config/llm.local.json`、`config/search.local.json`、`word/inner-brain-evaluation-report.md` 不存在；README BOM 为 `EF-BB-BF`。

## 0.142.0 偏好格式化本地回答第一阶段

- 目标：只在最近一条偏好应用确认记录仍有效时，把确认 ID、确认输入和偏好 ID/文本作为可审计附注追加到本地知识库命中回答和长期记忆兜底回答。有效条件沿用 v146：状态为 `confirmed`、当前已启用偏好 ID 和文本与确认记录完全一致、且当前已启用偏好无明显冲突。当前阶段不改变检索、路由、LLM 命令白名单、SearchRouter、InnerBrain、授权层或桌面执行决策。
- RED：目标测试先失败；失败点为缺少 `describe_preference_local_answer_note()`、本地知识库 `/ask` 回答不展示 `已确认偏好格式化：prefapp-...`、长期记忆兜底不展示该附注、版本仍为 `0.141.0`。
- GREEN：新增 `describe_preference_local_answer_note()`，复用有效确认记录选择逻辑；`JarvisAgent._answer_from_data()` 和长期记忆兜底返回会通过 `_apply_preference_local_answer_note()` 追加附注；撤销确认、停用偏好、删除偏好或启用集合变化后旧确认自动失效。
- 目标复验：`.\.venv\Scripts\python.exe -m unittest tests.test_preferences.PreferenceTests.test_preference_local_answer_note_requires_enabled_preferences_to_match_confirmation tests.test_agent.AgentTests.test_local_data_answer_includes_confirmed_preference_note_until_undone tests.test_agent.AgentTests.test_memory_fallback_includes_confirmed_preference_note_until_undone tests.test_llm.LLMTests.test_openai_provider_instructions_list_supported_agent_commands tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v`，`Ran 5 tests`，`OK`。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_preferences tests.test_agent tests.test_llm tests.test_project_metadata -v`，`Ran 445 tests in 6.577s`，`OK`。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，`Ran 777 tests in 8.625s`，`OK`。
- 命令行 smoke：临时项目保存并启用偏好，确认前 `/ask` 和长期记忆兜底均无 `已确认偏好格式化`；确认后 `/ask Jarvis Lite 推荐使用什么 Python 版本？` 保留 `data/runtime.md:1` 来源并追加 `prefapp-...` 附注，长期记忆兜底同样追加附注；撤销后两类本地回答均移除附注；输出 `preference-local-answer-format-smoke OK`。首次 smoke 失败根因是脚本误以为长期记忆兜底不会先尝试 LLM fallback，修正断言后通过。
- 源码桌面 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 退出码 0，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- Windows 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 成功，生成 `E:\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；复制版本化安装包 `E:\ai\jarvis-lite-dist\JarvisLiteSetup-0.142.0.exe`，大小 `59,715,584` 字节，时间戳 `2026-06-09 18:12:44`。构建日志包含既有 `Hidden import "tzdata" not found!` 警告和第三方 `pyautogui` 的 `SyntaxWarning: invalid escape sequence '\e'`。
- 安装包元数据：`E:\ai\jarvis-lite-dist\windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.142.0"` 和 `Jarvis Lite 0.142.0 installed`；`E:\ai\jarvis-lite-dist\JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.142.0 installation finished`；`JarvisLite.version.txt` 的 `filevers/prodvers` 为 `(0, 142, 0, 0)`；`desktop-exe\JarvisLite.exe` 的 `FileVersion/ProductVersion` 均为 `0.142.0`。
- 打包后 smoke：`E:\ai\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 重定向复验退出码 0，stdout 包含 `desktopPetWindow`，stderr 为空，无残留 `JarvisLite` 进程；PowerShell 表格中中文 stdout 有编码显示问题，不影响 smoke 结论。
