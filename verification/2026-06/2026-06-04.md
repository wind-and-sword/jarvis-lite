# 2026-06-04 验证记录

> 日期：2026-06-04
> 执行者：Codex

## 0.132.0 应用别名确认固化与撤销第一阶段

- 目标：在联系人别名确认固化后，支持应用别名候选的显式确认固化和撤销；`/config-candidate-confirm 编号` 只处理活跃 `app_alias`，写入 `config/apps.local.json` 的 `apps.<app_id>.aliases` 并标记已固化；`/config-candidate-undo 编号` 只处理历史中的已固化应用别名，删除对应别名并恢复候选为活跃。当前阶段不启动应用、不切换窗口、不点击、不输入，不把确认/撤销命令加入 LLM 白名单。
- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_app_registry.AppRegistryTests.test_app_alias_store_writes_matches_and_removes_alias_without_deleting_path tests.test_app_registry.AppRegistryTests.test_app_alias_candidate_requires_alias_and_registered_app_query tests.test_memory_config_candidates.MemoryConfigCandidateTests.test_confirm_app_alias_candidate_persists_and_undo_removes_alias tests.test_memory_config_candidates.MemoryConfigCandidateTests.test_confirm_supports_only_confirmed_high_risk_candidate_types tests.test_agent.AgentTests.test_config_candidate_confirm_and_undo_app_alias tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v` 先失败；失败点为缺少应用别名解析/写入/删除 helper、候选确认暂不支持 `app_alias`、Agent 尚未接入应用别名确认/撤销、版本仍为 `0.131.0`。
- GREEN：同一目标命令 6 项通过；复验结果为 `Ran 6 tests in 0.349s`，`OK`。
- 调试复验：命令行 smoke 首次暴露 `_read_local_registry_payload()` 中循环变量遮蔽顶层 payload，撤销应用别名后可能把 `aliases` 或 `path` 写到 JSON 根节点；修复后复跑 `tests.test_app_registry.AppRegistryTests.test_app_alias_store_writes_matches_and_removes_alias_without_deleting_path -v`，结果 `Ran 1 test in 0.040s`，`OK`。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_app_registry tests.test_memory_config_candidates tests.test_memory_config_manager tests.test_agent tests.test_llm tests.test_project_metadata -v` 通过，`Ran 437 tests in 20.043s`，`OK`。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v` 通过，`Ran 743 tests in 25.870s`，`OK`。
- 命令行 smoke：使用临时项目目录创建 `JarvisAgent`，执行 `/config-candidate-add app_alias 晨会入口 => chrome`、`/config-candidate-apply 1`、`/config-candidate-confirm 1`、`/app-find 晨会入口`、`/config-candidate-history`、`/config-candidate-undo 1`、`/app-find 晨会入口` 和 `/config-candidates`；确认后 `apps.local.json` 写入 Chrome 别名，`/app-find 晨会入口` 命中 `Chrome (chrome)`，撤销后别名被删除且候选恢复为活跃。
- 源码桌面 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 退出码 0，输出包含 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 在 IExpress 阶段超过工具超时，但后台 `iexpress`/`makecab` 进程随后完成并刷新 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；已复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.132.0.exe`，大小 `60448768` 字节，时间戳 `2026/6/4 14:11:28`。PyInstaller 报告既有 `Hidden import "tzdata" not found!` 警告，并出现第三方 `pyautogui` 的 `SyntaxWarning: invalid escape sequence '\e'`。
- 安装包元数据：`windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.132.0"` 和 `Jarvis Lite 0.132.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.132.0 installation finished`；`JarvisLite.version.txt` 的 `filevers/prodvers` 为 `(0, 132, 0, 0)`；`desktop-exe\JarvisLite.exe` 的 `FileVersion/ProductVersion` 均为 `0.132.0`。
- 打包后 smoke：使用 `Start-Process -Wait -PassThru -RedirectStandardOutput -RedirectStandardError` 执行 `E:\oyzj\ai\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke`，退出码 0，stdout 包含 `desktopPetWindow`，stderr 为空，未留下新的 `JarvisLite` 进程。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接检查通过，检查 `620` 个本地链接；敏感信息形态扫描通过，扫描 `19` 个变更/新增公开文件；`config/llm.local.json`、`config/search.local.json`、`word/inner-brain-evaluation-report.md` 不存在；README BOM 为 `EF-BB-BF`。

## 0.131.0 联系人别名确认固化与撤销第一阶段

- 目标：在高风险候选确认草稿之后，先支持联系人别名候选的显式确认固化和撤销；`/config-candidate-confirm 编号` 只处理活跃 `contact_alias`，写入 `config/contacts.local.json` 并标记已固化；`/config-candidate-undo 编号` 只处理历史中的已固化联系人别名，删除对应别名并恢复候选为活跃。当前阶段不查找真实联系人、不点击、不输入、不发送消息，不把确认/撤销命令加入 LLM 白名单。
- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_contacts tests.test_memory_config_candidates.MemoryConfigCandidateTests.test_confirm_contact_alias_candidate_persists_and_undo_removes_alias tests.test_memory_config_candidates.MemoryConfigCandidateTests.test_confirm_only_supports_contact_alias_in_first_stage tests.test_memory_config_candidates.MemoryConfigCandidateTests.test_apply_high_risk_candidates_returns_confirmation_draft_without_writing_config tests.test_memory_config_manager.MemoryConfigManagerTests.test_describe_memory_config_manager_reports_empty_storage tests.test_memory_config_manager.MemoryConfigManagerTests.test_describe_memory_config_manager_masks_provider_api_keys tests.test_agent.AgentTests.test_config_candidate_commands_require_arguments_and_are_listed tests.test_agent.AgentTests.test_config_candidate_apply_command_keeps_unsupported_candidate_active tests.test_agent.AgentTests.test_config_candidate_confirm_and_undo_contact_alias tests.test_llm.LLMTests.test_openai_provider_instructions_list_supported_agent_commands tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v` 先失败；失败点为缺少 `jarvis_lite.contacts`、Agent 尚未接入 `/config-candidate-confirm` 和 `/config-candidate-undo`、确认草稿缺少确认命令提示、版本仍为 `0.130.0`。GREEN 前修正 RED 测试中删除后才采样保存状态的时间点错误。
- GREEN：同一目标命令 12 项通过；复验结果为 `Ran 12 tests in 0.429s`，`OK`。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_contacts tests.test_memory_config_candidates tests.test_memory_config_manager tests.test_agent tests.test_llm tests.test_project_metadata -v` 通过，`Ran 428 tests in 17.105s`，`OK`。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v` 通过，`Ran 739 tests in 21.709s`，`OK`。
- 命令行 smoke：使用临时项目目录创建 `JarvisAgent`，执行 `/config-candidate-add contact_alias 小王 => 微信联系人王工`、`/config-candidate-apply 1`、`/config-candidate-confirm 1`、`/config-manager-status`、`/config-candidate-history`、`/config-candidate-undo 1` 和 `/config-candidates`；确认草稿包含 `确认固化：/config-candidate-confirm 1`，确认后 `contacts.local.json` 记录 `小王`，配置管家显示 `联系人别名：1 个`，历史显示 `1. 已固化 联系人别名：小王 => 微信联系人王工`，撤销后 `contacts.local.json` 保留空列表且候选恢复为活跃。
- 源码桌面 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 退出码 0，输出包含 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 退出码 0，生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；已复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.131.0.exe`，大小 `60448768` 字节。PyInstaller 报告既有 `Hidden import "tzdata" not found!` 警告，并出现第三方 `pyautogui` 的 `SyntaxWarning: invalid escape sequence '\e'`。
- 安装包元数据：`windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.131.0"` 和 `Jarvis Lite 0.131.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.131.0 installation finished`；`JarvisLite.version.txt` 的 `filevers/prodvers` 为 `(0, 131, 0, 0)`；`desktop-exe\JarvisLite.exe` 的 `FileVersion/ProductVersion` 均为 `0.131.0`。
- 打包后 smoke：使用 `Start-Process -Wait -PassThru -RedirectStandardOutput -RedirectStandardError` 执行 `E:\oyzj\ai\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke`，退出码 0，stdout 包含 `desktopPetWindow`，stderr 为空，未留下新的 `JarvisLite` 进程。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接检查通过，检查 `616` 个本地链接；敏感信息形态扫描通过，扫描 `21` 个变更/新增公开文件；`config/llm.local.json`、`config/search.local.json`、`word/inner-brain-evaluation-report.md` 不存在；README BOM 为 `EF-BB-BF`。

## 0.130.0 任务失败复盘窗口与授权摘要第一阶段

- 目标：任务失败复盘保存并展示当前窗口摘要、路由摘要和授权摘要；`/task-fail-capture` 在截图/OCR 失败复盘中同步携带窗口上下文；当前阶段不切换窗口、不点击、不输入、不自动重新执行外部动作。
- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_window_state.WindowStateTests.test_describe_task_window_context_returns_compact_foreground_summary tests.test_task_state.TaskStateTests.test_task_failure_records_window_route_and_authorization_context tests.test_agent.AgentTests.test_task_fail_command_records_window_context_in_replay tests.test_agent.AgentTests.test_task_fail_capture_command_records_screen_ocr_context tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v` 先失败；失败点为缺少 `describe_task_window_context`、`record_task_failure()` 尚不接收 `window_context`、Agent 尚未暴露同名 helper、项目版本仍为 `0.129.0`。
- GREEN：同一目标命令 5 项通过；复验结果为 `Ran 5 tests in 0.203s`，`OK`。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_task_state tests.test_agent tests.test_window_state tests.test_llm tests.test_project_metadata -v` 通过，`Ran 429 tests in 15.244s`，`OK`。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v` 通过，`Ran 734 tests in 19.108s`，`OK`。
- 命令行 smoke：使用临时项目目录创建 `JarvisAgent`，执行 `/task-start 检查窗口复盘 smoke`、`/task-fail 当前窗口不符合预期`、`/task-status`；复盘正文包含 `窗口上下文：当前窗口：...`、`路由摘要：command / /task-fail`、`授权摘要：explicit_command direct_execute`，状态页包含 `窗口：当前窗口：...`、`路由：command / /task-fail`、`授权：explicit_command direct_execute`，运行态 `window_context` 以 `当前窗口：` 开头。
- 源码桌面 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 退出码 0，输出包含 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 退出码 0，生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；已复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.130.0.exe`，大小 `60440576` 字节。PyInstaller 仅报告既有 `Hidden import "tzdata" not found!` 警告。
- 安装包元数据：`windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.130.0"` 和 `Jarvis Lite 0.130.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.130.0 installation finished`；`JarvisLite.version.txt` 的 `filevers/prodvers` 为 `(0, 130, 0, 0)`；`desktop-exe\JarvisLite.exe` 的 `FileVersion/ProductVersion` 均为 `0.130.0`。
- 打包后 smoke：`E:\oyzj\ai\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 退出码 0，stdout 包含 `desktopPetWindow`，stderr 为空，未留下新的 `JarvisLite` 进程。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接检查通过，检查 `612` 个本地链接；敏感信息形态扫描通过，扫描 `20` 个变更/新增公开文件；`config/llm.local.json`、`config/search.local.json`、`word/inner-brain-evaluation-report.md` 不存在；README BOM 为 `EF-BB-BF`。
