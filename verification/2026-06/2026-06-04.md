# 2026-06-04 验证记录

> 日期：2026-06-04
> 执行者：Codex

## 0.130.0 任务失败复盘窗口与授权摘要第一阶段

- 目标：任务失败复盘保存并展示当前窗口摘要、路由摘要和授权摘要；`/task-fail-capture` 在截图/OCR 失败复盘中同步携带窗口上下文；当前阶段不切换窗口、不点击、不输入、不自动重新执行外部动作。
- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_window_state.WindowStateTests.test_describe_task_window_context_returns_compact_foreground_summary tests.test_task_state.TaskStateTests.test_task_failure_records_window_route_and_authorization_context tests.test_agent.AgentTests.test_task_fail_command_records_window_context_in_replay tests.test_agent.AgentTests.test_task_fail_capture_command_records_screen_ocr_context tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v` 先失败；失败点为缺少 `describe_task_window_context`、`record_task_failure()` 尚不接收 `window_context`、Agent 尚未暴露同名 helper、项目版本仍为 `0.129.0`。
- GREEN：同一目标命令 5 项通过；复验结果为 `Ran 5 tests in 0.203s`，`OK`。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_task_state tests.test_agent tests.test_window_state tests.test_llm tests.test_project_metadata -v` 通过，`Ran 429 tests in 15.244s`，`OK`。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v` 通过，`Ran 734 tests in 19.108s`，`OK`。
- 命令行 smoke：使用临时项目目录创建 `JarvisAgent`，执行 `/task-start 检查窗口复盘 smoke`、`/task-fail 当前窗口不符合预期`、`/task-status`；复盘正文包含 `窗口上下文：当前窗口：...`、`路由摘要：command / /task-fail`、`授权摘要：explicit_command direct_execute`，状态页包含 `窗口：当前窗口：...`、`路由：command / /task-fail`、`授权：explicit_command direct_execute`，运行态 `window_context` 以 `当前窗口：` 开头。
- 源码桌面 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 退出码 0，输出包含 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 退出码 0，生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；已复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.130.0.exe`，大小 `60440576` 字节。PyInstaller 仅报告既有 `Hidden import "tzdata" not found!` 警告。
- 安装包元数据：`windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.130.0"` 和 `Jarvis Lite 0.130.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.130.0 installation finished`；`JarvisLite.version.txt` 的 `filevers/prodvers` 为 `(0, 130, 0, 0)`；`desktop-exe\JarvisLite.exe` 的 `FileVersion/ProductVersion` 均为 `0.130.0`。
- 打包后 smoke：`E:\oyzj\ai\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 退出码 0，stdout 包含 `desktopPetWindow`，stderr 为空，未留下新的 `JarvisLite` 进程。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接检查通过，检查 `612` 个本地链接；敏感信息形态扫描通过，扫描 `20` 个变更/新增公开文件；`config/llm.local.json`、`config/search.local.json`、`word/inner-brain-evaluation-report.md` 不存在；README BOM 为 `EF-BB-BF`。
