# 2026-06-14 验证记录

> 日期：2026-06-14
> 执行者：Codex

## 0.149.0 偏好应用状态单条偏好过滤第一阶段

- 目标：扩展 `/preference-apply-status [编号或ID] [输出面] [偏好编号或ID]`，支持按确认记录内单条偏好过滤状态解释，并可与 `reply`、`knowledge` 或 `memory` 输出面过滤组合。
- RED：目标命令先失败；失败点为 `describe_preference_application_status()` 不接受 `preference_reference` 参数，Agent 将 `pref-...` 当作未知输出面，项目版本仍为 `0.148.0`。
- GREEN：目标用例复跑通过，`Ran 2 tests in 0.162s`，`OK`。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_preferences tests.test_agent tests.test_llm tests.test_project_metadata -v`，`Ran 462 tests in 10.228s`，`OK`。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，`Ran 794 tests in 13.751s`，`OK`。
- 命令行 smoke：临时项目内执行偏好候选确认、启用、偏好应用确认、按 `pref-...` 过滤最近确认记录、按确认 ID + `knowledge` + `pref-...` 组合过滤、`preference 2` 编号过滤和未知偏好查询；输出 `preference-application-status-preference-filter-smoke OK`。
- 源码桌面 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 退出码 0，stdout 包含 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- Windows 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 成功，生成基础安装包 `E:\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；构建日志包含既有 `Hidden import "tzdata" not found!` 警告和第三方 `pyautogui` 的 `SyntaxWarning: invalid escape sequence '\e'`。
- 版本化安装包：复制生成 `E:\ai\jarvis-lite-dist\JarvisLiteSetup-0.149.0.exe`，大小 `59,723,776` 字节，时间戳 `2026/6/14 2:15:57`。
- 安装包元数据：`E:\ai\jarvis-lite-dist\windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.149.0"` 和 `Jarvis Lite 0.149.0 installed`；`E:\ai\jarvis-lite-dist\JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.149.0 installation finished`；`JarvisLite.version.txt` 的 `filevers/prodvers` 为 `(0, 149, 0, 0)`；`desktop-exe\JarvisLite.exe` 的 `FileVersion/ProductVersion` 均为 `0.149.0`。
- 打包后 smoke：`E:\ai\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 重定向复验退出码 0，stdout 包含 `desktopPetWindow`，stderr 为空，无残留 `JarvisLite` 进程。

## 最终收紧检查

- 文档同步后全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，`Ran 794 tests in 10.366s`，`OK`。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 提示。
- Markdown 本地链接检查：712 项通过，覆盖 433 个 Markdown 文件。
- 严格真实 key 形态扫描：23 个变更/新增文件无命中；首次扫描规则过宽，把 `task-state...` 文件名中的 `sk-` 片段误判，收紧为真实 key 边界和长度后复跑通过。
- 本地敏感配置文件：`config/llm.local.json`、`config/search.local.json` 和 `word/inner-brain-evaluation-report.md` 均不存在。

## 接手后提交前复验

- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，`Ran 794 tests in 14.338s`，`OK`。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 提示。
- Markdown 本地链接检查：496 项通过，覆盖 433 个 Markdown 文件。
- 严格真实 key 形态扫描：22 个变更/新增文件无命中。
- 本地敏感配置文件：`config/llm.local.json`、`config/search.local.json` 和 `word/inner-brain-evaluation-report.md` 均不存在。
