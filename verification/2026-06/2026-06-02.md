# 2026-06-02 验证记录

> 日期：2026-06-02
> 执行者：Codex

## 2026-06-02 0.53.0 InnerBrain 本机评估全量视图文件名候选提示验证

- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_local_command_lists_only_local_cases tests.test_agent.AgentTests.test_inner_brain_eval_local_file_command_lists_only_selected_local_file_cases tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v`，3 项中 2 项按预期 FAIL；本机全量评估缺少 `可聚焦文件：` 与具体文件命令，版本仍为 `0.52.0`；文件过滤视图未回归。
- GREEN：同一目标命令通过，3 项 OK。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v`，341 项 OK。
- 全量：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，562 项 OK。
- 桌面 smoke：源码和打包后 exe 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包：`E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.53.0.exe`，大小 `57,131,008` 字节。
- 安装脚本：`install.cmd` 包含 `DisplayVersion /d "0.53.0"` 和 `Jarvis Lite 0.53.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.53.0 installation finished`。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK (339 checked across 30 files)`；敏感信息差异扫描输出 `Sensitive diff scan OK`；`config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪且当前不存在，`Test-Path` 均为 `False`。

## 2026-06-02 0.52.0 InnerBrain 本机评估全量视图后续处理提示验证

- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_local_command_lists_only_local_cases tests.test_agent.AgentTests.test_inner_brain_eval_local_failed_command_guides_empty_local_evaluation_samples tests.test_agent.AgentTests.test_inner_brain_eval_local_file_command_lists_only_selected_local_file_cases tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v`，4 项中 3 项按预期 FAIL；本机全量评估和文件全量评估响应缺少 `后续处理：`，版本仍为 `0.51.0`；空样本负向断言通过。
- GREEN：同一目标命令通过，4 项 OK。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v`，341 项 OK。
- 全量：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，562 项 OK。
- 桌面 smoke：源码和打包后 exe 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包：`E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.52.0.exe`，大小 `57,131,008` 字节。
- 安装脚本：`install.cmd` 包含 `DisplayVersion /d "0.52.0"` 和 `Jarvis Lite 0.52.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.52.0 installation finished`。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK (334 checked across 30 files)`；敏感信息差异扫描输出 `Sensitive diff scan OK`；`config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪且当前不存在，`Test-Path` 均为 `False`。

## 2026-06-02 0.51.0 InnerBrain 本机 evaluation 已处理样本只读清单验证

- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_resolved_evaluation_lists_only_passed_local_cases tests.test_inner_brain.InnerBrainTests.test_inner_brain_resolved_evaluation_reports_empty_passed_list tests.test_agent.AgentTests.test_inner_brain_eval_local_resolved_command_lists_only_passed_local_cases tests.test_agent.AgentTests.test_inner_brain_eval_local_resolved_command_can_filter_local_file tests.test_agent.AgentTests.test_inner_brain_eval_local_resolved_command_reports_empty_passed_list tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v`，6 项按预期失败或错误；`describe_inner_brain_resolved_evaluation` 尚未导出，`/inner-brain-eval-local-resolved` 返回未知命令，版本仍为 `0.50.0`。
- GREEN：同一目标命令通过，6 项 OK。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v`，341 项 OK。
- 全量：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，562 项 OK。
- 桌面 smoke：源码和打包后 exe 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包：`E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.51.0.exe`，大小 `57,131,008` 字节。
- 安装脚本：`install.cmd` 包含 `DisplayVersion /d "0.51.0"` 和 `Jarvis Lite 0.51.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.51.0 installation finished`。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK (332 checked across 309 files)`；敏感信息差异扫描输出 `Sensitive diff scan OK`；`config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪且当前不存在，`Test-Path` 均为 `False`。

## 2026-06-02 0.50.0 InnerBrain 本机失败视图文件聚焦提示验证

- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_local_failed_command_guides_empty_local_evaluation_samples tests.test_agent.AgentTests.test_inner_brain_eval_local_failed_command_lists_only_failed_local_cases tests.test_agent.AgentTests.test_inner_brain_eval_local_file_failed_command_lists_only_selected_file_failures tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v`，4 项中 3 项按预期 FAIL；本机失败视图缺少文件聚焦/查看全部失败样本提示，版本仍为 `0.49.0`；空样本负向断言通过。
- GREEN：同一目标命令通过，4 项 OK。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v`，336 项 OK。
- 全量：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，557 项 OK。
- 桌面 smoke：源码和打包后 exe 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包：`E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.50.0.exe`，大小 `57,126,912` 字节。
- 安装脚本：`install.cmd` 包含 `DisplayVersion /d "0.50.0"` 和 `Jarvis Lite 0.50.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.50.0 installation finished`。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK (330 checked across 141 files)`；敏感信息差异扫描输出 `Sensitive diff scan OK`；`config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪且当前不存在，`Test-Path` 均为 `False`。

## 2026-06-02 0.49.0 InnerBrain 本机失败视图导出报告提示验证

- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_local_failed_command_guides_empty_local_evaluation_samples tests.test_agent.AgentTests.test_inner_brain_eval_local_failed_command_lists_only_failed_local_cases tests.test_agent.AgentTests.test_inner_brain_eval_local_file_failed_command_lists_only_selected_file_failures tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v`，4 项中 3 项按预期 FAIL；本机失败视图缺少 `后续处理：`，版本仍为 `0.48.0`；空样本负向断言通过。
- GREEN：同一目标命令通过，4 项 OK。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v`，336 项 OK。
- 全量：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，557 项 OK。
- 桌面 smoke：源码和打包后 exe 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包：`E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.49.0.exe`，大小 `57,131,008` 字节。
- 安装脚本：`install.cmd` 包含 `DisplayVersion /d "0.49.0"` 和 `Jarvis Lite 0.49.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.49.0 installation finished`。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK (327 checked across 140 files)`；敏感信息差异扫描输出 `Sensitive diff scan OK`；`config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪且当前不存在，`Test-Path` 均为 `False`。

## 2026-06-02 0.48.0 InnerBrain 本机失败报告导出后续处理提示验证

- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_can_filter_local_file tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v`，3 项按预期 FAIL；报告导出响应缺少 `后续处理：`，版本仍为 `0.47.0`。
- GREEN：同一目标命令通过，3 项 OK。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v`，336 项 OK。
- 全量：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，557 项 OK。
- 桌面 smoke：源码和打包后 exe 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包：`E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.48.0.exe`，大小 `57,131,008` 字节。
- 安装脚本：`install.cmd` 包含 `DisplayVersion /d "0.48.0"` 和 `Jarvis Lite 0.48.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.48.0 installation finished`。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK (324 checked across 138 files)`；敏感信息差异扫描输出 `Sensitive diff scan OK`；`config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪且当前不存在，`Test-Path` 均为 `False`。

## 2026-06-02 0.47.0 InnerBrain 本机评估样本保存后续验证提示验证

- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_add_command_saves_local_evaluation_case_without_training tests.test_agent.AgentTests.test_inner_brain_eval_add_candidate_saves_selected_candidate_command_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v`，3 项按预期 FAIL；保存反馈缺少 `后续验证：`，版本仍为 `0.46.0`。
- GREEN：同一目标命令通过，3 项 OK。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v`，336 项 OK。
- 全量：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，557 项 OK。
- 桌面 smoke：源码和打包后 exe 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包：`E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.47.0.exe`，大小 `57,131,008` 字节。
- 安装脚本：`install.cmd` 包含 `DisplayVersion /d "0.47.0"` 和 `Jarvis Lite 0.47.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.47.0 installation finished`。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK (321 checked across 137 files)`；敏感信息差异扫描输出 `Sensitive diff scan OK`；`config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪且当前不存在，`Test-Path` 均为 `False`。

## 2026-06-02 0.46.0 InnerBrain 本机评估空样本引导验证

- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_local_evaluation_empty_state_suggests_evaluation_sample_commands tests.test_agent.AgentTests.test_inner_brain_eval_local_failed_command_guides_empty_local_evaluation_samples tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v`，3 项按预期 FAIL；空本机评估描述缺少样本添加命令引导，版本仍为 `0.45.0`。
- GREEN：同一目标命令通过，3 项 OK。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v`，336 项 OK。
- 全量：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，557 项 OK。
- 实际空本机评估命令：`.\.venv\Scripts\python.exe src\app.py --once "/inner-brain-eval-local-failed"` 输出 `本机评估样本：`、`- 无`、`添加本机评估样本：`、四个本机 evaluation 样本添加入口和“不自动训练”说明。
- 桌面 smoke：源码和打包后 exe 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包：`E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.46.0.exe`，大小 `57,131,008` 字节。
- 安装脚本：`install.cmd` 包含 `DisplayVersion /d "0.46.0"` 和 `Jarvis Lite 0.46.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.46.0 installation finished`。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK (318 checked across 136 files)`；敏感信息差异扫描输出 `Sensitive diff scan OK`；`config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪且当前不存在，`Test-Path` 均为 `False`。

## 2026-06-02 0.45.0 InnerBrain 本机失败评估文件意图混淆修复建议分组验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_groups_fix_suggestions_by_file_intent_confusion tests.test_inner_brain.InnerBrainTests.test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期，4 项 FAIL。

- failures-only 描述尚无 `失败文件意图混淆修复建议：`。
- 导出的 Markdown 报告尚无按来源文件和混淆方向分组的显式修复建议。
- Agent 命令导出的本机失败报告尚无文件级分组修复建议。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.44.0`，期望 `0.45.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_groups_fix_suggestions_by_file_intent_confusion tests.test_inner_brain.InnerBrainTests.test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_can_filter_local_file tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，5 项 OK。

- `test_inner_brain_failed_evaluation_groups_fix_suggestions_by_file_intent_confusion` 验证同一来源文件和混淆方向下的显式修复建议会集中展示，排除无来源文件失败和意图一致失败，并验证指定单个文件时不展示文件级修复建议分组。
- `test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training` 验证 Markdown 报告包含 `失败文件意图混淆修复建议：` 且不写 training runtime 样本。
- `test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training` 验证 Agent 本机报告命令复用文件级分组修复建议。
- `test_inner_brain_eval_local_report_command_can_filter_local_file` 验证指定单个本机评估文件导出时省略 `失败文件意图混淆修复建议：`，避免重复列出同一个文件名。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.45.0`。

### 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v
```

结果：通过，334 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，555 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.45.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.45\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.45\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.45.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅报告已有的可选 `tzdata` hidden import 警告。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.45.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.45.0.exe` 大小均为 `57,131,008` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`，命令退出码为 0。
- `install.cmd` 写入 `DisplayVersion` 为 `0.45.0`，并包含 `Jarvis Lite 0.45.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.45.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK (315 checked across 136 files)`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-02 0.44.0 InnerBrain 本机评估失败意图混淆修复建议分组验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_groups_fix_suggestions_by_intent_confusion tests.test_inner_brain.InnerBrainTests.test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期，4 项 FAIL。

- failures-only 描述尚无 `失败意图混淆修复建议：`。
- 导出的 Markdown 报告尚无按混淆方向分组的显式修复建议。
- Agent 命令导出的本机失败报告尚无分组修复建议。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.43.0`，期望 `0.44.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_groups_fix_suggestions_by_intent_confusion tests.test_inner_brain.InnerBrainTests.test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，4 项 OK。

- `test_inner_brain_failed_evaluation_groups_fix_suggestions_by_intent_confusion` 验证 `knowledge.summary -> knowledge.status` 的两个失败样本会集中到同一分组，并排除意图一致的策略失败。
- `test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training` 验证 Markdown 报告包含 `失败意图混淆修复建议：` 且不写 training runtime 样本。
- `test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training` 验证 Agent 本机报告命令复用分组修复建议。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.44.0`。

### 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v
```

结果：通过，333 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，554 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.44.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.44\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.44\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.44.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅报告已有的可选 `tzdata` hidden import 警告。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.44.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.44.0.exe` 大小均为 `57,131,008` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`，命令退出码为 0。
- `install.cmd` 写入 `DisplayVersion` 为 `0.44.0`，并包含 `Jarvis Lite 0.44.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.44.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK (312 checked across 135 files)`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-02 0.43.0 InnerBrain 本机评估失败文件意图混淆汇总验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_summarizes_failed_file_intent_confusions tests.test_inner_brain.InnerBrainTests.test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期，1 项 ERROR、3 项 FAIL。

- `InnerBrainEvaluationReport` 尚无 `failed_source_file_intent_confusion_counts`。
- 导出的 Markdown 报告尚无 `失败文件意图混淆汇总：`。
- Agent 命令导出的报告尚无失败文件意图混淆汇总。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.42.0`，期望 `0.43.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_summarizes_failed_file_intent_confusions tests.test_inner_brain.InnerBrainTests.test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，4 项 OK。

- `test_inner_brain_failed_evaluation_summarizes_failed_file_intent_confusions` 验证失败样本可按 `source_file + expected_intent -> actual_intent` 计数，并排除无来源文件或意图一致的策略失败。
- `test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training` 验证 Markdown 报告包含失败文件意图混淆汇总且不写 training runtime 样本。
- `test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training` 验证 Agent 报告命令复用失败文件意图混淆汇总。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.43.0`。

### 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v
```

结果：通过，332 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，553 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.43.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.43\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.43\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.43.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅报告已有的可选 `tzdata` hidden import 警告。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.43.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.43.0.exe` 大小均为 `57,131,008` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`，命令退出码为 0。
- `install.cmd` 写入 `DisplayVersion` 为 `0.43.0`，并包含 `Jarvis Lite 0.43.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.43.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK (309 checked across 133 files)`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-02 0.42.0 InnerBrain 本机评估失败意图混淆汇总验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_summarizes_failed_intent_confusions tests.test_inner_brain.InnerBrainTests.test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期，1 项 ERROR、3 项 FAIL。

- `InnerBrainEvaluationReport` 尚无 `failed_intent_confusion_counts`。
- 导出的 Markdown 报告尚无 `失败意图混淆汇总：`。
- Agent 命令导出的报告尚无失败意图混淆汇总。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.41.0`，期望 `0.42.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_summarizes_failed_intent_confusions tests.test_inner_brain.InnerBrainTests.test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，4 项 OK。

- `test_inner_brain_failed_evaluation_summarizes_failed_intent_confusions` 验证失败样本可按 `expected_intent -> actual_intent` 计数，并排除意图一致的策略失败。
- `test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training` 验证 Markdown 报告包含失败意图混淆汇总且不写 training runtime 样本。
- `test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training` 验证 Agent 报告命令复用失败意图混淆汇总。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.42.0`。

### 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v
```

结果：通过，331 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，552 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.42.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.42\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.42\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.42.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅报告已有的可选 `tzdata` hidden import 警告。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.42.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.42.0.exe` 大小均为 `57,126,912` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`，命令退出码为 0。
- `install.cmd` 写入 `DisplayVersion` 为 `0.42.0`，并包含 `Jarvis Lite 0.42.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.42.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK (306 checked across 132 files)`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-02 0.41.0 InnerBrain 本机评估失败期望意图汇总验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_summarizes_failed_expected_intents tests.test_inner_brain.InnerBrainTests.test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期，1 项 ERROR、3 项 FAIL。

- `InnerBrainEvaluationReport` 尚无 `failed_expected_intent_counts`。
- 导出的 Markdown 报告尚无 `失败期望意图汇总：`。
- Agent 命令导出的报告尚无失败期望意图汇总。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.40.0`，期望 `0.41.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_summarizes_failed_expected_intents tests.test_inner_brain.InnerBrainTests.test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，4 项 OK。

- `test_inner_brain_failed_evaluation_summarizes_failed_expected_intents` 验证失败样本可按 `expected_intent` 计数，并输出典型样本。
- `test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training` 验证 Markdown 报告包含失败期望意图汇总且不写 training runtime 样本。
- `test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training` 验证 Agent 报告命令复用失败期望意图汇总。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.41.0`。

### 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v
```

结果：通过，330 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，551 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.41.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.41\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.41\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.41.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅报告已有的可选 `tzdata` hidden import 警告。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.41.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.41.0.exe` 大小均为 `57,126,912` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.41.0`，并包含 `Jarvis Lite 0.41.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.41.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK (303 checked across 132 files)`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-02 0.40.0 InnerBrain 本机评估失败类型汇总验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_summarizes_failure_reason_categories tests.test_inner_brain.InnerBrainTests.test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期，1 项 ERROR、3 项 FAIL。

- `InnerBrainEvaluationReport` 尚无 `failed_reason_category_counts`。
- 导出的 Markdown 报告尚无 `失败类型汇总：`。
- Agent 命令导出的报告尚无失败类型汇总。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.39.0`，期望 `0.40.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_summarizes_failure_reason_categories tests.test_inner_brain.InnerBrainTests.test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，4 项 OK。

- `test_inner_brain_failed_evaluation_summarizes_failure_reason_categories` 验证意图、命令和策略失败类型可分别计数，并输出典型样本。
- `test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training` 验证 Markdown 报告包含失败类型汇总且不写 training runtime 样本。
- `test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training` 验证 Agent 报告命令复用失败类型汇总。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.40.0`。

### 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v
```

结果：通过，329 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，550 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.40.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.40\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.40\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.40.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅报告已有的可选 `tzdata` hidden import 警告。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.40.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.40.0.exe` 大小均为 `57,126,912` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.40.0`，并包含 `Jarvis Lite 0.40.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.40.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK (300 checked across 131 files)`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-02 0.39.0 InnerBrain 本机评估失败原因汇总验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_summarizes_failure_reasons tests.test_inner_brain.InnerBrainTests.test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期，1 项 ERROR、3 项 FAIL。

- `InnerBrainEvaluationReport` 尚无 `failed_reason_counts`。
- 导出的 Markdown 报告尚无 `失败原因汇总：`。
- Agent 命令导出的报告尚无失败原因汇总。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.38.0`，期望 `0.39.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_summarizes_failure_reasons tests.test_inner_brain.InnerBrainTests.test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，4 项 OK。

- `test_inner_brain_failed_evaluation_summarizes_failure_reasons` 验证相同失败原因聚合为 2 条，并输出典型样本。
- `test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training` 验证 Markdown 报告包含失败原因汇总且不写 training runtime 样本。
- `test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training` 验证 Agent 报告命令复用失败原因汇总。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.39.0`。

### 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v
```

结果：复跑通过，328 项 OK。首次运行失败 1 项，根因是版本提升后更新检测测试 fixture 已改为 `0.39.1`，但断言仍为 `0.38.1`；同步断言后复跑通过。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，549 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.39.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.39\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.39\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.39.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅报告已有的可选 `tzdata` hidden import 警告。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.39.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.39.0.exe` 大小均为 `57,126,912` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.39.0`，并包含 `Jarvis Lite 0.39.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.39.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK (297 checked across 130 files)`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-02 0.38.0 InnerBrain 本机评估失败报告导出验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_can_filter_local_file tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期，4 项 FAIL。

- `export_inner_brain_evaluation_report` 尚不存在。
- `/inner-brain-eval-local-report` 返回未知命令。
- 指定文件导出同样返回未知命令。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.37.0`，期望 `0.38.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_can_filter_local_file tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，4 项 OK。

- `test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training` 验证报告写入 `word/inner-brain-evaluation-report.md`，包含失败文件、失败样例和修复建议。
- `test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training` 验证 `/inner-brain-eval-local-report` 可导出本机失败报告且不写 training runtime 样本。
- `test_inner_brain_eval_local_report_command_can_filter_local_file` 验证 `/inner-brain-eval-local-report failed-log` 只导出指定本机 JSONL 文件失败项。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.38.0`。

### 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v
```

结果：通过，327 项 OK。首次运行发现版本提升后更新检测测试 fixture 仍为 `0.37.1`，按根因将可用新版本 fixture 同步为 `0.38.1` 后复跑通过。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，548 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.38.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.38\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.38\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.38.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅报告已有的可选 `tzdata` hidden import 警告。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.38.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.38.0.exe` 大小均为 `57,126,912` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.38.0`，并包含 `Jarvis Lite 0.38.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.38.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- 目标测试复跑通过，4 项 OK。
- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK (294 checked across 30 files)`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-02 0.37.0 InnerBrain 本机评估失败文件分组验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_groups_local_failures_by_file tests.test_agent.AgentTests.test_inner_brain_eval_local_failed_command_groups_failures_by_local_file tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期，3 项 FAIL。

- 失败视图尚未显示 `失败文件：` 分组。
- 当前失败视图仍列出全部来源文件计数，包含通过文件 `real-log.jsonl`。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.36.0`，期望 `0.37.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_groups_local_failures_by_file tests.test_agent.AgentTests.test_inner_brain_eval_local_failed_command_groups_failures_by_local_file tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，3 项 OK。

- `test_inner_brain_failed_evaluation_groups_local_failures_by_file` 验证失败视图显示 `失败文件：`，只列出失败来源文件。
- `test_inner_brain_eval_local_failed_command_groups_failures_by_local_file` 验证 `/inner-brain-eval-local-failed` 复用失败文件分组，不写 training runtime 样本。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.37.0`。

### 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent tests.test_llm tests.test_desktop_widgets tests.test_desktop_bridge tests.test_desktop_app tests.test_conversation -v
```

结果：通过，418 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，545 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.37.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.37\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.37\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.37.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅报告已有的可选 `tzdata` hidden import 警告。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.37.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.37.0.exe` 大小均为 `57,126,912` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.37.0`，并包含 `Jarvis Lite 0.37.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.37.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- 目标测试复跑通过，3 项 OK。
- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK (291 checked across 30 files)`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-02 0.36.0 InnerBrain 本机评估 JSONL 文件过滤验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_evaluation_can_filter_local_evaluation_file tests.test_agent.AgentTests.test_inner_brain_eval_local_file_command_lists_only_selected_local_file_cases tests.test_agent.AgentTests.test_inner_brain_eval_local_file_failed_command_lists_only_selected_file_failures tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期，1 项 ERROR、3 项 FAIL。

- `evaluate_inner_brain()` 尚不接受 `source_file_filter`。
- `/inner-brain-eval-local-file` 与 `/inner-brain-eval-local-file-failed` 尚未实现，返回未知命令。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.35.0`，期望 `0.36.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_evaluation_can_filter_local_evaluation_file tests.test_agent.AgentTests.test_inner_brain_eval_local_file_command_lists_only_selected_local_file_cases tests.test_agent.AgentTests.test_inner_brain_eval_local_file_failed_command_lists_only_selected_file_failures tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，4 项 OK。

- `test_inner_brain_evaluation_can_filter_local_evaluation_file` 验证本机 JSONL 样本加载时保留 `source_file`，并可按 `real-log.jsonl` 过滤。
- `test_inner_brain_eval_local_file_command_lists_only_selected_local_file_cases` 验证 `/inner-brain-eval-local-file real-log.jsonl` 只显示指定文件样本，不写 training runtime 样本。
- `test_inner_brain_eval_local_file_failed_command_lists_only_selected_file_failures` 验证 `/inner-brain-eval-local-file-failed failed-log.jsonl` 只显示指定文件失败样本和显式修复建议。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.36.0`。

### 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent tests.test_llm tests.test_desktop_widgets tests.test_desktop_bridge tests.test_desktop_app tests.test_conversation -v
```

结果：通过，416 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，543 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.36.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.36\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.36\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.36.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅报告已有的可选 `tzdata` hidden import 警告。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.36.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.36.0.exe` 大小均为 `57,122,816` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.36.0`，并包含 `Jarvis Lite 0.36.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.36.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK (288 checked across 126 files)`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-02 0.35.0 InnerBrain 候选编号写入本机评估样本验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_add_candidate_saves_selected_candidate_command_without_training tests.test_agent.AgentTests.test_inner_brain_eval_label_candidate_saves_selected_candidate_label_without_training tests.test_agent.AgentTests.test_inner_brain_eval_add_candidate_reports_missing_candidate_without_writing_case tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期，4 项失败。

- `/inner-brain-eval-add-candidate` 与 `/inner-brain-eval-label-candidate` 尚未实现，未写出 `data/inner-brain/evaluation/runtime.jsonl`。
- 缺失候选编号路径仍返回未知命令。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.34.0`，期望 `0.35.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_add_candidate_saves_selected_candidate_command_without_training tests.test_agent.AgentTests.test_inner_brain_eval_label_candidate_saves_selected_candidate_label_without_training tests.test_agent.AgentTests.test_inner_brain_eval_add_candidate_reports_missing_candidate_without_writing_case tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，4 项 OK。

- `test_inner_brain_eval_add_candidate_saves_selected_candidate_command_without_training` 验证第 1 条候选可按编号保存为命令评估样本，不写 training runtime 样本，且保存动作不污染最近路由。
- `test_inner_brain_eval_label_candidate_saves_selected_candidate_label_without_training` 验证第 1 条候选可按编号保存为 intent 评估样本，候选仍保留。
- `test_inner_brain_eval_add_candidate_reports_missing_candidate_without_writing_case` 验证候选编号不存在时不写入 evaluation 或 training 文件。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.35.0`。

### 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent tests.test_llm tests.test_desktop_widgets tests.test_desktop_bridge tests.test_desktop_app tests.test_conversation -v
```

结果：通过，413 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，540 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.35.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.35\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.35\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.35.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅报告已有的可选 `tzdata` hidden import 警告。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.35.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.35.0.exe` 大小均为 `57,122,816` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.35.0`，并包含 `Jarvis Lite 0.35.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.35.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK (278 files)`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。
