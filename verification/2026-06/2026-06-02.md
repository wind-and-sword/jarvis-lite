# 2026-06-02 验证记录

> 日期：2026-06-02
> 执行者：Codex

## 2026-06-02 0.41.0 InnerBrain 本机评估失败期望意图汇总验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_summarizes_failed_expected_intents tests.test_inner_brain.InnerBrainTests.test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期，1 项 ERROR、3 项 FAIL。

- `InnerBrainEvaluationReport` 尚无 `failed_expected_intent_counts`。
- 导出的 Markdown 报告尚无 `失败期望意图汇总：`。
- Agent 命令导出的报告尚无失败期望意图汇总。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.40.0`，期望 `0.41.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_summarizes_failed_expected_intents tests.test_inner_brain.InnerBrainTests.test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，4 项 OK。

- `test_inner_brain_failed_evaluation_summarizes_failed_expected_intents` 验证失败样本可按 `expected_intent` 计数，并输出典型样本。
- `test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training` 验证 Markdown 报告包含失败期望意图汇总且不写 training runtime 样本。
- `test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training` 验证 Agent 报告命令复用失败期望意图汇总。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.41.0`。

### 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v
```

结果：通过，330 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，551 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.41.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.41\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.41\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.41.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅报告已有的可选 `tzdata` hidden import 警告。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.41.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.41.0.exe` 大小均为 `57,126,912` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.41.0`，并包含 `Jarvis Lite 0.41.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.41.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK (303 checked across 132 files)`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-02 0.40.0 InnerBrain 本机评估失败类型汇总验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_summarizes_failure_reason_categories tests.test_inner_brain.InnerBrainTests.test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期，1 项 ERROR、3 项 FAIL。

- `InnerBrainEvaluationReport` 尚无 `failed_reason_category_counts`。
- 导出的 Markdown 报告尚无 `失败类型汇总：`。
- Agent 命令导出的报告尚无失败类型汇总。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.39.0`，期望 `0.40.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_summarizes_failure_reason_categories tests.test_inner_brain.InnerBrainTests.test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，4 项 OK。

- `test_inner_brain_failed_evaluation_summarizes_failure_reason_categories` 验证意图、命令和策略失败类型可分别计数，并输出典型样本。
- `test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training` 验证 Markdown 报告包含失败类型汇总且不写 training runtime 样本。
- `test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training` 验证 Agent 报告命令复用失败类型汇总。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.40.0`。

### 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v
```

结果：通过，329 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，550 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.40.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.40\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.40\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.40.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅报告已有的可选 `tzdata` hidden import 警告。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.40.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.40.0.exe` 大小均为 `57,126,912` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.40.0`，并包含 `Jarvis Lite 0.40.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.40.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK (300 checked across 131 files)`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-02 0.39.0 InnerBrain 本机评估失败原因汇总验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_summarizes_failure_reasons tests.test_inner_brain.InnerBrainTests.test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期，1 项 ERROR、3 项 FAIL。

- `InnerBrainEvaluationReport` 尚无 `failed_reason_counts`。
- 导出的 Markdown 报告尚无 `失败原因汇总：`。
- Agent 命令导出的报告尚无失败原因汇总。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.38.0`，期望 `0.39.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_summarizes_failure_reasons tests.test_inner_brain.InnerBrainTests.test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，4 项 OK。

- `test_inner_brain_failed_evaluation_summarizes_failure_reasons` 验证相同失败原因聚合为 2 条，并输出典型样本。
- `test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training` 验证 Markdown 报告包含失败原因汇总且不写 training runtime 样本。
- `test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training` 验证 Agent 报告命令复用失败原因汇总。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.39.0`。

### 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v
```

结果：复跑通过，328 项 OK。首次运行失败 1 项，根因是版本提升后更新检测测试 fixture 已改为 `0.39.1`，但断言仍为 `0.38.1`；同步断言后复跑通过。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，549 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.39.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.39\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.39\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.39.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅报告已有的可选 `tzdata` hidden import 警告。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.39.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.39.0.exe` 大小均为 `57,126,912` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.39.0`，并包含 `Jarvis Lite 0.39.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.39.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK (297 checked across 130 files)`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-02 0.38.0 InnerBrain 本机评估失败报告导出验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_can_filter_local_file tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期，4 项 FAIL。

- `export_inner_brain_evaluation_report` 尚不存在。
- `/inner-brain-eval-local-report` 返回未知命令。
- 指定文件导出同样返回未知命令。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.37.0`，期望 `0.38.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_can_filter_local_file tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，4 项 OK。

- `test_export_inner_brain_evaluation_report_writes_failed_markdown_without_training` 验证报告写入 `word/inner-brain-evaluation-report.md`，包含失败文件、失败样例和修复建议。
- `test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training` 验证 `/inner-brain-eval-local-report` 可导出本机失败报告且不写 training runtime 样本。
- `test_inner_brain_eval_local_report_command_can_filter_local_file` 验证 `/inner-brain-eval-local-report failed-log` 只导出指定本机 JSONL 文件失败项。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.38.0`。

### 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent -v
```

结果：通过，327 项 OK。首次运行发现版本提升后更新检测测试 fixture 仍为 `0.37.1`，按根因将可用新版本 fixture 同步为 `0.38.1` 后复跑通过。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，548 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.38.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.38\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.38\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.38.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅报告已有的可选 `tzdata` hidden import 警告。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.38.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.38.0.exe` 大小均为 `57,126,912` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.38.0`，并包含 `Jarvis Lite 0.38.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.38.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- 目标测试复跑通过，4 项 OK。
- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK (294 checked across 30 files)`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-02 0.37.0 InnerBrain 本机评估失败文件分组验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_groups_local_failures_by_file tests.test_agent.AgentTests.test_inner_brain_eval_local_failed_command_groups_failures_by_local_file tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期，3 项 FAIL。

- 失败视图尚未显示 `失败文件：` 分组。
- 当前失败视图仍列出全部来源文件计数，包含通过文件 `real-log.jsonl`。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.36.0`，期望 `0.37.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_groups_local_failures_by_file tests.test_agent.AgentTests.test_inner_brain_eval_local_failed_command_groups_failures_by_local_file tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，3 项 OK。

- `test_inner_brain_failed_evaluation_groups_local_failures_by_file` 验证失败视图显示 `失败文件：`，只列出失败来源文件。
- `test_inner_brain_eval_local_failed_command_groups_failures_by_local_file` 验证 `/inner-brain-eval-local-failed` 复用失败文件分组，不写 training runtime 样本。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.37.0`。

### 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent tests.test_llm tests.test_desktop_widgets tests.test_desktop_bridge tests.test_desktop_app tests.test_conversation -v
```

结果：通过，418 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，545 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.37.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.37\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.37\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.37.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅报告已有的可选 `tzdata` hidden import 警告。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.37.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.37.0.exe` 大小均为 `57,126,912` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.37.0`，并包含 `Jarvis Lite 0.37.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.37.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- 目标测试复跑通过，3 项 OK。
- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK (291 checked across 30 files)`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-02 0.36.0 InnerBrain 本机评估 JSONL 文件过滤验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_evaluation_can_filter_local_evaluation_file tests.test_agent.AgentTests.test_inner_brain_eval_local_file_command_lists_only_selected_local_file_cases tests.test_agent.AgentTests.test_inner_brain_eval_local_file_failed_command_lists_only_selected_file_failures tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期，1 项 ERROR、3 项 FAIL。

- `evaluate_inner_brain()` 尚不接受 `source_file_filter`。
- `/inner-brain-eval-local-file` 与 `/inner-brain-eval-local-file-failed` 尚未实现，返回未知命令。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.35.0`，期望 `0.36.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_evaluation_can_filter_local_evaluation_file tests.test_agent.AgentTests.test_inner_brain_eval_local_file_command_lists_only_selected_local_file_cases tests.test_agent.AgentTests.test_inner_brain_eval_local_file_failed_command_lists_only_selected_file_failures tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，4 项 OK。

- `test_inner_brain_evaluation_can_filter_local_evaluation_file` 验证本机 JSONL 样本加载时保留 `source_file`，并可按 `real-log.jsonl` 过滤。
- `test_inner_brain_eval_local_file_command_lists_only_selected_local_file_cases` 验证 `/inner-brain-eval-local-file real-log.jsonl` 只显示指定文件样本，不写 training runtime 样本。
- `test_inner_brain_eval_local_file_failed_command_lists_only_selected_file_failures` 验证 `/inner-brain-eval-local-file-failed failed-log.jsonl` 只显示指定文件失败样本和显式修复建议。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.36.0`。

### 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent tests.test_llm tests.test_desktop_widgets tests.test_desktop_bridge tests.test_desktop_app tests.test_conversation -v
```

结果：通过，416 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，543 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.36.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.36\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.36\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.36.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅报告已有的可选 `tzdata` hidden import 警告。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.36.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.36.0.exe` 大小均为 `57,122,816` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.36.0`，并包含 `Jarvis Lite 0.36.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.36.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK (288 checked across 126 files)`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-02 0.35.0 InnerBrain 候选编号写入本机评估样本验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_add_candidate_saves_selected_candidate_command_without_training tests.test_agent.AgentTests.test_inner_brain_eval_label_candidate_saves_selected_candidate_label_without_training tests.test_agent.AgentTests.test_inner_brain_eval_add_candidate_reports_missing_candidate_without_writing_case tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期，4 项失败。

- `/inner-brain-eval-add-candidate` 与 `/inner-brain-eval-label-candidate` 尚未实现，未写出 `data/inner-brain/evaluation/runtime.jsonl`。
- 缺失候选编号路径仍返回未知命令。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.34.0`，期望 `0.35.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_add_candidate_saves_selected_candidate_command_without_training tests.test_agent.AgentTests.test_inner_brain_eval_label_candidate_saves_selected_candidate_label_without_training tests.test_agent.AgentTests.test_inner_brain_eval_add_candidate_reports_missing_candidate_without_writing_case tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，4 项 OK。

- `test_inner_brain_eval_add_candidate_saves_selected_candidate_command_without_training` 验证第 1 条候选可按编号保存为命令评估样本，不写 training runtime 样本，且保存动作不污染最近路由。
- `test_inner_brain_eval_label_candidate_saves_selected_candidate_label_without_training` 验证第 1 条候选可按编号保存为 intent 评估样本，候选仍保留。
- `test_inner_brain_eval_add_candidate_reports_missing_candidate_without_writing_case` 验证候选编号不存在时不写入 evaluation 或 training 文件。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.35.0`。

### 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent tests.test_llm tests.test_desktop_widgets tests.test_desktop_bridge tests.test_desktop_app tests.test_conversation -v
```

结果：通过，413 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，540 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.35.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.35\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.35\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.35.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅报告已有的可选 `tzdata` hidden import 警告。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.35.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.35.0.exe` 大小均为 `57,122,816` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.35.0`，并包含 `Jarvis Lite 0.35.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.35.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK (278 files)`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。
