# 2026-06-03 验证记录

> 日期：2026-06-03
> 执行者：Codex

## 0.69.0 InnerBrain 本机 evaluation 保存反馈按文件报告入口

- 目标：`/inner-brain-eval-add`、`/inner-brain-eval-label`、`/inner-brain-eval-add-candidate` 和 `/inner-brain-eval-label-candidate` 保存本机 evaluation 样本后，直接提示 `/inner-brain-eval-local-report runtime.jsonl`。
- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_add_command_saves_local_evaluation_case_without_training tests.test_agent.AgentTests.test_inner_brain_eval_label_command_saves_local_evaluation_case_without_training tests.test_agent.AgentTests.test_inner_brain_eval_add_candidate_saves_selected_candidate_command_without_training tests.test_agent.AgentTests.test_inner_brain_eval_label_candidate_saves_selected_candidate_label_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v` 先失败，4 个保存反馈缺少按文件报告入口，版本仍为 `0.68.0`。
- GREEN：同一目标命令 5 项通过。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_agent tests.test_project_metadata -v`，304 项通过。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，566 项通过。
- 源码 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 成功生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅出现既有可选 `Hidden import "tzdata" not found!` 警告。
- 版本化安装包：复制生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.69.0.exe`，大小 `57,131,008` 字节。
- 安装脚本与 SED：`install.cmd` 包含 `DisplayVersion /d "0.69.0"` 和 `Jarvis Lite 0.69.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.69.0 installation finished`，目标文件仍为 `JarvisLiteSetup.exe`。
- 打包后 smoke：`..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`；未发现残留 `JarvisLite*` 进程。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK: 394 checked across 334 files`；敏感信息差异扫描无命中；`config/llm.local.json` 与 `config/search.local.json` 当前不存在，`Test-Path` 均为 `False`。

## 0.70.0 InnerBrain 本机失败报告导出反馈当前文件已处理入口

- 目标：`/inner-brain-eval-local-report 文件名` 导出指定文件失败报告后，直接提示 `/inner-brain-eval-local-resolved 当前文件名`，便于对照同文件已处理样本。
- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_can_filter_local_file tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v` 先失败，文件级报告导出反馈缺少 `/inner-brain-eval-local-resolved failed-log.jsonl`，版本仍为 `0.69.0`。
- GREEN：同一目标命令 2 项通过。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_agent tests.test_project_metadata -v`，304 项通过。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，566 项通过。
- 源码 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 成功生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅出现既有可选 `Hidden import "tzdata" not found!` 警告。
- 版本化安装包：复制生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.70.0.exe`，大小 `57,131,008` 字节。
- 安装脚本与 SED：`install.cmd` 包含 `DisplayVersion /d "0.70.0"` 和 `Jarvis Lite 0.70.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.70.0 installation finished`，`TargetName=E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 打包后 smoke：`..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`；未发现残留 `JarvisLite*` 进程。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK: 398 checked across 335 files`；敏感信息差异扫描无命中；`config/llm.local.json` 与 `config/search.local.json` 当前不存在，`Test-Path` 均为 `False`。

## 0.71.0 InnerBrain 本机失败报告导出反馈当前文件总览入口

- 目标：`/inner-brain-eval-local-report 文件名` 导出指定文件失败报告后，直接提示 `/inner-brain-eval-local-file 当前文件名`，便于回到同文件通过/失败总览。
- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_can_filter_local_file tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v` 先失败，文件级报告导出反馈缺少 `/inner-brain-eval-local-file failed-log.jsonl`，版本仍为 `0.70.0`。
- GREEN：同一目标命令 2 项通过。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_agent tests.test_project_metadata -v`，304 项通过。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，566 项通过。
- 源码 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 成功生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅出现既有可选 `Hidden import "tzdata" not found!` 警告。
- 版本化安装包：复制生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.71.0.exe`，大小 `57,135,104` 字节。
- 安装脚本与 SED：`install.cmd` 包含 `DisplayVersion /d "0.71.0"` 和 `Jarvis Lite 0.71.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.71.0 installation finished`，`TargetName=E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 打包后 smoke：`..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`；未发现残留 `JarvisLite*` 进程。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK: 401 checked across 336 files`；敏感信息差异扫描无命中；`config/llm.local.json` 与 `config/search.local.json` 当前不存在，`Test-Path` 均为 `False`。

## 0.72.0 InnerBrain 本机文件失败视图当前文件总览入口

- 目标：`/inner-brain-eval-local-file-failed 文件名` 聚焦指定文件失败样本后，直接提示 `/inner-brain-eval-local-file 当前文件名`，便于回到同一 JSONL 文件的通过/失败总览。
- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_local_file_failed_command_lists_only_selected_file_failures tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v` 先失败，指定文件失败视图缺少 `/inner-brain-eval-local-file failed-log.jsonl`，版本仍为 `0.71.0`。
- GREEN：同一目标命令 2 项通过。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_agent tests.test_project_metadata -v`，304 项通过。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，566 项通过。
- 源码 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 成功生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅出现既有可选 `Hidden import "tzdata" not found!` 警告。
- 版本化安装包：复制生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.72.0.exe`，大小 `57,135,104` 字节。
- 安装脚本与 SED：`windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.72.0"` 和 `Jarvis Lite 0.72.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.72.0 installation finished`，`TargetName=E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 打包后 smoke：`..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`；未发现残留 `JarvisLite*` 进程。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK: 404 checked across 337 files`；敏感信息差异扫描无命中；`config/llm.local.json` 与 `config/search.local.json` 当前不存在，`Test-Path` 均为 `False`。

## 0.73.0 InnerBrain 本机已处理指定文件视图当前文件总览入口

- 目标：`/inner-brain-eval-local-resolved 文件名` 查看指定文件已处理样本后，直接提示 `/inner-brain-eval-local-file 当前文件名`，便于回到同一 JSONL 文件的通过/失败总览。
- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_local_resolved_command_can_filter_local_file tests.test_agent.AgentTests.test_inner_brain_eval_local_resolved_command_suggests_report_for_file_with_pending_failures tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v` 先失败，指定文件已处理视图缺少 `/inner-brain-eval-local-file real-log.jsonl`，版本仍为 `0.72.0`。
- GREEN：同一目标命令 3 项通过。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_agent tests.test_project_metadata -v`，304 项通过。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，566 项通过。
- 源码 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 成功生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅出现既有可选 `Hidden import "tzdata" not found!` 警告。
- 版本化安装包：复制生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.73.0.exe`，大小 `57,135,104` 字节。
- 安装脚本与 SED：`windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.73.0"` 和 `Jarvis Lite 0.73.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.73.0 installation finished`，`TargetName=E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 打包后 smoke：`..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`；未发现残留 `JarvisLite*` 进程。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK: 407 checked across 338 files`；敏感信息差异扫描无命中；`config/llm.local.json` 与 `config/search.local.json` 当前不存在，`Test-Path` 均为 `False`。

## 0.74.0 InnerBrain 本机失败视图失败文件分组当前文件总览入口

- 目标：`/inner-brain-eval-local-failed` 的 `失败文件：` 分组为每个失败文件补充 `/inner-brain-eval-local-file 当前文件名`，便于从同文件失败分组回到通过/失败总览。
- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_local_failed_command_groups_failures_by_local_file tests.test_agent.AgentTests.test_inner_brain_eval_local_failed_command_sorts_failure_files_by_failed_count tests.test_inner_brain.InnerBrainTests.test_inner_brain_failed_evaluation_sorts_failure_files_by_failed_count tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v` 先失败，失败文件分组缺少 `总览：/inner-brain-eval-local-file ...`，版本仍为 `0.73.0`。
- GREEN：同一目标命令 4 项通过。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_agent tests.test_inner_brain tests.test_project_metadata -v`，349 项通过。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，566 项通过。
- 源码 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 成功生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅出现既有可选 `Hidden import "tzdata" not found!` 警告。
- 版本化安装包：复制生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.74.0.exe`，大小 `57,135,104` 字节。
- 安装脚本与 SED：`windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.74.0"` 和 `Jarvis Lite 0.74.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.74.0 installation finished`，`TargetName=E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 打包后 smoke：`..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`；未发现残留 `JarvisLite*` 进程。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK: 410 checked across 339 files`；敏感信息差异扫描输出 `Sensitive diff scan OK`；`config/llm.local.json` 与 `config/search.local.json` 当前不存在，`Test-Path` 均为 `False`。
- 备注：SED 目标路径检查首次使用 `Select-String -Pattern` 误把 Windows 反斜杠路径当作正则解析；按系统化调试确认根因为命令写法后，改用 `-SimpleMatch` 复验通过。

## 0.75.0 InnerBrain 本机已处理视图文件候选当前文件总览入口

- 目标：`/inner-brain-eval-local-resolved` 的 `可查看文件：` 候选为每个有已处理样本的文件补充 `/inner-brain-eval-local-file 当前文件名`，便于从已处理候选回到同文件通过/失败总览。
- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_local_resolved_command_lists_only_passed_local_cases tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v` 先失败，已处理文件候选缺少 `总览：/inner-brain-eval-local-file ...`，版本仍为 `0.74.0`。
- GREEN：同一目标命令 2 项通过。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_agent tests.test_project_metadata -v`，304 项通过。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，566 项通过。
- 源码 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 成功生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅出现既有可选 `Hidden import "tzdata" not found!` 警告。
- 版本化安装包：复制生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.75.0.exe`，大小 `57,135,104` 字节。
- 安装脚本与 SED：`windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.75.0"` 和 `Jarvis Lite 0.75.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.75.0 installation finished`，`TargetName=E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 打包后 smoke：`..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`；未发现残留 `JarvisLite*` 进程。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK: 413 checked across 340 files`；敏感信息差异扫描输出 `Sensitive diff scan OK`；`config/llm.local.json` 与 `config/search.local.json` 当前不存在，`Test-Path` 均为 `False`。

## 0.76.0 InnerBrain 本机评估全量视图文件候选总览标签

- 目标：`/inner-brain-eval-local` 的 `可聚焦文件：` 候选把同文件全部样本入口标注为 `总览：/inner-brain-eval-local-file 当前文件名`，便于和待处理失败、报告入口区分。
- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_local_command_lists_only_local_cases tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v` 先失败，全量本机评估文件候选仍使用裸 `/inner-brain-eval-local-file ...`，版本仍为 `0.75.0`。
- GREEN：同一目标命令 2 项通过。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_agent tests.test_project_metadata -v`，304 项通过。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，566 项通过。
- 源码 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 成功生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅出现既有可选 `Hidden import "tzdata" not found!` 警告。
- 版本化安装包：复制生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.76.0.exe`，大小 `57,135,104` 字节。
- 安装脚本与 SED：`windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.76.0"` 和 `Jarvis Lite 0.76.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.76.0 installation finished`，`TargetName=E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 打包后 smoke：`..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`；未发现残留 `JarvisLite*` 进程。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK: 416 checked across 348 files`；敏感信息差异扫描输出 `Sensitive diff scan OK`；`config/llm.local.json` 与 `config/search.local.json` 当前不存在，`Test-Path` 均为 `False`。

## 0.77.0 InnerBrain 本机文件失败视图当前文件总览标签

- 目标：`/inner-brain-eval-local-file-failed 文件名` 的后续处理把同文件全部样本入口标注为 `当前文件总览：/inner-brain-eval-local-file 当前文件名`，便于和全量候选行的总览语义保持一致。
- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_local_file_failed_command_lists_only_selected_file_failures tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v` 先失败，指定文件失败视图仍输出旧的 `查看当前文件全部样本：...`，版本仍为 `0.76.0`。
- GREEN：同一目标命令 2 项通过。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_agent tests.test_project_metadata -v`，304 项通过。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，566 项通过。
- 源码 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅出现既有可选 `Hidden import "tzdata" not found!` 警告。
- 版本化安装包：复制生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.77.0.exe`，大小 `57,135,104` 字节。
- 安装脚本与 SED：`windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.77.0"` 和 `Jarvis Lite 0.77.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.77.0 installation finished`，`TargetName=E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 打包后 smoke：`..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`，未发现残留 `JarvisLite*` 进程。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK: 419 checked across 169 files`；敏感信息差异扫描输出 `Sensitive diff scan OK`；`config/llm.local.json` 与 `config/search.local.json` 当前不存在，`Test-Path` 均为 `False`。
- 备注：安装脚本和 SED 首次按错误仓库内路径检查失败；按系统化调试定位到构建产物真实路径 `..\jarvis-lite-dist\windows-installer-stage\install.cmd` 与 `..\jarvis-lite-dist\JarvisLiteSetup.sed` 后复验通过。Markdown 链接检查使用 Git 跟踪的 Markdown 文件范围，未把 `.codex` 忽略留痕文件计入提交验证范围。

## 0.78.0 InnerBrain 本机已处理指定文件视图当前文件总览标签

- 目标：`/inner-brain-eval-local-resolved 文件名` 的后续处理把同文件全部样本入口标注为 `当前文件总览：/inner-brain-eval-local-file 当前文件名`，便于和指定文件失败视图、全量候选行的总览语义保持一致。
- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_local_resolved_command_can_filter_local_file tests.test_agent.AgentTests.test_inner_brain_eval_local_resolved_command_suggests_report_for_file_with_pending_failures tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v` 先失败，指定文件已处理视图仍输出旧的 `查看当前文件全部样本：...`，版本仍为 `0.77.0`。
- GREEN：同一目标命令 3 项通过。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_agent tests.test_project_metadata -v`，304 项通过。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，566 项通过。
- 源码 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅出现既有可选 `Hidden import "tzdata" not found!` 警告。
- 版本化安装包：复制生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.78.0.exe`，大小 `57,131,008` 字节。
- 安装脚本与 SED：`windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.78.0"` 和 `Jarvis Lite 0.78.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.78.0 installation finished`，`TargetName=E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 打包后 smoke：`..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`，未发现残留 `JarvisLite*` 进程。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK: 422 checked across 170 files`；敏感信息差异扫描输出 `Sensitive diff scan OK`；`config/llm.local.json` 与 `config/search.local.json` 当前不存在，`Test-Path` 均为 `False`。

## 0.79.0 InnerBrain 本机失败报告导出反馈当前文件总览标签

- 目标：`/inner-brain-eval-local-report 文件名` 的导出反馈把同文件全部样本入口标注为 `当前文件总览：/inner-brain-eval-local-file 当前文件名`，便于和指定文件失败视图、指定文件已处理视图的总览语义保持一致。
- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_can_filter_local_file tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v` 先失败，指定文件报告导出反馈仍输出旧的 `查看当前文件全部样本：...`，版本仍为 `0.78.0`。
- GREEN：同一目标命令 2 项通过。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_agent tests.test_project_metadata -v`，304 项通过。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，566 项通过。
- 源码 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅出现既有可选 `Hidden import "tzdata" not found!` 警告。
- 版本化安装包：复制生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.79.0.exe`，大小 `57,135,104` 字节。
- 安装脚本与 SED：`windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.79.0"` 和 `Jarvis Lite 0.79.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.79.0 installation finished`，`TargetName=E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 打包后 smoke：`..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`，等待后未发现残留 `JarvisLite.exe` 进程。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK: 425 checked across 169 files`；敏感信息差异扫描输出 `Sensitive diff scan OK`；`config/llm.local.json` 与 `config/search.local.json` 当前不存在，`Test-Path` 均为 `False`。

## 0.80.0 InnerBrain 本机失败报告导出反馈当前文件待处理失败标签

- 目标：`/inner-brain-eval-local-report 文件名` 的导出反馈把同文件失败入口标注为 `查看当前文件待处理失败样本：/inner-brain-eval-local-file-failed 当前文件名`，便于和指定文件已处理视图的待处理失败语义保持一致。
- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_can_filter_local_file tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v` 先失败，指定文件报告导出反馈仍输出旧的 `复查当前文件失败样本：...`，版本仍为 `0.79.0`。
- GREEN：同一目标命令 2 项通过。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_agent tests.test_project_metadata -v`，304 项通过。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，566 项通过。
- 源码 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅出现既有可选 `Hidden import "tzdata" not found!` 警告。
- 版本化安装包：复制生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.80.0.exe`，大小 `57,131,008` 字节。
- 安装脚本与 SED：`windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.80.0"` 和 `Jarvis Lite 0.80.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.80.0 installation finished`，`TargetName=E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 打包后 smoke：`..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`；显式进程复查输出 `No residual JarvisLite.exe process.`。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK: 428 checked across 172 files`；敏感信息差异扫描输出 `Sensitive diff scan OK`；`config/llm.local.json` 与 `config/search.local.json` 当前不存在，`Test-Path` 均为 `False`。
- 调试备注：打包后 smoke 与进程检查合并命令首次因 `Get-Process` 无结果产生非零退出；按系统化调试确认根因为检查命令写法后，改用显式分支复验无残留进程。

## 0.81.0 InnerBrain 本机当前文件反馈全部待处理失败标签

- 目标：`/inner-brain-eval-local-file-failed 文件名` 和 `/inner-brain-eval-local-report 文件名` 的后续处理把返回全量失败视图的入口标注为 `查看全部待处理失败样本：/inner-brain-eval-local-failed`，便于和指定文件已处理视图的待处理失败语义保持一致。
- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_local_file_failed_command_lists_only_selected_file_failures tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_can_filter_local_file tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v` 先失败，指定文件失败视图和报告导出反馈仍输出旧的 `查看全部本机失败样本：...`，版本仍为 `0.80.0`。
- GREEN：同一目标命令 3 项通过。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_agent tests.test_project_metadata -v`，304 项通过。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，566 项通过。
- 源码 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅出现既有可选 `Hidden import "tzdata" not found!` 警告。
- 版本化安装包：复制生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.81.0.exe`，大小 `57,131,008` 字节。
- 安装脚本与 SED：`windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.81.0"` 和 `Jarvis Lite 0.81.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.81.0 installation finished`，`TargetName=E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 打包后 smoke：`..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`；显式进程复查输出 `No residual JarvisLite.exe process.`。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK: 431 checked across 174 files`；敏感信息差异扫描输出 `Sensitive diff scan OK`；`config/llm.local.json` 与 `config/search.local.json` 当前不存在，`Test-Path` 均为 `False`。

## 0.82.0 InnerBrain 本机失败报告导出反馈全量待处理失败标签

- 目标：`/inner-brain-eval-local-report` 未指定文件导出全量失败报告后，把返回失败视图的入口标注为 `查看待处理失败样本：/inner-brain-eval-local-failed`，便于和指定文件报告导出反馈、全量已处理视图的待处理失败语义保持一致。
- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_local_report_command_exports_failed_markdown_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v` 先失败，全量报告导出反馈仍输出旧的 `查看本机失败样本：...`，版本仍为 `0.81.0`。
- GREEN：同一目标命令 2 项通过。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_agent tests.test_project_metadata -v`，304 项通过。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，566 项通过。
- 源码 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅出现既有可选 `Hidden import "tzdata" not found!` 警告。
- 版本化安装包：复制生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.82.0.exe`，大小 `57,135,104` 字节。
- 安装脚本与 SED：`windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.82.0"` 和 `Jarvis Lite 0.82.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.82.0 installation finished`，`TargetName=E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 打包后 smoke：`..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`；显式进程复查输出 `No residual JarvisLite.exe process.`。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK (175 files).`；敏感信息差异扫描输出 `Sensitive diff scan OK`；过期标记扫描无命中；`config/llm.local.json` 与 `config/search.local.json` 当前不存在，`Test-Path` 均为 `False`。

## 0.83.0 InnerBrain 本机文件失败视图全部待处理失败报告标签

- 目标：`/inner-brain-eval-local-file-failed 文件名` 聚焦指定文件失败样本后，把导出全量报告的入口标注为 `导出全部待处理失败报告：/inner-brain-eval-local-report`，便于和同一反馈中的 `查看全部待处理失败样本` 语义保持一致。
- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_local_file_failed_command_lists_only_selected_file_failures tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v` 先失败，指定文件失败视图仍输出旧的 `导出全部本机失败报告：...`，版本仍为 `0.82.0`。
- GREEN：同一目标命令 2 项通过。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_agent tests.test_project_metadata -v`，304 项通过。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，566 项通过。
- 源码 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅出现既有可选 `Hidden import "tzdata" not found!` 警告。
- 版本化安装包：复制生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.83.0.exe`，大小 `57,131,008` 字节。
- 安装脚本与 SED：`windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.83.0"` 和 `Jarvis Lite 0.83.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.83.0 installation finished`，`TargetName=E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 打包后 smoke：`..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`；显式进程复查输出 `No residual JarvisLite.exe process.`。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK (176 files).`；敏感信息差异扫描输出 `Sensitive diff scan OK`；当前文档过期标记扫描无命中；`config/llm.local.json` 与 `config/search.local.json` 当前不存在，`Test-Path` 均为 `False`。

## 0.84.0 InnerBrain 本机失败视图待处理失败报告标签

- 目标：`/inner-brain-eval-local-failed` 未指定文件查看全量待处理失败样本后，把全量报告入口标注为 `导出待处理失败报告：/inner-brain-eval-local-report`，便于和同一视图里的待处理失败语义保持一致。
- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_local_failed_command_lists_only_failed_local_cases tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v` 先失败，全量失败视图仍输出旧的 `导出本机失败报告：...`，版本仍为 `0.83.0`。
- 附加 RED：打包后 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 首次在 2 秒检查点残留 `JarvisLite.exe` 进程；新增 `tests.test_desktop_app.DesktopAppTests.test_smoke_mode_creates_desktop_pet_window` 清理断言后，当前实现会留下本次 smoke 创建的 `desktopPetWindow` 顶层 widget。
- GREEN：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_local_failed_command_lists_only_failed_local_cases tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version tests.test_desktop_app.DesktopAppTests.test_smoke_mode_creates_desktop_pet_window -v`，3 项通过。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_agent tests.test_project_metadata tests.test_desktop_app -v`，310 项通过。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，566 项通过。
- 源码 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅出现既有可选 `Hidden import "tzdata" not found!` 警告。
- 版本化安装包：复制生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.84.0.exe`，大小 `57,135,104` 字节。
- 安装脚本与 SED：`windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.84.0"` 和 `Jarvis Lite 0.84.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.84.0 installation finished`，`TargetName=E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 打包后 smoke：`..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`；显式进程复查输出 `ProcessCount=0`。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK: 440 checked across 177 files`；敏感信息差异扫描无命中；旧文案和过期版本扫描无命中；`config/llm.local.json` 与 `config/search.local.json` 当前不存在，`Test-Path` 均为 `False`。
- 调试备注：打包后 smoke 残留问题根因为 smoke 分支只关闭窗口但未显式销毁 Qt widget；实现已在 smoke 分支关闭并 `deleteLater()` 面板和桌面窗口，并处理 `DeferredDelete` 事件后退出。

## 0.85.0 InnerBrain 本机失败视图按文件待处理失败报告标签

- 目标：`/inner-brain-eval-local-failed` 未指定文件查看全量待处理失败样本后，把按文件报告入口标注为 `按文件导出待处理失败报告：/inner-brain-eval-local-report 文件名`，便于和同一视图里的全量待处理失败报告入口保持一致。
- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_local_failed_command_lists_only_failed_local_cases tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v` 先失败，全量失败视图仍输出旧的 `按文件导出失败报告：...`，版本仍为 `0.84.0`。
- GREEN：同一目标命令 2 项通过。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_agent tests.test_project_metadata -v`，304 项通过。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，566 项通过。
- 源码 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅出现既有可选 `Hidden import "tzdata" not found!` 警告。
- 版本化安装包：复制生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.85.0.exe`，大小 `57,135,104` 字节。
- 安装脚本与 SED：`windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.85.0"` 和 `Jarvis Lite 0.85.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.85.0 installation finished`，`TargetName=E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 打包后 smoke：`..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`；显式进程复查输出 `ProcessCount=0`。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK: 443 checked across 178 files`；敏感信息差异扫描无命中；旧文案和过期版本扫描无命中；`config/llm.local.json` 与 `config/search.local.json` 当前不存在，`Test-Path` 均为 `False`。

## 0.86.0 InnerBrain 本机当前文件待处理失败报告标签

- 目标：当前文件总览、当前文件失败视图和当前文件已处理视图在当前文件仍有待处理失败时，把报告入口标注为 `导出当前文件待处理失败报告：/inner-brain-eval-local-report 文件名`，便于和同一反馈中的待处理失败样本语义保持一致。
- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_local_file_command_suggests_report_for_failed_selected_file tests.test_agent.AgentTests.test_inner_brain_eval_local_file_failed_command_lists_only_selected_file_failures tests.test_agent.AgentTests.test_inner_brain_eval_local_resolved_command_suggests_report_for_file_with_pending_failures tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v` 先失败，三个当前文件反馈仍输出旧的 `导出当前文件失败报告：...`，版本仍为 `0.85.0`。
- GREEN：同一目标命令 4 项通过。
- 相邻回归：首次因更新测试 manifest 夹具仍为 `0.85.1` 且断言未同步而失败；按系统化调试定位为版本联动测试数据残留后，更新到 `0.86.1` 并复跑 `.\.venv\Scripts\python.exe -m unittest tests.test_agent tests.test_project_metadata -v`，304 项通过。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，566 项通过。
- 源码 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅出现既有可选 `Hidden import "tzdata" not found!` 警告。
- 版本化安装包：复制生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.86.0.exe`，大小 `57,135,104` 字节。
- 安装脚本与 SED：`windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.86.0"` 和 `Jarvis Lite 0.86.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.86.0 installation finished`，`TargetName=E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 打包后 smoke：`..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`；显式进程复查输出 `ProcessCount=0`。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK: 437 checked across 175 files`；敏感信息差异扫描无命中；旧文案和过期版本扫描无命中；`config/llm.local.json` 与 `config/search.local.json` 当前不存在，`Test-Path` 均为 `False`。

## 0.87.0 InnerBrain 本机评估样本保存反馈待处理失败标签

- 目标：本机 evaluation 样本保存反馈把后续验证入口标注为 `只看待处理失败样本：/inner-brain-eval-local-failed` 和 `导出样本文件待处理失败报告：/inner-brain-eval-local-report runtime.jsonl`，便于和本机失败视图的待处理语义保持一致。
- RED：`.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_eval_add_command_saves_local_evaluation_case_without_training tests.test_agent.AgentTests.test_inner_brain_eval_label_command_saves_local_evaluation_case_without_training tests.test_agent.AgentTests.test_inner_brain_eval_add_candidate_saves_selected_candidate_command_without_training tests.test_agent.AgentTests.test_inner_brain_eval_label_candidate_saves_selected_candidate_label_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v` 先失败，四个保存反馈仍输出旧的 `只看失败样本` 和 `导出样本文件失败报告`，版本仍为 `0.86.0`。
- GREEN：同一目标命令 5 项通过。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_agent tests.test_project_metadata -v`，304 项通过。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，566 项通过。
- 源码 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅出现既有可选 `Hidden import "tzdata" not found!` 警告。
- 版本化安装包：复制生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.87.0.exe`，大小 `57,135,104` 字节。
- 安装脚本与 SED：`windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.87.0"` 和 `Jarvis Lite 0.87.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.87.0 installation finished`，`TargetName=E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 打包后 smoke：`..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`；显式进程复查输出 `ProcessCount=0`。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 工作区换行提示；Markdown 本地链接检查输出 `Markdown local links OK: 440 checked across 176 files`；严格密钥形态扫描无命中；旧文案和过期版本扫描无命中；`config/llm.local.json` 与 `config/search.local.json` 当前不存在，`Test-Path` 均为 `False`。
