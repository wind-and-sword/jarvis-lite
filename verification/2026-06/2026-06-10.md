# 2026-06-10 验证记录

> 日期：2026-06-10
> 执行者：Codex

## 0.147.0 偏好应用状态解释第一阶段

- 目标：新增 `/preference-apply-status [编号或ID]` 只读解释偏好应用确认记录，默认解释最近一条确认记录，也可按编号或 `prefapp-...` 指定；逐面说明普通回复上下文、本地知识库回答附注和长期记忆兜底回答附注是否生效，并给出撤销、非最近记录、普通回复上下文开关停用、本地回答类型停用、当前启用偏好集合变化或偏好冲突等失效原因。
- RED：目标命令先失败；失败点为缺少 `describe_preference_application_status`，Agent `/preference-apply-status` 返回未知命令，项目版本仍为 `0.146.0`。
- GREEN：同一目标命令复跑通过，`Ran 458 tests in 6.984s`，`OK`。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，`Ran 790 tests in 9.454s`，`OK`。
- 命令行 smoke：临时项目内执行偏好候选确认、启用、偏好应用确认、默认 `/preference-apply-status`、停用普通回复上下文、停用 `knowledge`、按确认 ID 查询和未知引用查询；输出 `preference-application-status-explain-smoke OK`。
- 源码桌面 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 退出码 0，stdout 包含 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- Windows 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 成功，生成基础安装包 `E:\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；构建日志包含既有 `Hidden import "tzdata" not found!` 警告和第三方 `pyautogui` 的 `SyntaxWarning: invalid escape sequence '\e'`。
- 版本化安装包：复制生成 `E:\ai\jarvis-lite-dist\JarvisLiteSetup-0.147.0.exe`，大小 `59,719,680` 字节，时间戳 `2026/6/10 11:52:58`。
- 安装包元数据：`E:\ai\jarvis-lite-dist\windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.147.0"`；`JarvisLite.version.txt` 的 `filevers/prodvers` 为 `(0, 147, 0, 0)`；`desktop-exe\JarvisLite.exe` 的 `FileVersion/ProductVersion` 均为 `0.147.0`。
- 打包后 smoke：`E:\ai\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 重定向复验退出码 0，stdout 包含 `desktopPetWindow`，stderr 为空，无残留 `JarvisLite` 进程。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 提示；Markdown 本地链接检查 `696` 项通过，覆盖 `429` 个 tracked/untracked Markdown 文件；严格真实 key 形态扫描无命中；`config/llm.local.json`、`config/search.local.json` 和 `word/inner-brain-evaluation-report.md` 均不存在。

## 0.146.0 偏好普通回复上下文开关第一阶段

- 目标：新增用户显式可配置的普通回复偏好上下文开关，默认保持最近有效确认记录进入普通 LLM fallback 上下文和 `/llm-context-preview`；停用后只影响普通回复上下文，不撤销确认记录，不删除或停用偏好，不影响本地知识库回答和长期记忆兜底回答附注。
- RED：目标命令先失败；失败点为缺少 `describe_preference_reply_context_settings` / `set_preference_reply_context_enabled`，Agent 返回未知命令，停用普通回复上下文后 `/llm-context-preview` 仍展示 `已确认偏好应用：prefapp-...`，版本仍为 `0.145.0`。
- GREEN：同一目标命令复跑通过，`Ran 455 tests in 7.065s`，`OK`。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，`Ran 787 tests in 9.338s`，`OK`。
- 命令行 smoke：临时项目内执行 `/preference-reply-context`、偏好候选确认、偏好启用、偏好应用确认、`/llm-context-preview`、本地知识库回答、`/preference-reply-context-disable`、再次预览和本地知识库回答、`/preference-reply-context-enable`；输出 `preference-reply-context-settings-smoke OK`。
- 源码桌面 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 退出码 0，stdout 包含 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- Windows 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 成功，生成基础安装包 `E:\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；构建日志包含既有 `Hidden import "tzdata" not found!` 警告和第三方 `pyautogui` 的 `SyntaxWarning: invalid escape sequence '\e'`。
- 版本化安装包：复制生成 `E:\ai\jarvis-lite-dist\JarvisLiteSetup-0.146.0.exe`，大小 `59,719,680` 字节，时间戳 `2026/6/10 10:12:08`。
- 安装包元数据：`E:\ai\jarvis-lite-dist\windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.146.0"` 和 `Jarvis Lite 0.146.0 installed`；`E:\ai\jarvis-lite-dist\JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.146.0 installation finished`；`JarvisLite.version.txt` 的 `filevers/prodvers` 为 `(0, 146, 0, 0)`；`desktop-exe\JarvisLite.exe` 的 `FileVersion/ProductVersion` 均为 `0.146.0`。
- 打包后 smoke：`E:\ai\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 退出码 0；按 UTF-8 读取 stdout 确认包含 `desktopPetWindow`；stderr 长度为 0；残留 `JarvisLite` 进程数为 0。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 提示；Markdown 本地链接检查 `689` 项通过，覆盖 `428` 个 tracked/untracked Markdown 文件；严格真实 key 形态扫描无命中；`config/llm.local.json`、`config/search.local.json` 和 `word/inner-brain-evaluation-report.md` 均不存在。

## 0.145.0 偏好本地回答类型开关第一阶段

- 目标：新增用户显式可配置的本地回答附注类型开关，默认保持 `knowledge` 和 `memory` 双开；可显式查看、启用或停用本地知识库回答与长期记忆兜底回答两类附注；不把新增命令加入 LLM 白名单，不改变普通 LLM fallback、SearchRouter、InnerBrain、路由或桌面执行决策。
- RED：目标命令先失败；失败点为缺少 `describe_preference_local_answer_type_settings` / `set_preference_local_answer_type_enabled`，Agent 返回未知命令，停用 knowledge 后知识库附注仍展示，版本仍为 `0.144.0`。
- GREEN：同一目标命令复跑通过，`Ran 7 tests`，`OK`。
- 相邻回归：`.\.venv\Scripts\python.exe -m unittest tests.test_preferences tests.test_agent tests.test_llm tests.test_project_metadata -v`，`Ran 451 tests in 7.182s`，`OK`。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，`Ran 783 tests in 9.632s`，`OK`。
- 命令行 smoke：临时项目内执行 `/preference-answer-types`、偏好候选确认、偏好启用、偏好应用确认、知识库回答、停用 knowledge、长期记忆兜底、停用 memory、重新启用两类类型和 `/llm-context-preview`；输出 `preference-local-answer-type-settings-smoke OK`。
- 源码桌面 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 退出码 0，stdout 包含 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- Windows 安装包构建：`.\.venv\Scripts\python.exe scripts\build_windows_installer.py` 成功，生成基础安装包 `E:\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；构建日志包含既有 `Hidden import "tzdata" not found!` 警告和第三方 `pyautogui` 的 `SyntaxWarning: invalid escape sequence '\e'`。
- 版本化安装包：复制生成 `E:\ai\jarvis-lite-dist\JarvisLiteSetup-0.145.0.exe`，大小 `59,719,680` 字节，时间戳 `2026/6/10 9:43:11`。
- 安装包元数据：`E:\ai\jarvis-lite-dist\windows-installer-stage\install.cmd` 包含 `DisplayVersion /d "0.145.0"` 和 `Jarvis Lite 0.145.0 installed`；`E:\ai\jarvis-lite-dist\JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.145.0 installation finished`；`JarvisLite.version.txt` 的 `filevers/prodvers` 为 `(0, 145, 0, 0)`；`desktop-exe\JarvisLite.exe` 的 `FileVersion/ProductVersion` 均为 `0.145.0`。
- 打包后 smoke：`E:\ai\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 退出码 0；按 UTF-8 读取 stdout 确认包含 `desktopPetWindow`；stderr 长度为 0；残留 `JarvisLite` 进程数为 0。

## 最终收紧检查

- 文档同步后全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，`Ran 790 tests in 9.193s`，`OK`。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 提示。
- Markdown 本地链接检查：696 项通过，覆盖 429 个 tracked/untracked Markdown 文件。
- 严格真实 key 形态扫描：无命中。
- 本地敏感配置文件：`config/llm.local.json`、`config/search.local.json` 和 `word/inner-brain-evaluation-report.md` 均不存在。
- 文档入口规模：README 108 行，根 `verification.md` 23 行。
