# 2026-06-01 验证记录

> 日期：2026-06-01
> 执行者：Codex

## 2026-06-01 0.19.0 InnerBrain 候选按编号教学验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_teach_candidate_saves_selected_candidate_command tests.test_agent.AgentTests.test_inner_brain_teach_candidate_reports_missing_candidate tests.test_agent.AgentTests.test_inner_brain_teach_candidate_rejects_unknown_target_command -v
.\.venv\Scripts\python.exe -m unittest tests.test_project_metadata -v
```

结果：符合预期。

- 3 个候选教学测试失败或错误，`/inner-brain-teach-candidate` 尚未实现。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.18.0`，期望 `0.19.0`。

### GREEN 与后续验证

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_teach_candidate_saves_selected_candidate_command tests.test_agent.AgentTests.test_inner_brain_teach_candidate_reports_missing_candidate tests.test_agent.AgentTests.test_inner_brain_teach_candidate_rejects_unknown_target_command tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，4 项 OK。

- `test_inner_brain_teach_candidate_saves_selected_candidate_command` 验证第 1 条候选可保存为 `/kb` 命令样本，并且保存动作不把自身写入最近路由。
- `test_inner_brain_teach_candidate_reports_missing_candidate` 验证候选编号不存在时不写入 runtime 样本。
- `test_inner_brain_teach_candidate_rejects_unknown_target_command` 验证未知目标命令会被白名单拒绝。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.19.0`。

## 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_desktop_widgets tests.test_desktop_bridge tests.test_desktop_app tests.test_agent tests.test_llm tests.test_inner_brain tests.test_conversation -v
```

结果：通过，375 项 OK。

## 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，502 项 OK。

## 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.19.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.19\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.19\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.19.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.19.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.19.0.exe` 大小均为 `57,102,336` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 退出码为 0。
- `install.cmd` 写入 `DisplayVersion` 为 `0.19.0`，并包含 `Jarvis Lite 0.19.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.19.0 installation finished`。

## 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-01 0.25.0 桌面候选模板状态同步验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_desktop_widgets.DesktopWidgetTests.test_panel_empty_inner_brain_candidates_disables_candidate_template_buttons tests.test_desktop_widgets.DesktopWidgetTests.test_panel_inner_brain_candidates_result_limits_template_index_to_candidate_count tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期。

- 桌面候选模板尚无状态同步辅助方法，空候选和候选数量同步测试失败。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.24.0`，期望 `0.25.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_desktop_widgets.DesktopWidgetTests.test_panel_empty_inner_brain_candidates_disables_candidate_template_buttons tests.test_desktop_widgets.DesktopWidgetTests.test_panel_inner_brain_candidates_result_limits_template_index_to_candidate_count tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，3 项 OK。

- `test_panel_empty_inner_brain_candidates_disables_candidate_template_buttons` 验证 `/inner-brain-candidates` 返回空候选时显示 `候选模板：暂无候选`，并禁用“填教学”“填标注”。
- `test_panel_inner_brain_candidates_result_limits_template_index_to_candidate_count` 验证有候选时显示候选数量、启用模板按钮，并把编号上限同步到候选数。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.25.0`。

### 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_desktop_widgets tests.test_desktop_bridge tests.test_desktop_app tests.test_agent tests.test_llm tests.test_inner_brain tests.test_conversation -v
```

结果：通过，390 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，517 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.25.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.25\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.25\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.25.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.25.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.25.0.exe` 大小均为 `57,110,528` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.25.0`，并包含 `Jarvis Lite 0.25.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.25.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-01 0.29.0 InnerBrain 固定评估集验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_evaluation_reports_repeatable_seed_baseline tests.test_agent.AgentTests.test_inner_brain_eval_command_reports_repeatable_seed_baseline tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期。

- InnerBrain 评估 API 尚不存在，`describe_inner_brain_evaluation` 导入失败。
- `/inner-brain-eval` 尚未接入 Agent，返回未知命令。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.28.0`，期望 `0.29.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_evaluation_reports_repeatable_seed_baseline tests.test_agent.AgentTests.test_inner_brain_eval_command_reports_repeatable_seed_baseline tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，3 项 OK。

- `test_inner_brain_evaluation_reports_repeatable_seed_baseline` 验证固定评估集不少于 8 条、失败数为 0，并输出 `seed_evaluation`。
- `test_inner_brain_eval_command_reports_repeatable_seed_baseline` 验证 `/inner-brain-eval` 返回评估摘要，不再是未知命令。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.29.0`。

### InnerBrain 专项与相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain -v
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent tests.test_llm tests.test_desktop_widgets tests.test_desktop_bridge tests.test_desktop_app tests.test_conversation -v
```

结果：

- InnerBrain 专项通过，26 项 OK。
- 相邻回归通过，397 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，524 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.29.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.29\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.29\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.29.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.29.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.29.0.exe` 大小均为 `57,114,624` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.29.0`，并包含 `Jarvis Lite 0.29.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.29.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK (268 files)`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-01 0.30.0 InnerBrain 本机评估集扩展验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_evaluation_loads_local_jsonl_cases tests.test_agent.AgentTests.test_inner_brain_eval_command_includes_local_evaluation_cases_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期。

- `load_evaluation_cases` 尚未实现，InnerBrain 本机评估加载测试导入失败。
- `/inner-brain-eval` 仍只显示 `seed_evaluation`，未合并本机评估 JSONL。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.29.0`，期望 `0.30.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_evaluation_loads_local_jsonl_cases tests.test_agent.AgentTests.test_inner_brain_eval_command_includes_local_evaluation_cases_without_training tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，3 项 OK。

- `test_inner_brain_evaluation_loads_local_jsonl_cases` 验证 `data/inner-brain/evaluation/*.jsonl` 可加载为 `local_evaluation`，并与 seed 评估集一起执行。
- `test_inner_brain_eval_command_includes_local_evaluation_cases_without_training` 验证 `/inner-brain-eval` 输出本机评估计数，且不创建 `data/inner-brain/training/runtime.jsonl`。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.30.0`。

### InnerBrain 专项与相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain -v
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent tests.test_llm tests.test_desktop_widgets tests.test_desktop_bridge tests.test_desktop_app tests.test_conversation -v
```

结果：

- InnerBrain 专项通过，27 项 OK。
- 相邻回归通过，399 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，526 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.30.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.30\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.30\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.30.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅报告已有的可选 `tzdata` hidden import 警告。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.30.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.30.0.exe` 大小均为 `57,118,720` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.30.0`，并包含 `Jarvis Lite 0.30.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.30.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-01 0.31.0 InnerBrain 评估失败修复建议验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_evaluation_describes_failed_cases_with_training_suggestions tests.test_agent.AgentTests.test_inner_brain_eval_command_suggests_explicit_training_for_failed_local_cases tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期。

- 失败评估描述尚未输出 `失败修复建议：`。
- Agent `/inner-brain-eval` 对失败本机评估样本只输出 FAIL 和原因，不输出显式训练建议。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.30.0`，期望 `0.31.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_evaluation_describes_failed_cases_with_training_suggestions tests.test_agent.AgentTests.test_inner_brain_eval_command_suggests_explicit_training_for_failed_local_cases tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，3 项 OK。

- `test_inner_brain_evaluation_describes_failed_cases_with_training_suggestions` 验证失败报告会提示 `/inner-brain-teach 请看看资料库状态 => /kb-summary`，且不创建 runtime 样本。
- `test_inner_brain_eval_command_suggests_explicit_training_for_failed_local_cases` 验证 Agent 命令同样输出失败修复建议。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.31.0`。

### InnerBrain 专项与相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain -v
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent tests.test_llm tests.test_desktop_widgets tests.test_desktop_bridge tests.test_desktop_app tests.test_conversation -v
```

结果：

- InnerBrain 专项通过，28 项 OK。
- 相邻回归通过，401 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，528 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.31.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.31\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.31\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.31.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅报告已有的可选 `tzdata` hidden import 警告。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.31.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.31.0.exe` 大小均为 `57,118,720` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.31.0`，并包含 `Jarvis Lite 0.31.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.31.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-01 0.32.0 InnerBrain 评估失败过滤视图验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_evaluation_can_describe_failed_cases_only tests.test_agent.AgentTests.test_inner_brain_eval_failed_command_lists_only_failed_cases tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期。

- `describe_inner_brain_evaluation()` 尚不接受 `failures_only` 参数。
- Agent `/inner-brain-eval-failed` 仍返回未知命令。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.31.0`，期望 `0.32.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_evaluation_can_describe_failed_cases_only tests.test_agent.AgentTests.test_inner_brain_eval_failed_command_lists_only_failed_cases tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，3 项 OK。

- `test_inner_brain_evaluation_can_describe_failed_cases_only` 验证失败过滤视图只显示失败样本，并保留 `/inner-brain-teach 请看看资料库状态 => /kb-summary` 修复建议。
- `test_inner_brain_eval_failed_command_lists_only_failed_cases` 验证 Agent `/inner-brain-eval-failed` 只输出失败样本，不输出通过样本，也不创建 runtime 训练样本。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.32.0`。

### InnerBrain 专项与相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain -v
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent tests.test_llm tests.test_desktop_widgets tests.test_desktop_bridge tests.test_desktop_app tests.test_conversation -v
```

结果：

- InnerBrain 专项通过，29 项 OK。
- 相邻回归通过，403 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，530 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.32.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.32\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.32\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.32.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅报告已有的可选 `tzdata` hidden import 警告。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.32.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.32.0.exe` 大小均为 `57,118,720` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.32.0`，并包含 `Jarvis Lite 0.32.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.32.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-01 0.33.0 InnerBrain 本机评估过滤视图验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_evaluation_can_filter_local_evaluation_source tests.test_agent.AgentTests.test_inner_brain_eval_local_command_lists_only_local_cases tests.test_agent.AgentTests.test_inner_brain_eval_local_failed_command_lists_only_failed_local_cases tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期。

- `evaluate_inner_brain()` 尚不接受 `source_filter` 参数。
- Agent `/inner-brain-eval-local` 与 `/inner-brain-eval-local-failed` 仍返回未知命令。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.32.0`，期望 `0.33.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_inner_brain_evaluation_can_filter_local_evaluation_source tests.test_agent.AgentTests.test_inner_brain_eval_local_command_lists_only_local_cases tests.test_agent.AgentTests.test_inner_brain_eval_local_failed_command_lists_only_failed_local_cases tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，4 项 OK。

- `test_inner_brain_evaluation_can_filter_local_evaluation_source` 验证 `source_filter="local_evaluation"` 只执行本机 JSONL 样本，报告名为 `local_evaluation`。
- `test_inner_brain_eval_local_command_lists_only_local_cases` 验证 Agent `/inner-brain-eval-local` 只输出本机样本，不输出 seed 样本，也不创建 runtime 训练样本。
- `test_inner_brain_eval_local_failed_command_lists_only_failed_local_cases` 验证 Agent `/inner-brain-eval-local-failed` 只显示本机失败项和修复建议。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.33.0`。

### InnerBrain 专项与相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain -v
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent tests.test_llm tests.test_desktop_widgets tests.test_desktop_bridge tests.test_desktop_app tests.test_conversation -v
```

结果：

- InnerBrain 专项通过，30 项 OK。
- 相邻回归通过，406 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，533 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.33.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.33\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.33\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.33.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`；PyInstaller 仅报告已有的可选 `tzdata` hidden import 警告。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.33.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.33.0.exe` 大小均为 `57,118,720` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.33.0`，并包含 `Jarvis Lite 0.33.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.33.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-01 0.26.0 桌面候选选择绑定验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_desktop_widgets.DesktopWidgetTests.test_panel_empty_inner_brain_candidates_disables_candidate_selector tests.test_desktop_widgets.DesktopWidgetTests.test_panel_inner_brain_candidate_selector_binds_selected_candidate_to_templates tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期。

- 桌面候选模板尚无候选下拉框 helper，2 个桌面测试报 `AttributeError`。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.25.0`，期望 `0.26.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_desktop_widgets.DesktopWidgetTests.test_panel_empty_inner_brain_candidates_disables_candidate_selector tests.test_desktop_widgets.DesktopWidgetTests.test_panel_inner_brain_candidate_selector_binds_selected_candidate_to_templates tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，3 项 OK。

- `test_panel_empty_inner_brain_candidates_disables_candidate_selector` 验证空候选时候选下拉框显示 `暂无候选` 并禁用。
- `test_panel_inner_brain_candidate_selector_binds_selected_candidate_to_templates` 验证候选下拉框展示当前候选，选择第 2 条候选会同步编号，并让“填标注”使用第 2 条候选编号填入模板。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.26.0`。

### 桌面专项与相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_desktop_widgets -v
.\.venv\Scripts\python.exe -m unittest tests.test_desktop_bridge tests.test_desktop_app tests.test_agent tests.test_llm tests.test_inner_brain tests.test_conversation -v
```

结果：

- 桌面 widget 专项通过，37 项 OK。
- 相邻回归通过，355 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，519 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.26.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.26\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.26\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.26.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.26.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.26.0.exe` 大小均为 `57,110,528` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.26.0`，并包含 `Jarvis Lite 0.26.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.26.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-01 0.27.0 桌面候选目标预填验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_desktop_widgets.DesktopWidgetTests.test_panel_exposes_inner_brain_candidate_template_controls tests.test_desktop_widgets.DesktopWidgetTests.test_panel_inner_brain_teach_template_can_prefill_known_target_command tests.test_desktop_widgets.DesktopWidgetTests.test_panel_inner_brain_label_template_can_prefill_known_intent_template tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期。

- 桌面面板尚无 `candidate_teach_target_options()`、`change_candidate_teach_target()`、`change_candidate_label_target()` 等目标预填 helper，3 个桌面测试失败。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.26.0`，期望 `0.27.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_desktop_widgets.DesktopWidgetTests.test_panel_exposes_inner_brain_candidate_template_controls tests.test_desktop_widgets.DesktopWidgetTests.test_panel_inner_brain_teach_template_can_prefill_known_target_command tests.test_desktop_widgets.DesktopWidgetTests.test_panel_inner_brain_label_template_can_prefill_known_intent_template tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，4 项 OK。

- `test_panel_exposes_inner_brain_candidate_template_controls` 验证桌面训练区暴露教学命令和标注模板选项。
- `test_panel_inner_brain_teach_template_can_prefill_known_target_command` 验证选择 `/kb` 后，“填教学”只把 `/inner-brain-teach-candidate 编号 => /kb` 写入输入框，不提交。
- `test_panel_inner_brain_label_template_can_prefill_known_intent_template` 验证选择 `web.search query=关键词` 后，“填标注”只把 `/inner-brain-label-candidate 编号 => web.search query=关键词` 写入输入框，不提交。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.27.0`。

### 桌面专项与相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_desktop_widgets -v
.\.venv\Scripts\python.exe -m unittest tests.test_desktop_bridge tests.test_desktop_app tests.test_agent tests.test_llm tests.test_inner_brain tests.test_conversation -v
```

结果：

- 桌面 widget 专项通过，39 项 OK。
- 相邻回归通过，355 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，521 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.27.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.27\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.27\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.27.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.27.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.27.0.exe` 大小均为 `57,114,624` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.27.0`，并包含 `Jarvis Lite 0.27.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.27.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-01 0.28.0 InnerBrain 样本包含签名置信度补偿验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_sample_classifier_boosts_contained_sample_signature tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期。

- `帮我看一下知识库状态` 已命中 `knowledge.status`，但置信度仍低于高置信阈值，策略为 `CLARIFY`，不能直接执行。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.27.0`，期望 `0.28.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain.InnerBrainTests.test_sample_classifier_boosts_contained_sample_signature tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，2 项 OK。

- `test_sample_classifier_boosts_contained_sample_signature` 验证长句完整包含 `知识库状态` 样本签名时，结果为 `knowledge.status`、`EXECUTE`、`seed_sample`，并生成 `/kb`。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.28.0`。

### InnerBrain 专项与相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain -v
.\.venv\Scripts\python.exe -m unittest tests.test_inner_brain tests.test_agent tests.test_llm tests.test_desktop_widgets tests.test_desktop_bridge tests.test_desktop_app tests.test_conversation -v
```

结果：

- InnerBrain 专项通过，25 项 OK。
- 相邻回归通过，395 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，522 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.28.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.28\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.28\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.28.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.28.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.28.0.exe` 大小均为 `57,114,624` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.28.0`，并包含 `Jarvis Lite 0.28.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.28.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK (266 files)`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-01 0.21.0 InnerBrain 候选频次排序验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_candidates_prioritize_repeated_fallback_prompts tests.test_agent.AgentTests.test_inner_brain_teach_candidate_uses_frequency_ranked_candidate tests.test_agent.AgentTests.test_inner_brain_label_candidate_uses_frequency_ranked_candidate tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期。

- 重复候选仍按最近顺序展示，没有 `出现次数：2`。
- `/inner-brain-teach-candidate 1` 和 `/inner-brain-label-candidate 1` 仍选中最新单次候选。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.20.0`，期望 `0.21.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_candidates_prioritize_repeated_fallback_prompts tests.test_agent.AgentTests.test_inner_brain_teach_candidate_uses_frequency_ranked_candidate tests.test_agent.AgentTests.test_inner_brain_label_candidate_uses_frequency_ranked_candidate tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，4 项 OK。

- `test_inner_brain_candidates_prioritize_repeated_fallback_prompts` 验证重复候选显示 `出现次数：2` 并排在单次候选前。
- `test_inner_brain_teach_candidate_uses_frequency_ranked_candidate` 验证编号教学使用频次排序后的第 1 条候选。
- `test_inner_brain_label_candidate_uses_frequency_ranked_candidate` 验证编号标注使用频次排序后的第 1 条候选。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.21.0`。

### 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_desktop_widgets tests.test_desktop_bridge tests.test_desktop_app tests.test_agent tests.test_llm tests.test_inner_brain tests.test_conversation -v
```

结果：通过，381 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，508 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.21.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.21\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.21\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.21.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.21.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.21.0.exe` 大小均为 `57,102,336` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.21.0`，并包含 `Jarvis Lite 0.21.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.21.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-01 0.22.0 InnerBrain 候选运行态统计验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_candidates_keep_persistent_counts_after_recent_history_rotation tests.test_agent.AgentTests.test_inner_brain_teach_candidate_uses_persistent_rank_after_recent_history_rotation tests.test_agent.AgentTests.test_inner_brain_teach_candidate_removes_trained_persistent_candidate tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期。

- 最近 5 条路由轮转后，高频候选仍会丢失。
- `/inner-brain-teach-candidate 1` 仍选中最新单次候选，而不是运行态高频候选。
- 教学后旧候选仍会从候选列表出现。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.21.0`，期望 `0.22.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_candidates_keep_persistent_counts_after_recent_history_rotation tests.test_agent.AgentTests.test_inner_brain_teach_candidate_uses_persistent_rank_after_recent_history_rotation tests.test_agent.AgentTests.test_inner_brain_teach_candidate_removes_trained_persistent_candidate tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，4 项 OK。

- `test_inner_brain_candidates_keep_persistent_counts_after_recent_history_rotation` 验证候选出现次数跨最近 5 条路由保留。
- `test_inner_brain_teach_candidate_uses_persistent_rank_after_recent_history_rotation` 验证编号教学使用运行态统计排序后的第 1 条候选。
- `test_inner_brain_teach_candidate_removes_trained_persistent_candidate` 验证显式教学后候选从运行态统计中移除。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.22.0`。

### 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_desktop_widgets tests.test_desktop_bridge tests.test_desktop_app tests.test_agent tests.test_llm tests.test_inner_brain tests.test_conversation -v
```

结果：通过，384 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，511 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.22.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.22\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.22\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.22.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.22.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.22.0.exe` 大小均为 `57,106,432` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.22.0`，并包含 `Jarvis Lite 0.22.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.22.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-01 0.23.0 桌面内脑候选快捷入口验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_desktop_bridge.DesktopBridgeTests.test_quick_commands_include_current_assistant_capabilities tests.test_desktop_bridge.DesktopBridgeTests.test_direct_quick_commands_exclude_commands_that_need_arguments tests.test_desktop_widgets.DesktopWidgetTests.test_panel_exposes_only_direct_quick_command_buttons tests.test_desktop_widgets.DesktopWidgetTests.test_panel_inner_brain_candidates_quick_command_submits_candidates_command tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期。

- 桌面快捷命令清单缺少 `/inner-brain-candidates`。
- 直接快捷命令和桌面按钮缺少“内脑候选”。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.22.0`，期望 `0.23.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_desktop_bridge.DesktopBridgeTests.test_quick_commands_include_current_assistant_capabilities tests.test_desktop_bridge.DesktopBridgeTests.test_direct_quick_commands_exclude_commands_that_need_arguments tests.test_desktop_widgets.DesktopWidgetTests.test_panel_exposes_only_direct_quick_command_buttons tests.test_desktop_widgets.DesktopWidgetTests.test_panel_inner_brain_candidates_quick_command_submits_candidates_command tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，5 项 OK。

- `test_quick_commands_include_current_assistant_capabilities` 验证快捷命令包含 `/inner-brain-candidates`。
- `test_direct_quick_commands_exclude_commands_that_need_arguments` 验证“内脑候选”属于无需参数的直接快捷命令。
- `test_panel_exposes_only_direct_quick_command_buttons` 验证桌面面板显示“内脑候选”按钮。
- `test_panel_inner_brain_candidates_quick_command_submits_candidates_command` 验证点击按钮会执行 `/inner-brain-candidates` 并展示候选。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.23.0`。

### 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_desktop_widgets tests.test_desktop_bridge tests.test_desktop_app tests.test_agent tests.test_llm tests.test_inner_brain tests.test_conversation -v
```

结果：通过，385 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，512 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.23.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.23\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.23\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.23.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.23.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.23.0.exe` 大小均为 `57,106,432` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 退出码为 0，窗口版 exe 无控制台输出。
- `install.cmd` 写入 `DisplayVersion` 为 `0.23.0`，并包含 `Jarvis Lite 0.23.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.23.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`；首次脚本按 Windows 默认 GBK 解码 diff 失败，改为 bytes 捕获并按 UTF-8 容错解码后复跑通过。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-01 0.24.0 桌面候选训练模板填充验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_desktop_widgets.DesktopWidgetTests.test_panel_exposes_inner_brain_candidate_template_controls tests.test_desktop_widgets.DesktopWidgetTests.test_panel_inner_brain_teach_template_button_fills_input_without_submitting tests.test_desktop_widgets.DesktopWidgetTests.test_panel_inner_brain_label_template_button_fills_label_template_for_selected_candidate tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期。

- 桌面面板尚无候选模板控件辅助方法，3 个桌面测试报 `AttributeError`。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.23.0`，期望 `0.24.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_desktop_widgets.DesktopWidgetTests.test_panel_exposes_inner_brain_candidate_template_controls tests.test_desktop_widgets.DesktopWidgetTests.test_panel_inner_brain_teach_template_button_fills_input_without_submitting tests.test_desktop_widgets.DesktopWidgetTests.test_panel_inner_brain_label_template_button_fills_label_template_for_selected_candidate tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，4 项 OK。

- `test_panel_exposes_inner_brain_candidate_template_controls` 验证候选编号默认是 1，并暴露“填教学”“填标注”按钮。
- `test_panel_inner_brain_teach_template_button_fills_input_without_submitting` 验证“填教学”只填入 `/inner-brain-teach-candidate 编号 => `，不提交、不写 transcript。
- `test_panel_inner_brain_label_template_button_fills_label_template_for_selected_candidate` 验证“填标注”按编号填入 `/inner-brain-label-candidate 编号 => intent slot=value`。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.24.0`。

### 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_desktop_widgets tests.test_desktop_bridge tests.test_desktop_app tests.test_agent tests.test_llm tests.test_inner_brain tests.test_conversation -v
```

结果：通过，388 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，515 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.24.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.24\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.24\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.24.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.24.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.24.0.exe` 大小均为 `57,110,528` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 退出码为 0，窗口版 exe 无控制台输出。
- `install.cmd` 写入 `DisplayVersion` 为 `0.24.0`，并包含 `Jarvis Lite 0.24.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.24.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。

## 2026-06-01 0.20.0 InnerBrain 候选按编号标注验证

### RED

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_label_candidate_saves_selected_candidate_label tests.test_agent.AgentTests.test_inner_brain_label_candidate_reports_missing_candidate tests.test_agent.AgentTests.test_inner_brain_label_candidate_rejects_invalid_slot_assignment tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：符合预期。

- 3 个候选标注测试失败或错误，`/inner-brain-label-candidate` 尚未实现。
- 版本一致性测试失败，`pyproject.toml` 仍为 `0.19.0`，期望 `0.20.0`。

### GREEN

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_agent.AgentTests.test_inner_brain_label_candidate_saves_selected_candidate_label tests.test_agent.AgentTests.test_inner_brain_label_candidate_reports_missing_candidate tests.test_agent.AgentTests.test_inner_brain_label_candidate_rejects_invalid_slot_assignment tests.test_project_metadata.ProjectMetadataTests.test_project_version_matches_release_package_version -v
```

结果：通过，4 项 OK。

- `test_inner_brain_label_candidate_saves_selected_candidate_label` 验证第 1 条候选可保存为 `knowledge.status command=/kb` 标注样本，并且保存动作不把自身写入最近路由。
- `test_inner_brain_label_candidate_reports_missing_candidate` 验证候选编号不存在时不写入 runtime 样本。
- `test_inner_brain_label_candidate_rejects_invalid_slot_assignment` 验证 slot 缺少 `=` 时返回标注参数错误且不写入样本。
- `test_project_version_matches_release_package_version` 验证 `pyproject.toml` 与包版本同为 `0.20.0`。

### 相邻回归

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_desktop_widgets tests.test_desktop_bridge tests.test_desktop_app tests.test_agent tests.test_llm tests.test_inner_brain tests.test_conversation -v
```

结果：通过，378 项 OK。

### 全量测试

命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

结果：通过，505 项 OK。

### 桌面与安装包验证

命令：

```powershell
.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke
.\.venv\Scripts\python.exe scripts\build_windows_installer.py
Copy-Item -LiteralPath ..\jarvis-lite-dist\JarvisLiteSetup.exe -Destination ..\jarvis-lite-dist\JarvisLiteSetup-0.20.0.exe -Force
..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke
Select-String -Path ..\jarvis-lite-dist\windows-installer-stage\install.cmd -Pattern 'DisplayVersion|Jarvis Lite 0\.20\.0 installed'
Select-String -Path ..\jarvis-lite-dist\JarvisLiteSetup.sed -Pattern 'Jarvis Lite 0\.20\.0 installation finished'
Get-Item ..\jarvis-lite-dist\JarvisLiteSetup.exe, ..\jarvis-lite-dist\JarvisLiteSetup-0.20.0.exe
```

结果：

- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- PyInstaller + IExpress 构建成功，输出 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`。
- 版本化安装包复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.20.0.exe`。
- `JarvisLiteSetup.exe` 与 `JarvisLiteSetup-0.20.0.exe` 大小均为 `57,102,336` 字节。
- 打包后的 `..\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke` 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `install.cmd` 写入 `DisplayVersion` 为 `0.20.0`，并包含 `Jarvis Lite 0.20.0 installed`。
- `JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.20.0 installation finished`。

### 收尾检查

命令：

```powershell
git diff --check
Markdown 本地链接检查脚本
敏感信息差异扫描
git ls-files config/llm.local.json config/search.local.json
Test-Path .\config\llm.local.json
Test-Path .\config\search.local.json
```

结果：

- `git diff --check` 退出码为 0，仅输出工作区 LF/CRLF 换行提示。
- Markdown 本地链接检查通过，输出 `Markdown local links OK`。
- 敏感信息差异扫描通过，输出 `Sensitive diff scan OK`。
- `config/llm.local.json` 与 `config/search.local.json` 未被 Git 跟踪。
- 本机 `config/llm.local.json` 与 `config/search.local.json` 当前均不存在，`Test-Path` 均为 `False`。
