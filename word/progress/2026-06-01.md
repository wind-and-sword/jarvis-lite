# 2026-06-01 进度记录

> 日期：2026-06-01
> 执行者：Codex

## 当日目标

- 延续 InnerBrain 训练闭环，保持“外脑处理开放问题、内脑沉淀固定动作”的边界。
- 收口 `0.19.0`：让 `/inner-brain-candidates` 的候选可通过编号直接教学为明确命令。
- 收口 `0.20.0`：让 `/inner-brain-candidates` 的候选可通过编号直接人工标注为 `intent/slot` 样本。
- 收口 `0.21.0`：让重复出现的 InnerBrain 训练候选按出现次数优先排序。
- 收口 `0.22.0`：让 InnerBrain 训练候选出现次数写入本地运行态统计，并在显式训练后移除对应候选。
- 收口 `0.23.0`：让桌面面板和托盘可一键打开 InnerBrain 训练候选。
- 收口 `0.24.0`：让桌面面板可按候选编号填入 teach/label 显式训练模板。
- 收口 `0.25.0`：让桌面候选模板按钮和编号上限随最近候选列表同步。
- 收口 `0.26.0`：让桌面候选下拉框直接绑定候选列表单条候选。
- 收口 `0.27.0`：让桌面候选训练模板可显式预填常见教学命令和标注 intent。
- 收口 `0.28.0`：增强 InnerBrain 样本分类器，在用户长句包含已知样本签名时补偿置信度。
- 收口 `0.29.0`：建立 InnerBrain 固定评估集和 `/inner-brain-eval` 评估入口。
- 收口 `0.30.0`：让 `/inner-brain-eval` 合并本机 local evaluation JSONL 样本。
- 收口 `0.31.0`：让 `/inner-brain-eval` 在失败时输出显式训练修复建议。

## 完成事项

- 新增 `/inner-brain-teach-candidate 编号 => /命令`：
  - 复用 `/inner-brain-candidates` 的候选筛选顺序。
  - 目标命令复用 `/inner-brain-teach` 的白名单校验和保存逻辑。
  - 保存后重新加载 InnerBrain，后续相同输入可走本地内脑。
  - 命令本身不记录到最近路由历史，避免改变候选编号。
- 更新 v23 方案文档和项目主方案。
- 新增 `/inner-brain-label-candidate 编号 => intent [slot=value ...]`：
  - 复用 `/inner-brain-candidates` 的候选筛选顺序。
  - 复用 `/inner-brain-label` 的 slot 解析和 runtime 样本保存逻辑。
  - 保存后重新加载 InnerBrain，后续相同输入可走本地内脑。
  - 命令本身不记录到最近路由历史，避免改变候选编号。
- 更新 v24 方案文档和项目主方案。
- 新增候选频次排序：
  - `/inner-brain-candidates` 对最近 5 条候选按原文精确聚合。
  - 输出展示 `出现次数：N`。
  - 排序规则为出现次数降序，同频按最近一次出现顺序。
  - `/inner-brain-teach-candidate` 与 `/inner-brain-label-candidate` 使用同一排序后的编号。
- 更新 v25 方案文档和项目主方案。
- 新增候选运行态统计：
  - `llm-fallback`、`memory-fallback` 和 `inner-brain-clarify` 会写入本地运行态候选统计。
  - `/inner-brain-candidates` 优先展示运行态候选统计，候选被最近 5 条路由挤出后仍保留出现次数。
  - `/inner-brain-teach-candidate` 与 `/inner-brain-label-candidate` 使用同一统计编号。
  - 显式 teach、label 或 adopt 后移除对应候选，避免已训练文本继续提示。
- 更新 v26 方案文档和项目主方案。
- 新增桌面“内脑候选”快捷入口：
  - `quick_commands()` 新增“内脑候选”并指向 `/inner-brain-candidates`。
  - `direct_quick_commands()` 将其纳入无需参数的直接快捷命令。
  - 桌面面板按钮和托盘菜单复用同一入口。
- 更新 v27 方案文档和项目主方案。
- 新增桌面候选训练模板填充入口：
  - 桌面面板新增候选编号 stepper，默认从第 1 条候选开始。
  - “填教学”将 `/inner-brain-teach-candidate 编号 => ` 写入输入框。
  - “填标注”将 `/inner-brain-label-candidate 编号 => intent slot=value` 写入输入框。
  - 填充动作不自动提交、不写 transcript、不改变路由状态。
- 更新 v28 方案文档和项目主方案。
- 新增桌面候选模板状态同步：
  - `/inner-brain-candidates` 返回空候选时，桌面面板显示“候选模板：暂无候选”并禁用模板按钮。
  - `/inner-brain-candidates` 返回候选时，桌面面板显示候选数量并把编号上限收紧到实际候选数。
  - 普通聊天、填模板和训练命令不触发 UI 自动训练。
- 更新 v29 方案文档和项目主方案。
- 新增桌面候选选择绑定：
  - `/inner-brain-candidates` 返回候选时，桌面候选下拉框展示当前候选。
  - 选择候选下拉框中的单条候选会同步候选编号。
  - 修改编号也会同步下拉框当前项。
  - “填教学”“填标注”继续只写入输入框，不自动发送、不自动训练。
- 更新 v30 方案文档和项目主方案。
- 新增桌面候选目标预填：
  - “填教学”可从常见可教学命令中预填目标命令。
  - “填标注”可从常见 `intent slot=value` 模板中预填标注目标。
  - 预填内容仍只进入输入框，用户必须检查后手动发送。
  - 教学命令列表复用 Agent 当前允许教学的命令集合，避免 UI 预填后端不接受的命令。
- 更新 v31 方案文档和项目主方案。
- 新增 InnerBrain 包含签名置信度补偿：
  - 保留原有字符 n-gram Dice 相似度。
  - 当用户长句完整包含长度不少于 4 的样本签名时，根据覆盖比例补偿置信度。
  - 不新增自然语言意图正则，不改变阈值，不改变训练样本格式。
  - “帮我看一下知识库状态”可直接命中 `knowledge.status`，避免掉到中置信澄清。
- 更新 v32 方案文档和项目主方案。
- 新增 InnerBrain 固定评估集：
  - 新增 `InnerBrainEvaluationCase`、`InnerBrainEvaluationCaseResult` 和 `InnerBrainEvaluationReport`。
  - 新增 `seed_evaluation_cases()`、`evaluate_inner_brain()` 和 `describe_inner_brain_evaluation()`。
  - `/inner-brain-eval` 输出评估集名称、通过数、失败数、准确率和逐条样例。
  - 评估不写入 runtime 样本、不自动训练、不污染最近路由候选。
- 更新 v33 方案文档和项目主方案。
- 新增 InnerBrain 本机评估集扩展：
  - `InnerBrainEvaluationCase` 增加 `source` 字段。
  - 新增 `load_evaluation_cases()` 读取 `data/inner-brain/evaluation/*.jsonl`。
  - `/inner-brain-eval` 默认合并 seed 和本机评估样本。
  - 输出增加 `seed_evaluation` 与 `local_evaluation` 来源计数。
  - 本机评估样本不会写入 runtime 训练样本，不改变聊天路由。
- 更新 v34 方案文档和项目主方案。
- 新增 InnerBrain 评估失败修复建议：
  - `InnerBrainEvaluationReport` 增加失败结果视图。
  - `/inner-brain-eval` 在失败时追加“失败修复建议”。
  - 带 `expected_command` 的失败样本会提示 `/inner-brain-teach 文本 => /命令`。
  - 只带 intent 的可执行失败样本会提示 `/inner-brain-label 文本 => intent`。
  - 建议本身不写 runtime 样本、不自动训练。
- 更新 v35 方案文档和项目主方案。

## 验证摘要

- `0.19.0` RED/GREEN：新增 3 个 Agent 测试和 1 个版本测试先失败，覆盖候选按编号教学、候选不存在、未知目标命令和版本提升；实现后目标测试通过。
- 相邻回归 `tests.test_desktop_widgets tests.test_desktop_bridge tests.test_desktop_app tests.test_agent tests.test_llm tests.test_inner_brain tests.test_conversation` 375 项通过。
- 全量 `python -m unittest discover -s tests -v` 502 项通过。
- 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.19.0.exe`，大小 `57,102,336` 字节；安装脚本和完成弹窗均显示 `0.19.0`。
- `git diff --check`、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.20.0` RED/GREEN：新增 3 个 Agent 测试和 1 个版本测试先失败，覆盖候选按编号标注、候选不存在、slot 格式错误和版本提升；实现后目标测试通过。
- `0.20.0` 相邻回归 378 项通过，全量 `python -m unittest discover -s tests -v` 505 项通过。
- `0.20.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.20.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.20.0.exe`，大小 `57,102,336` 字节；安装脚本和完成弹窗均显示 `0.20.0`。
- `0.20.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.21.0` RED/GREEN：新增 3 个 Agent 测试和 1 个版本测试先失败，覆盖重复候选优先排序、编号教学使用频次排序、编号标注使用频次排序和版本提升；实现后目标测试通过。
- `0.21.0` 相邻回归 381 项通过，全量 `python -m unittest discover -s tests -v` 508 项通过。
- `0.21.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.21.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.21.0.exe`，大小 `57,102,336` 字节；安装脚本和完成弹窗均显示 `0.21.0`。
- `0.21.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.22.0` RED/GREEN：新增 3 个 Agent 测试和 1 个版本测试先失败，覆盖候选运行态统计跨最近历史保留、编号教学使用运行态统计、教学后移除候选和版本提升；实现后目标测试通过。
- `0.22.0` 相邻回归 384 项通过，全量 `python -m unittest discover -s tests -v` 511 项通过。
- `0.22.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.22.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.22.0.exe`，大小 `57,106,432` 字节；安装脚本和完成弹窗均显示 `0.22.0`。
- `0.22.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.23.0` RED/GREEN：新增 4 个桌面快捷入口测试和 1 个版本测试先失败，覆盖快捷命令清单、直接快捷按钮、点击“内脑候选”执行 `/inner-brain-candidates` 和版本提升；实现后目标测试通过。
- `0.23.0` 相邻回归 385 项通过，全量 `python -m unittest discover -s tests -v` 512 项通过。
- `0.23.0` 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`；打包后窗口版 exe smoke 退出码为 0。
- `0.23.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.23.0.exe`，大小 `57,106,432` 字节；安装脚本和完成弹窗均显示 `0.23.0`。
- `0.23.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.24.0` RED/GREEN：新增 3 个桌面模板填充测试和 1 个版本测试先失败，覆盖候选模板控件、填教学只填输入框、填标注按编号填入模板和版本提升；实现后目标测试通过。
- `0.24.0` 相邻回归 388 项通过，全量 `python -m unittest discover -s tests -v` 515 项通过。
- `0.24.0` 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`；打包后窗口版 exe smoke 退出码为 0。
- `0.24.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.24.0.exe`，大小 `57,110,528` 字节；安装脚本和完成弹窗均显示 `0.24.0`。
- `0.24.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.25.0` RED/GREEN：新增 2 个桌面候选模板状态同步测试和 1 个版本测试先失败，覆盖空候选禁用模板、有候选同步编号上限和版本提升；实现后目标测试通过。
- `0.25.0` 相邻回归 390 项通过，全量 `python -m unittest discover -s tests -v` 517 项通过。
- `0.25.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.25.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.25.0.exe`，大小 `57,110,528` 字节；安装脚本和完成弹窗均显示 `0.25.0`。
- `0.25.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.26.0` RED/GREEN：新增 2 个桌面候选选择绑定测试和 1 个版本测试先失败，覆盖空候选禁用下拉框、有候选展示下拉选项、选择候选同步编号、模板使用所选编号和版本提升；实现后目标测试通过。
- `0.26.0` 桌面 widget 专项 37 项通过，相邻回归 355 项通过，全量 `python -m unittest discover -s tests -v` 519 项通过。
- `0.26.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.26.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.26.0.exe`，大小 `57,110,528` 字节；安装脚本和完成弹窗均显示 `0.26.0`。
- `0.26.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.27.0` RED/GREEN：新增 3 个桌面候选目标预填测试和 1 个版本测试先失败，覆盖目标下拉框选项、教学命令预填、标注 intent 模板预填、填入不提交和版本提升；实现后目标测试通过。
- `0.27.0` 桌面 widget 专项 39 项通过，相邻回归 355 项通过，全量 `python -m unittest discover -s tests -v` 521 项通过。
- `0.27.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.27.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.27.0.exe`，大小 `57,114,624` 字节；安装脚本和完成弹窗均显示 `0.27.0`。
- `0.27.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.28.0` RED/GREEN：新增 1 个 InnerBrain 样本签名包含补偿测试和 1 个版本测试先失败，覆盖“帮我看一下知识库状态”从中置信澄清提升为高置信执行，以及版本提升；实现后目标测试通过。
- `0.28.0` InnerBrain 专项 25 项通过。
- `0.29.0` RED/GREEN：新增 InnerBrain 评估 API、Agent `/inner-brain-eval` 命令和版本一致性测试先失败；实现后目标测试通过。
- `0.29.0` InnerBrain 专项 26 项通过，相邻回归 397 项通过，全量 `python -m unittest discover -s tests -v` 524 项通过。
- `0.29.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.29.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.29.0.exe`，大小 `57,114,624` 字节；安装脚本和完成弹窗均显示 `0.29.0`。
- `0.29.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.30.0` RED/GREEN：新增本机评估 JSONL 加载、Agent `/inner-brain-eval` 合并本机评估样本和版本一致性测试先失败；实现后目标测试通过。
- `0.30.0` InnerBrain 专项 27 项通过，相邻回归 399 项通过，全量 `python -m unittest discover -s tests -v` 526 项通过。
- `0.30.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.30.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.30.0.exe`，大小 `57,118,720` 字节；安装脚本和完成弹窗均显示 `0.30.0`。
- `0.30.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.31.0` RED/GREEN：新增 InnerBrain 失败评估建议、Agent `/inner-brain-eval` 失败建议和版本一致性测试先失败；实现后目标测试通过。
- `0.31.0` InnerBrain 专项 28 项通过，相邻回归 401 项通过，全量 `python -m unittest discover -s tests -v` 528 项通过。
- `0.31.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.31.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.31.0.exe`，大小 `57,118,720` 字节；安装脚本和完成弹窗均显示 `0.31.0`。
- `0.31.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。

## 下一步

- 继续把真实日志中确认稳定的表达加入 InnerBrain 固定评估集或本机 `data/inner-brain/evaluation/*.jsonl`。
- 继续评估轻量 embedding 或小型分类器方案，但不从零训练通用 LLM，且必须先经过固定评估集对比。
