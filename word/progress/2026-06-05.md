# 2026-06-05 进度

> 日期：2026-06-05
> 执行者：Codex

## 当日目标

- 回顾 `日志.txt` 和本地留痕，确认 skill YAML 报错未影响项目发布状态。
- 继续推进 Jarvis Lite 1.0 验收线里的高风险候选显式确认固化与撤销闭环。

## 完成事项

- 确认 `0.132.0` 已完成并推送到 `origin/main`，当前工作区从应用别名确认固化与撤销阶段继续。
- 启动 `0.133.0` 授权规则确认固化与撤销第一阶段。
- RED：授权规则 JSON 存储、授权规则候选确认/撤销、配置管家授权规则计数、Agent 显式确认/撤销命令、授权状态展示和版本一致性测试先失败。
- GREEN：新增 `authorization_rules.py`，`/config-candidate-confirm 编号` 支持活跃授权规则候选并写入 `config/authorization.local.json`，`/config-candidate-undo 编号` 仅删除对应授权规则并恢复候选为活跃。
- `/authorization-status` 和 `/config-manager-status` 只读展示本地授权规则数量；当前阶段不改变 `authorize_intent_execution()` 的既有执行决策。
- 启动并完成 `0.134.0` 偏好确认固化与撤销第一阶段。
- RED：偏好 JSON 存储、偏好候选确认/撤销、配置管家偏好计数、Agent 显式偏好确认/撤销命令和版本一致性测试先失败。
- GREEN：新增 `preferences.py`，`/config-candidate-confirm 编号` 支持活跃偏好候选并写入 `config/preferences.local.json`，`/config-candidate-undo 编号` 删除对应偏好并恢复候选为活跃。
- `/config-manager-status` 只读展示本地偏好数量；当前阶段保存的偏好不改变回复风格、LLM prompt、路由或执行决策。
- 启动并完成 `0.135.0` 偏好显式启用与停用第一阶段。
- RED：偏好 `enabled` 字段、按编号启用/停用 helper、Agent `/preference-status`、`/preference-enable`、`/preference-disable`、配置管家入口、LLM 白名单边界和版本一致性测试先失败。
- GREEN：偏好确认后默认未启用，`/preference-status` 展示本地偏好总数、已启用/未启用数量和编号列表，`/preference-enable 编号` 与 `/preference-disable 编号` 只切换 `preferences.local.json` 的 `enabled` 字段。
- 当前阶段启用状态只用于本地可审计管理，不改变回复风格、LLM prompt、路由或执行决策。
- 启动并完成 `0.136.0` 偏好应用预览第一阶段。
- RED：偏好应用预览 helper、Agent `/preference-preview [输入文本]`、配置管家入口、LLM 白名单边界和版本一致性测试先失败。
- GREEN：`/preference-preview [输入文本]` 只读展示当前已启用偏好和应用草案，无启用偏好时提示先显式启用；预览不会写入 LLM prompt、不会改变路由或执行决策。
- 启动并完成 `0.137.0` 偏好稳定 ID 与冲突提示第一阶段。
- RED：偏好稳定 ID、旧记录确定性 ID、按 ID 启停、状态/预览 ID 展示、已启用偏好冲突提示、Agent 编号或 ID 启停和版本一致性测试先失败。
- GREEN：新偏好写入 `id`，旧偏好缺失 `id` 时按偏好文本生成稳定 ID，`/preference-enable 编号或ID` 与 `/preference-disable 编号或ID` 可显式启停；`/preference-status` 和 `/preference-preview [输入文本]` 展示稳定 ID，已启用偏好存在明显互斥表达时只读提示人工确认；当前阶段不自动裁决冲突，不改变回复、LLM prompt、路由或执行决策。
- 启动并完成 `0.138.0` 偏好应用确认草稿第一阶段。
- RED：偏好应用确认草稿 helper、Agent `/preference-apply-draft [输入文本]`、配置管家入口、LLM 白名单边界和版本一致性测试先失败。
- GREEN：`/preference-apply-draft [输入文本]` 生成待确认偏好应用草稿，展示输入、已启用偏好稳定 ID、冲突提示和确认边界；当前阶段不真正应用偏好，不改变回复、LLM prompt、路由或执行决策，不把偏好应用草稿命令加入 LLM 白名单。
- 启动并完成 `0.139.0` 偏好应用确认命令第一阶段。
- RED：偏好应用确认 helper、Agent `/preference-apply-confirm [输入文本]`、配置管家入口、LLM 白名单边界和版本一致性测试先失败。
- GREEN：`/preference-apply-confirm [输入文本]` 只在显式命令输出中确认本次偏好应用；无启用偏好时拒绝确认，存在明显冲突时拒绝确认并提示停用冲突偏好；确认范围仅限本次命令输出，不写入 LLM prompt，不影响普通聊天、路由或执行决策。

## 验证摘要

- `0.133.0` 目标测试：27 项通过。
- 相邻回归 440 项通过；全量 `unittest` 746 项通过。
- 命令行 smoke 验证授权规则候选可确认写入、撤销删除并恢复活跃候选；源码桌面 smoke 和打包后 smoke 均输出 `desktopPetWindow`，无残留 `JarvisLite` 进程。
- 安装包构建成功，版本化产物为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.133.0.exe`，大小 `60,452,864` 字节；安装脚本、SED、`JarvisLite.version.txt` 和 `JarvisLite.exe` 版本资源均为 `0.133.0`。
- 静态检查通过：`git diff --check` 退出码 0，Markdown 本地链接 630 项通过，严格真实密钥形态扫描 23 个公开变更/新增文件无命中，本地敏感配置文件不存在，README BOM 为 `EF-BB-BF`。
- `0.134.0` 目标测试：20 项通过。
- 相邻回归 434 项通过；全量 `unittest` 750 项通过。
- 命令行 smoke 验证偏好候选可确认写入、撤销删除并恢复活跃候选；源码桌面 smoke 和打包后 smoke 均输出 `desktopPetWindow`，无残留 `JarvisLite` 进程。
- 安装包构建成功，版本化产物为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.134.0.exe`，大小 `60,461,056` 字节；安装脚本、SED、`JarvisLite.version.txt` 和 `JarvisLite.exe` 版本资源均为 `0.134.0`。
- 静态检查通过：`git diff --check` 退出码 0，Markdown 本地链接 308 项通过，严格真实密钥形态扫描 20 个公开变更/新增文件无命中，本地敏感配置文件不存在，README BOM 为 `EF-BB-BF`。
- `0.135.0` 目标测试：10 项通过。
- 相邻回归 437 项通过。
- 全量 `unittest` 753 项通过。
- 命令行 smoke 验证偏好候选确认后默认未启用，`/preference-enable 1` 写入 `enabled=true`，`/preference-disable 1` 写回 `enabled=false`，配置管家展示 `/preference-status` 入口；源码桌面 smoke 和打包后 smoke 均输出 `desktopPetWindow`，无残留 `JarvisLite` 进程。
- 安装包构建成功，版本化产物为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.135.0.exe`，大小 `60,461,056` 字节，时间戳 `2026/6/5 13:02:31`；安装脚本、SED、`JarvisLite.version.txt` 和 `JarvisLite.exe` 版本资源均为 `0.135.0`。
- 文档后元数据复验 7 项通过。
- 静态检查通过：`git diff --check` 退出码 0，Markdown 本地链接 494 项通过，严格真实密钥形态扫描 20 个公开变更/新增文件无命中，本地敏感配置文件不存在，README BOM 为 `EF-BB-BF`。
- `0.136.0` 目标测试：11 项通过。
- 相邻回归 440 项通过；全量 `unittest` 756 项通过。
- 命令行 smoke 验证无启用偏好时提示先启用，启用后 `/preference-preview 帮我总结知识库` 展示预览输入、已启用偏好和不自动改变回复/LLM/路由/执行决策边界；源码桌面 smoke 和打包后 smoke 均输出 `desktopPetWindow`，无残留 `JarvisLite` 进程。
- 安装包构建成功，版本化产物为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.136.0.exe`，大小 `60,465,152` 字节，时间戳 `2026/6/5 14:43:18`；安装脚本、SED、`JarvisLite.version.txt` 和 `JarvisLite.exe` 版本资源均为 `0.136.0`。
- 文档后元数据复验 7 项通过。
- 静态检查通过：`git diff --check` 退出码 0，Markdown 本地链接 498 项通过，严格真实密钥形态扫描 20 个公开变更/新增文件无命中，本地敏感配置文件不存在，README BOM 为 `EF-BB-BF`。
- `0.137.0` 目标测试：13 项通过。
- 相邻回归 442 项通过。
- 全量 `unittest` 758 项通过。
- 命令行 smoke 验证两个偏好按稳定 ID 启用后，`/preference-status` 与 `/preference-preview 总结这个项目` 展示 ID 和简洁/详细冲突提示，按 ID 停用可写回；源码桌面 smoke 和打包后 smoke 均输出 `desktopPetWindow`。
- 安装包构建成功，版本化产物为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.137.0.exe`，大小 `60,465,152` 字节，时间戳 `2026/6/5 16:05:57`；安装脚本、SED、`JarvisLite.version.txt` 和 `JarvisLite.exe` 版本资源均为 `0.137.0`。
- 文档后元数据复验 7 项通过。
- 静态检查通过：`git diff --check` 退出码 0，Markdown 本地链接 646 项通过，严格真实密钥形态扫描 15 个公开变更/新增文件无命中，本地敏感配置文件不存在，README BOM 为 `EF-BB-BF`。
- `0.138.0` 目标测试：429 项通过。
- 相邻回归 445 项通过。
- 全量 `unittest` 761 项通过。
- 命令行 smoke 验证偏好候选确认后，无启用偏好时 `/preference-apply-draft 帮我总结知识库` 提示先启用；启用后展示待确认偏好应用草稿、输入、偏好文本和不真正应用偏好边界；配置管家展示 `/preference-apply-draft` 入口。
- 源码桌面 smoke 输出 `desktopPetWindow`；打包后 smoke 退出码 0，stdout 包含 `desktopPetWindow`，stderr 为空，无残留 `JarvisLite` 进程。
- 安装包构建成功，版本化产物为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.138.0.exe`，大小 `60,465,152` 字节，时间戳 `2026/6/5 17:10:33`；安装脚本、SED、`JarvisLite.version.txt` 和 `JarvisLite.exe` 版本资源均为 `0.138.0`。
- 静态检查通过：`git diff --check` 退出码 0，Markdown 本地链接 650 项通过，严格真实密钥形态扫描 16 个公开变更/新增文件无命中，本地敏感配置文件不存在，README BOM 为 `EF-BB-BF`。
- `0.139.0` 相邻回归 449 项通过。
- 全量 `unittest` 765 项通过。
- 命令行 smoke 验证无启用偏好时 `/preference-apply-confirm` 拒绝确认，启用后确认本次命令输出范围，启用简洁/详细冲突偏好后拒绝确认并提示停用冲突偏好。
- 源码桌面 smoke 输出 `desktopPetWindow`；打包后 smoke 退出码 0，stdout 包含 `desktopPetWindow`，stderr 为空，无残留 `JarvisLite` 进程。
- 安装包构建成功，版本化产物为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.139.0.exe`，大小 `60,469,248` 字节，时间戳 `2026/6/5 18:09:00`；安装脚本、SED、`JarvisLite.version.txt` 和 `JarvisLite.exe` 版本资源均为 `0.139.0`。
- 静态检查通过：`git diff --check` 退出码 0，Markdown 本地链接 654 项通过，严格真实密钥形态扫描 17 个公开变更/新增文件无命中，本地敏感配置文件不存在，README BOM 为 `EF-BB-BF`。

## 下一步

- 提交 `0.139.0` 偏好应用确认命令第一阶段。
- 后续继续评估偏好确认后的可审计应用记录和更细粒度适用范围。
