# 2026-05-29 进度记录

> 日期：2026-05-29
> 执行者：Codex

## 当日目标

- 继续 v6 InnerBrain 样本分类器优先方案的下一阶段，不再依赖反复人工回复“继续”。
- 完成 InnerBrain 缺槽多轮澄清第一版，让用户下一句可以直接补齐关键槽位。
- 发布 `0.1.7` 可安装测试包后继续推进 query 补槽，并发布 `0.1.8` 可安装测试包，同步正式文档和验证记录。
- 继续扩大多槽位澄清覆盖，把编号+标签联合补槽打包为 `0.1.9` 可安装测试包。
- 继续扩大单槽位澄清覆盖，把目录别名和经验内容补槽打包为 `0.1.10` 可安装测试包。
- 收口 `0.2.0` 里程碑：InnerBrain 样本分类器优先 + LLM/Search 外脑互补 + 多轮澄清 v1 可安装闭环。
- 启动并收口 `0.3.0` 外脑 provider 配置闭环 v1，让 `qwen`/`gemini` 可作为 provider alias 复用 OpenAI-compatible adapter。
- 启动 `0.4.0` 运行态配置初始化 v1，让安装后的外脑与联网搜索配置草稿可直接由 Agent 生成。
- 启动 `0.5.0` 本地配置检查 v1，让用户填完外脑或联网搜索配置后可先做只读检查。
- 启动 `0.6.0` 本地配置写入 v1，让用户可通过显式 `key=value` 命令写入外脑和联网搜索配置。
- 启动 `0.7.0` 连通性诊断 v1，让写入配置后可直接测试外脑和联网搜索 provider。
- 启动 `0.8.0` 桌面配置面板 v1，让安装版用户可在面板里写入、检查和测试外脑/联网搜索配置。
- 启动 `0.14.0` 最近路由决策状态 v1，让桌面面板明确展示最近输入由命令、本地内脑、知识库、LLM fallback 还是记忆兜底处理。

## 完成事项

- 实现 InnerBrain pending 澄清状态：
  - `JarvisAgent` 在 `InnerBrainPolicy.CLARIFY` 时保存本次 `InnerBrainResult`。
  - 下一句非命令输入会先尝试补齐 pending 缺失槽位，再决定是否继续执行或继续澄清。
  - 当前 pending 状态仅保存在内存中，不跨应用重启。
- 实现第一批补槽类型：
  - `knowledge.import` 缺少 `source` 时，用户可下一句直接回复文件或目录路径，Agent 继续执行 `/import`。
  - `desktop.delete_shortcut` 缺少 `items` 时，用户可下一句直接回复快捷方式名称，Agent 继续执行桌面 `.lnk` 删除。
  - 支持“取消”“取消补充”“不用了”“先不用”“算了”取消当前澄清状态。
- 更新 v6 方案文档：
  - 记录多轮澄清状态第一版的职责、边界和当前覆盖范围。
  - 明确样本分类器仍负责自然语言主识别，解析函数只在已知 intent 下抽取路径、名称、编号和标签等结构化槽位。
- 准备 `0.1.7` 可安装测试包：
  - 项目版本提升到 `0.1.7`。
  - README 安装说明更新为 `0.1.7`，并说明本轮包含多轮澄清补槽闭环。
  - 生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`，并复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.1.7.exe`。
- 扩展联网搜索 query 补槽：
  - `web.search missing=query` 后，用户下一句直接回复关键词即可继续执行 `/search`。
  - `web.search_summarize missing=query` 后，用户下一句直接回复关键词即可继续执行 `/search-summary`，保持 SearchRouter 先拿来源、LLMRouter 再基于来源总结。
  - query 澄清提示显示为“查询关键词”，并给出 `/search 关键词` 或 `/search-summary 关键词`。
- 准备 `0.1.8` 可安装测试包：
  - 项目版本提升到 `0.1.8`。
  - README 安装说明更新为 `0.1.8`，并说明本轮包含联网搜索 query 补槽。
  - 生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`，并复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.1.8.exe`。
- 扩展编号+标签联合补槽：
  - `document.tag_numbered_recent missing=result_index,tags` 后，用户下一句可直接回复“第二份 项目 Python”。
  - `result_index` 澄清展示为“编号”，`tags` 展示为“标签”，提示文案改为一句式补充示例。
  - 补槽解析会从标签文本中移除开头编号词，避免把“第二份”写成资料标签。
- 准备 `0.1.9` 可安装测试包：
  - 项目版本提升到 `0.1.9`。
  - README 安装说明更新为 `0.1.9`，并说明本轮包含编号+标签联合补槽。
  - 生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`，并复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.1.9.exe`。
- 扩展目录别名和经验内容补槽：
  - `directory.open_alias missing=alias` 后，用户下一句可回复“目录是项目”，Agent 清理为 `alias=项目` 并继续记录打开目录请求。
  - `experience.record missing=experience` 后，用户下一句可回复“经验是导入资料后先打标签”，Agent 清理为经验内容并继续写入经验记忆。
  - 澄清提示把 `alias` 展示为“目录别名”，把 `experience` 展示为“经验内容”，并提供一句式补充示例。
- 准备 `0.1.10` 可安装测试包：
  - 项目版本提升到 `0.1.10`。
  - README 安装说明更新为 `0.1.10`，并说明本轮包含目录别名和经验内容补槽。
  - 生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`，并复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.1.10.exe`。
- 收口多轮澄清 v1 并准备 `0.2.0`：
  - `document.read_path missing=path` 后，用户可下一句回复“文件是 note.txt”，提示展示为“文件路径”并继续执行 `/read`。
  - `document.read_numbered_recent missing=result_index` 后，用户可回复“第二份”，提示示例不再使用搜索结果语境。
  - `document.tag_recent missing=tags` 后，用户可回复“标签是项目 Python”，继续给当前资料打标签。
  - `tag_group.preview_tagging missing=alias,tags` 后，用户可回复“项目 归档”，第一个词作为标签组，其余词作为新标签，避免把“项目”重复写入新增标签。
  - `experience.search` 和 `experience.advice` 缺少 `query` 时，提示展示为“经验关键词”，用户可回复“关键词是导入资料”继续执行经验搜索或经验建议。
  - 项目版本提升到 `0.2.0`，README 安装说明同步本轮覆盖范围。
- 收口外脑 provider 配置闭环 v1 并准备 `0.3.0`：
  - `LLMSettings` 新增 `adapter_provider`，`qwen`/`gemini` 保留为原始 provider，同时映射到 `openai-compatible` adapter。
  - `build_llm_router()` 对 alias 复用现有 `OpenAIResponsesProvider`，不新增厂商 SDK 或并行执行层。
  - `/llm-status` 与 `/llm-enable` 会同时显示 `Provider` 和 `Adapter`，便于确认 alias 已生效。
  - `/llm-config-example qwen` 和 `/llm-config-example gemini` 直接输出对应 alias 的 PowerShell 模板。
  - 项目版本提升到 `0.3.0`，README 和 v7 方案文档同步本轮范围。
- 收口运行态配置初始化 v1 并准备 `0.4.0`：
  - 新增 `/llm-config-init [provider]`，默认 `openai-compatible`，支持 `qwen`/`gemini` 等现有 provider，生成 `config/llm.local.json` 草稿。
  - 新增 `/search-config-init [provider]`，默认 `tavily`，生成 `config/search.local.json` 草稿。
  - 草稿中的 API key、model 和 base_url 默认空值，避免误判为已配置或误触发真实网络调用。
  - 已存在本地配置时不覆盖，也不显示已有 API key。
  - InnerBrain seed 样本新增“生成外脑配置”“创建外脑配置文件”“生成联网搜索配置”“创建搜索配置文件”，自然语言入口仍由 `JarvisAgent` 执行。
  - 项目版本提升到 `0.4.0`，README 和 v8 方案文档同步本轮范围。
- 收口本地配置检查 v1 并准备 `0.5.0`：
  - 新增 `/llm-config-check`，重新读取 `config/llm.local.json` 和环境变量覆盖，展示 Provider、Adapter、Model、Base URL、SDK Base URL、API key 状态和配置问题。
  - 新增 `/search-config-check`，重新读取 `config/search.local.json` 和环境变量覆盖，展示 Provider、Base URL、Max results、API key 状态和配置问题。
  - 检查命令只读配置，不创建、不覆盖、不修改本地配置，也不发起真实网络调用。
  - InnerBrain seed 样本新增“检查外脑配置”“看看外脑配置有没有问题”“检查联网搜索配置”“看看搜索配置有没有问题”。
  - 修复 LLM 本地配置 JSON 错误在 `provider=off` 状态下被吞掉的问题。
  - 项目版本提升到 `0.5.0`，README、PROJECT-PLAN 和 v9 方案文档同步本轮范围。
- 收口本地配置写入 v1 并准备 `0.6.0`：
  - 新增 `/llm-config-set key=value ...`，可创建或更新 `config/llm.local.json`，支持 `provider`、`model`、`base_url`、`api_key` 和 `fake_response`。
  - 新增 `/search-config-set key=value ...`，可创建或更新 `config/search.local.json`，支持 `provider`、`api_key`、`base_url`、`max_results` 和 `fake_results`。
  - 写入命令保留未指定字段，拒绝未知字段、未知 provider、非法 `max_results` 和损坏 JSON，不产生部分坏配置。
  - 响应和日志只显示变更字段，不显示真实 API key。
  - InnerBrain seed 样本新增“设置外脑配置”“修改外脑配置”“设置联网搜索配置”“修改搜索配置”，自然语言入口只给命令用法，不猜测敏感值。
  - 项目版本提升到 `0.6.0`，README、PROJECT-PLAN 和 v10 方案文档同步本轮范围。
- 收口连通性诊断 v1 并准备 `0.7.0`：
  - `/llm-smoke [prompt]` 在非注入 router 场景下每次执行前重新读取当前 `config/llm.local.json`。
  - 新增 `/search-smoke [query]`，默认查询 `Python 版本`，重新读取当前 `config/search.local.json` 后执行一次 provider 连通性测试。
  - `/search-smoke` 成功时展示返回来源条数和预览，失败时展示可读错误；它不写入最近联网搜索上下文。
  - InnerBrain seed 样本新增“测试外脑连接”“检查外脑连接”“测试联网搜索连接”“检查搜索连接”。
  - LLM 外脑命令白名单新增 `/llm-smoke [prompt]` 和 `/search-smoke [query]`，仍由 `JarvisAgent` 校验和执行。
  - 项目版本提升到 `0.7.0`，README、PROJECT-PLAN 和 v11 方案文档同步本轮范围。
- 收口桌面配置面板 v1 并准备 `0.8.0`：
  - `AssistantPanel` 新增外脑配置区，可填写 provider、model、base_url 和 api_key，并点击写入、检查、测试。
  - `AssistantPanel` 新增联网搜索配置区，可填写 provider、api_key、base_url 和 max_results，并点击写入、检查、测试。
  - `DesktopBridge.send_sensitive()` 执行真实配置命令，但只把脱敏显示文本写入 transcript 和会话历史。
  - 写入动作复用 `/llm-config-set` 和 `/search-config-set`，检查和 smoke 动作复用已有命令。
  - 项目版本提升到 `0.8.0`，README、PROJECT-PLAN 和 v12 方案文档同步本轮范围。
- 收口 LLM 外脑多轮澄清 v1 并准备 `0.9.0`：
  - LLM 返回 `clarify` 时，`JarvisAgent` 保存原始问题、澄清问题和当时上下文。
  - 用户下一句直接补充时，Agent 将“原始问题 + 澄清问题 + 用户补充”再次交给 LLM。
  - 澄清补齐后的 LLM `answer` 会直接输出，白名单 `command` 仍由 `JarvisAgent` 校验并执行。
  - 用户回复“取消补充”等取消短语时清空外脑待澄清状态，不再次调用 provider。
  - 项目版本提升到 `0.9.0`，README、PROJECT-PLAN 和 v13 方案文档同步本轮范围。
- 收口 LLM 外脑澄清状态持久化 v1 并准备 `0.10.0`：
  - `RuntimeContext` 新增 LLM pending 澄清结构，保存原始问题、澄清问题和上下文。
  - 新 Agent 实例会恢复待补充外脑问题，用户下一句仍可继续补齐。
  - 新增 `/recent-context` 显式查看入口，最近上下文会展示待补充外脑问题、原始问题和取消提示。
  - 取消或补齐外脑澄清后会清空运行态 pending。
  - 项目版本提升到 `0.10.0`，README、PROJECT-PLAN 和 v14 方案文档同步本轮范围。
- 收口 LLM 外脑澄清轮数与过期策略 v1 并准备 `0.11.0`：
  - LLM pending 结构新增 `created_at` 和 `clarification_count`。
  - 连续 LLM `clarify` 会保留最初原始问题并递增澄清轮次。
  - 超过 3 轮连续澄清后结束 pending，并提示用户重新完整描述需求。
  - 超过 12 小时未补充的 pending 会在 Agent 启动时清理。
  - 最近上下文展示澄清轮次和过期策略。
  - 项目版本提升到 `0.11.0`，README、PROJECT-PLAN 和 v15 方案文档同步本轮范围。
- 收口桌面外脑待补充状态 v1 并准备 `0.12.0`：
  - `JarvisAgent` 新增只读 LLM 澄清状态文本。
  - `DesktopBridge` 每次发送后附带最新外脑 pending 状态。
  - `AssistantPanel` 新增固定外脑状态 label，启动时和每次响应后刷新。
  - 桌面面板显示待补充问题、轮次、原始问题和取消提示；无 pending 时显示“外脑待补充：无”。
  - 项目版本提升到 `0.12.0`，README、PROJECT-PLAN 和 v16 方案文档同步本轮范围。
- 收口桌面外脑运行状态 v1 并准备 `0.13.0`：
  - `RuntimeContext` 新增最近 LLM 调用快照，保存触发来源、输入摘要、返回类型和结果摘要。
  - `JarvisAgent` 新增只读 LLM 运行状态文本，展示外脑启用状态、provider、model 和最近调用。
  - LLM fallback、smoke、搜索总结/比较和澄清补充路径都会记录最近调用。
  - `DesktopBridge` 每次发送后附带最新外脑运行状态。
  - `AssistantPanel` 新增固定外脑运行状态 label，启动时和每次响应后刷新。
  - 项目版本提升到 `0.13.0`，README、PROJECT-PLAN 和 v17 方案文档同步本轮范围。
- 收口最近路由决策状态 v1 并准备 `0.14.0`：
  - `RuntimeContext` 新增最近路由决策快照，保存 route、detail、输入摘要、结果摘要和时间。
  - `JarvisAgent` 在 `command`、`inner-brain`、`inner-brain-clarify`、`knowledge`、`llm-fallback` 和 `memory-fallback` 路径记录最近路由。
  - `JarvisAgent.route_status_text()` 暴露只读状态，桌面可直接看到“最近路由：inner-brain / assistant.greeting”这类说明。
  - `DesktopBridge` 每次发送后附带最新路由状态。
  - `AssistantPanel` 新增固定路由状态 label，启动时和每次响应后刷新。
  - 项目版本提升到 `0.14.0`，README、PROJECT-PLAN 和 v18 方案文档同步本轮范围。

## 验证摘要

- InnerBrain 多轮澄清 RED/GREEN：新增 3 个 Agent 测试先失败，覆盖导入路径补槽、桌面快捷方式名称补槽和取消补充；实现 pending 澄清状态后目标测试通过。
- 专项回归：澄清提示、补槽执行和取消补充 4 个测试通过；`tests.test_inner_brain tests.test_agent` 共 229 项通过。
- 版本打包 RED/GREEN：版本一致性测试先因 `pyproject.toml` 仍为 `0.1.6` 失败；同步项目元数据、README 和测试目标到 `0.1.7` 后通过。
- 全量验证：全量 `unittest` 423 项通过；源码桌面 smoke 和打包后 `JarvisLite.exe --smoke` 均通过。
- 安装器验证：`install.cmd` 包含 `DisplayVersion /d "0.1.7"`、覆盖安装提示和用户数据保留提示；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.1.7 installation finished`。
- query 补槽 RED/GREEN：新增 2 个 Agent 测试先失败，澄清文案只显示原始 `query` 且补齐后不能生成 `/search` 或 `/search-summary`；改为优先使用 `slots.query` 后目标测试通过。
- query 补槽回归：`tests.test_inner_brain tests.test_agent` 共 231 项通过。
- `0.1.8` 打包 RED/GREEN：版本一致性测试先因 `pyproject.toml` 仍为 `0.1.7` 失败；同步项目元数据、README 和测试目标到 `0.1.8` 后通过。
- `0.1.8` 安装器验证：打包后 `JarvisLite.exe --smoke` 通过；`install.cmd` 包含 `DisplayVersion /d "0.1.8"`、覆盖安装提示和用户数据保留提示；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.1.8 installation finished`。
- `0.1.8` 收尾验证：全量 `unittest` 425 项通过，源码桌面 smoke 和打包后 `JarvisLite.exe --smoke` 均通过。
- 编号+标签联合补槽 RED/GREEN：新增 1 个 Agent 测试先失败，澄清文案只显示原始 `result_index、tags` 且编号词会污染标签；改为中文缺槽名、一句式提示和已知 intent 下的标签文本清理后目标测试通过。
- 编号+标签联合补槽回归：`tests.test_inner_brain tests.test_agent` 共 232 项通过。
- `0.1.9` 版本 RED/GREEN：版本一致性测试先因 `pyproject.toml` 仍为 `0.1.8` 失败；同步项目元数据、README 和测试目标到 `0.1.9` 后通过。
- `0.1.9` 收尾验证：全量 `unittest` 426 项通过，源码桌面 smoke 通过，打包后 `JarvisLite.exe --smoke` 退出码 0。
- `0.1.9` 安装器验证：`install.cmd` 包含 `DisplayVersion /d "0.1.9"`、覆盖安装提示和用户数据保留提示；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.1.9 installation finished`；安装包生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.1.9.exe`。
- 目录别名和经验内容补槽 RED/GREEN：新增 2 个 Agent 测试先失败，澄清文案只显示原始 `alias`/`experience` 且不能清理“目录是”“经验是”前缀；补齐中文缺槽名、提示示例和补槽归一化后目标测试通过。
- 目录别名和经验内容补槽回归：`tests.test_inner_brain tests.test_agent` 共 234 项通过。
- `0.1.10` 版本 RED/GREEN：版本一致性测试先因 `pyproject.toml` 仍为 `0.1.9` 失败；同步项目元数据、README 和测试目标到 `0.1.10` 后通过。
- `0.1.10` 收尾验证：全量 `unittest` 428 项通过，源码桌面 smoke 通过，打包后 `JarvisLite.exe --smoke` 退出码 0。
- `0.1.10` 安装器验证：`install.cmd` 包含 `DisplayVersion /d "0.1.10"`、覆盖安装提示和用户数据保留提示；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.1.10 installation finished`；安装包生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.1.10.exe`。
- `0.2.0` 多轮澄清 v1 RED/GREEN：新增 6 个 Agent 测试先失败，覆盖文件路径、编号资料、当前资料标签、标签组+新标签、经验搜索关键词和经验建议关键词；补齐提示文案和已知 intent 下的标签组补槽拆分后目标测试通过。
- `0.2.0` 专项回归：`tests.test_inner_brain tests.test_agent` 共 240 项通过。
- `0.2.0` 版本 RED/GREEN：版本一致性测试先因 `pyproject.toml` 仍为 `0.1.10` 失败；同步项目元数据、README 和测试目标到 `0.2.0` 后通过。
- `0.2.0` 收尾验证：全量 `unittest` 434 项通过，源码桌面 smoke 通过，打包后 `JarvisLite.exe --smoke` 退出码 0。
- `0.2.0` 安装器验证：`install.cmd` 包含 `DisplayVersion /d "0.2.0"`、覆盖安装提示和用户数据保留提示；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.2.0 installation finished`；安装包生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.2.0.exe`。
- `0.3.0` provider alias RED/GREEN：新增 5 个 LLM/Agent 测试先失败，覆盖 qwen/gemini alias 构建、缺 base_url 诊断、配置模板和 `/llm-enable` 状态展示；实现 alias -> OpenAI-compatible adapter 后通过。
- `0.3.0` 专项回归：`tests.test_llm tests.test_agent` 共 251 项通过。
- `0.3.0` 版本 RED/GREEN：版本一致性测试先因 `pyproject.toml` 仍为 `0.2.0` 失败；同步项目元数据、README 和测试目标到 `0.3.0` 后通过。
- `0.3.0` 收尾验证：全量 `unittest` 438 项通过，源码桌面 smoke 通过，打包后 `JarvisLite.exe --smoke` 退出码 0。
- `0.3.0` 安装器验证：`install.cmd` 包含 `DisplayVersion /d "0.3.0"`、覆盖安装提示和用户数据保留提示；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.3.0 installation finished`；安装包生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.3.0.exe`。
- `0.4.0` 配置初始化 RED/GREEN：新增 4 个 Agent 测试先失败，覆盖 LLM qwen 草稿创建、已有配置不覆盖且不泄漏 key、搜索 tavily 草稿创建和自然语言配置初始化入口；实现后通过。
- `0.4.0` 版本 RED/GREEN：版本一致性测试先因 `pyproject.toml` 仍为 `0.3.0` 失败；同步项目元数据、README 和测试目标到 `0.4.0` 后通过。
- `0.4.0` 相邻回归：`tests.test_agent tests.test_inner_brain tests.test_llm tests.test_search` 共 289 项通过。
- `0.4.0` 收尾验证：全量 `unittest` 442 项通过，源码桌面 smoke 通过，打包后 `JarvisLite.exe --smoke` 退出码 0。
- `0.4.0` 安装器验证：`install.cmd` 包含 `DisplayVersion /d "0.4.0"`、覆盖安装提示和用户数据保留提示；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.4.0 installation finished`；安装包生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.4.0.exe`。
- `0.5.0` 配置检查 RED/GREEN：新增 4 个 Agent 测试先失败，覆盖 LLM/Search 只读检查、无 API key 泄漏、LLM 无效 JSON 报告和自然语言配置检查入口；实现后通过。
- `0.5.0` 版本 RED/GREEN：版本一致性测试先因 `pyproject.toml` 仍为 `0.4.0` 失败；同步项目元数据到 `0.5.0` 后通过。
- `0.5.0` 相邻回归：`tests.test_agent tests.test_inner_brain tests.test_llm tests.test_search` 首次因测试清单仍用 `0.4.1` 作为新版本失败，改为 `0.5.1` 后 293 项通过。
- `0.5.0` 收尾验证：全量 `unittest` 446 项通过，源码桌面 smoke 通过，`/llm-config-check` 与 `/search-config-check` CLI smoke 通过，打包后 `JarvisLite.exe --smoke` 退出码 0。
- `0.5.0` 安装器验证：`install.cmd` 包含 `DisplayVersion /d "0.5.0"`、覆盖安装提示和用户数据保留提示；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.5.0 installation finished`；安装包生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.5.0.exe`。
- `0.6.0` 配置写入 RED/GREEN：新增 6 个 Agent 测试先失败，覆盖 LLM/Search 配置写入、保留未指定字段、错误不部分写入、自然语言配置写入用法和 API key 不泄漏；实现后通过。
- `0.6.0` 版本 RED/GREEN：版本一致性测试先因 `pyproject.toml` 仍为 `0.5.0` 失败；同步项目元数据到 `0.6.0` 后通过。
- `0.6.0` 相邻回归：`tests.test_agent tests.test_inner_brain tests.test_llm tests.test_search` 共 299 项通过。
- `0.6.0` 收尾验证：全量 `unittest` 452 项通过，源码桌面 smoke 通过，`/llm-config-set`、`/llm-config-check`、`/search-config-set` 和 `/search-config-check` CLI smoke 通过，打包后 `JarvisLite.exe --smoke` 退出码 0。
- `0.6.0` 安装器验证：`install.cmd` 包含 `DisplayVersion /d "0.6.0"`、覆盖安装提示和用户数据保留提示；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.6.0 installation finished`；安装包生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.6.0.exe`，大小 57,081,856 bytes。
- `0.7.0` 连通性诊断 RED/GREEN：新增 5 个 Agent 测试和 1 个版本测试先失败，覆盖 `/llm-smoke` 运行时配置热重载、`/search-smoke` 新命令、最近联网搜索上下文不污染、自然语言 smoke 入口和版本提升；实现后通过。
- `0.7.0` 相邻回归：`tests.test_agent tests.test_inner_brain tests.test_llm tests.test_search` 共 304 项通过。
- `0.7.0` 收尾验证：全量 `unittest` 457 项通过，源码桌面 smoke 通过，`/llm-smoke` 和 `/search-smoke` CLI smoke 通过，打包后 `JarvisLite.exe --smoke` 退出码 0。
- `0.7.0` 安装器验证：`install.cmd` 包含 `DisplayVersion /d "0.7.0"`、覆盖安装提示和用户数据保留提示；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.7.0 installation finished`；安装包生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.7.0.exe`，大小 57,081,856 bytes。
- `0.8.0` 桌面配置面板 RED/GREEN：新增 5 个桌面测试和 1 个版本测试先失败，覆盖敏感提交脱敏、LLM/Search 配置控件、桌面写入配置不泄漏 key、检查和 smoke 按钮复用既有命令、版本提升；实现后通过。
- `0.8.0` 相邻回归：`tests.test_desktop_widgets tests.test_desktop_bridge tests.test_desktop_app tests.test_agent tests.test_llm tests.test_search` 共 316 项通过。
- `0.8.0` 收尾验证：全量 `unittest` 462 项通过，源码桌面 smoke 通过，打包后 `JarvisLite.exe --smoke` 退出码 0。
- `0.8.0` 安装器验证：`install.cmd` 包含 `DisplayVersion /d "0.8.0"`、覆盖安装提示和用户数据保留提示；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.8.0 installation finished`；安装包生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.8.0.exe`，大小 57,085,952 bytes。
- `0.9.0` LLM 外脑多轮澄清 RED/GREEN：新增 3 个 Agent 测试和 1 个版本测试先失败，覆盖外脑澄清后补充执行最终命令、补充后返回最终 answer、取消补充不二次调用 provider、版本提升；实现后目标测试通过。
- `0.9.0` 相邻回归：`tests.test_agent tests.test_llm tests.test_inner_brain tests.test_conversation tests.test_desktop_bridge tests.test_desktop_widgets` 共 332 项通过。
- `0.9.0` 收尾验证：全量 `unittest` 465 项通过，源码桌面 smoke 通过，打包后 `JarvisLite.exe --smoke` 退出码 0。
- `0.9.0` 安装器验证：`install.cmd` 包含 `DisplayVersion /d "0.9.0"`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.9.0 installation finished`；安装包生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.9.0.exe`，大小 57,090,048 bytes。
- `0.10.0` LLM 外脑澄清状态持久化 RED/GREEN：新增 3 个 Agent 测试和 1 个版本测试先失败，覆盖外脑待澄清状态跨实例恢复、最近上下文展示 pending、不消耗 pending、取消后清理运行态状态和版本提升；实现后目标测试通过。
- `0.10.0` 相邻回归：`tests.test_agent tests.test_llm tests.test_inner_brain tests.test_conversation tests.test_desktop_bridge tests.test_desktop_widgets` 共 335 项通过。
- `0.10.0` 收尾验证：全量 `unittest` 468 项通过，源码桌面 smoke 通过，打包后 `JarvisLite.exe --smoke` 命令退出码 0。
- `0.10.0` 安装器验证：`install.cmd` 包含 `DisplayVersion /d "0.10.0"` 和 `Jarvis Lite 0.10.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.10.0 installation finished`；安装包生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.10.0.exe`，大小 57,085,952 bytes。
- `0.11.0` LLM 外脑澄清轮数与过期策略 RED/GREEN：新增 3 个 Agent 测试和 1 个版本测试先失败，覆盖连续澄清保留原始问题并递增轮次、超过最大轮数后清空 pending、过期 runtime pending 启动后清理和版本提升；实现后目标测试通过。
- `0.11.0` 相邻回归：`tests.test_agent tests.test_llm tests.test_inner_brain tests.test_conversation tests.test_desktop_bridge tests.test_desktop_widgets` 共 338 项通过。
- `0.11.0` 收尾验证：全量 `unittest` 471 项通过，源码桌面 smoke 通过，打包后 `JarvisLite.exe --smoke` 退出码 0。
- `0.11.0` 安装器验证：`install.cmd` 包含 `DisplayVersion /d "0.11.0"` 和 `Jarvis Lite 0.11.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.11.0 installation finished`；安装包生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.11.0.exe`，大小 57,090,048 bytes。
- `0.12.0` 桌面外脑待补充状态 RED/GREEN：新增 1 个 Bridge 测试、3 个 Panel 测试和 1 个版本测试先失败，覆盖外脑 clarify 后状态快照、面板固定显示、取消刷新、重启恢复和版本提升；实现后目标测试通过。
- `0.12.0` 相邻回归：`tests.test_desktop_widgets tests.test_desktop_bridge tests.test_desktop_app tests.test_agent tests.test_llm tests.test_inner_brain tests.test_conversation` 共 347 项通过。
- `0.12.0` 收尾验证：全量 `unittest` 474 项通过，源码桌面 smoke 通过，打包后 `JarvisLite.exe --smoke` 退出码 0。
- `0.12.0` 安装器验证：`install.cmd` 包含 `DisplayVersion /d "0.12.0"` 和 `Jarvis Lite 0.12.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.12.0 installation finished`；安装包生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.12.0.exe`，大小 57,094,144 bytes。
- `0.13.0` 桌面外脑运行状态 RED/GREEN：新增 2 个 Agent 测试、1 个 Bridge 测试、2 个 Panel 测试和 1 个版本测试先失败，覆盖最近外脑调用快照、跨启动恢复、桌面响应透传、面板固定展示和版本提升；实现后目标测试通过。
- `0.13.0` 相邻回归：`tests.test_desktop_widgets tests.test_desktop_bridge tests.test_desktop_app tests.test_agent tests.test_llm tests.test_inner_brain tests.test_conversation` 共 351 项通过。
- `0.13.0` 收尾验证：全量 `unittest` 478 项通过，源码桌面 smoke 通过，打包后 `JarvisLite.exe --smoke` 退出码 0。
- `0.13.0` 安装器验证：`install.cmd` 包含 `DisplayVersion /d "0.13.0"` 和 `Jarvis Lite 0.13.0 installed`；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.13.0 installation finished`；安装包生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.13.0.exe`，大小 57,094,144 bytes。
- `0.14.0` 最近路由决策状态 RED/GREEN：新增 3 个 Agent 测试、1 个 Bridge 测试、2 个 Panel 测试和 1 个版本测试先失败，覆盖本地内脑问候不调用外脑、LLM fallback 路由、身份记忆路由、桌面响应透传、面板固定展示和版本提升；实现后目标测试通过。
- `0.14.0` 相邻回归：`tests.test_desktop_widgets tests.test_desktop_bridge tests.test_desktop_app tests.test_agent tests.test_llm tests.test_inner_brain tests.test_conversation` 共 356 项通过。
- 静态验证：`git diff --check` 退出 0（仅 LF/CRLF 工作区换行提示），Markdown 本地链接检查通过，敏感信息扫描通过，本地 LLM/Search 配置未被 Git 跟踪且工作区本地配置文件已清理。
- 完整验证见 [../../verification/2026-05/2026-05-29.md](../../verification/2026-05/2026-05-29.md)。

## 下一步

- 继续扩大多轮澄清覆盖的缺失槽位类型，例如更多组合槽位、路径+标签、最近文件导入来源和目录整理别名。
- 继续基于真实使用日志缩小 `legacy_fallback` 范围。
- 评估更稳定的轻量分类器方案，例如字符 n-gram、embedding 相似度或小型分类器。
- 根据真实外脑配置反馈决定是否需要 Gemini/Qwen 原生 adapter；若兼容端点足够，优先继续打磨提示词、上下文、配置面板、检查和 smoke 诊断。
