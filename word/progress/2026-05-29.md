# 2026-05-29 进度记录

> 日期：2026-05-29
> 执行者：Codex

## 当日目标

- 继续 v6 InnerBrain 样本分类器优先方案的下一阶段，不再依赖反复人工回复“继续”。
- 完成 InnerBrain 缺槽多轮澄清第一版，让用户下一句可以直接补齐关键槽位。
- 发布 `0.1.7` 可安装测试包后继续推进 query 补槽，并发布 `0.1.8` 可安装测试包，同步正式文档和验证记录。
- 继续扩大多槽位澄清覆盖，把编号+标签联合补槽打包为 `0.1.9` 可安装测试包。
- 继续扩大单槽位澄清覆盖，把目录别名和经验内容补槽打包为 `0.1.10` 可安装测试包。
- 收口 `0.2.0` 里程碑：InnerBrain 样本分类器优先 + LLM/Search 外脑互补 + 多轮澄清 v1 可安装闭环。
- 启动并收口 `0.3.0` 外脑 provider 配置闭环 v1，让 `qwen`/`gemini` 可作为 provider alias 复用 OpenAI-compatible adapter。

## 完成事项

- 实现 InnerBrain pending 澄清状态：
  - `JarvisAgent` 在 `InnerBrainPolicy.CLARIFY` 时保存本次 `InnerBrainResult`。
  - 下一句非命令输入会先尝试补齐 pending 缺失槽位，再决定是否继续执行或继续澄清。
  - 当前 pending 状态仅保存在内存中，不跨应用重启。
- 实现第一批补槽类型：
  - `knowledge.import` 缺少 `source` 时，用户可下一句直接回复文件或目录路径，Agent 继续执行 `/import`。
  - `desktop.delete_shortcut` 缺少 `items` 时，用户可下一句直接回复快捷方式名称，Agent 继续执行桌面 `.lnk` 删除。
  - 支持“取消”“取消补充”“不用了”“先不用”“算了”取消当前澄清状态。
- 更新 v6 方案文档：
  - 记录多轮澄清状态第一版的职责、边界和当前覆盖范围。
  - 明确样本分类器仍负责自然语言主识别，解析函数只在已知 intent 下抽取路径、名称、编号和标签等结构化槽位。
- 准备 `0.1.7` 可安装测试包：
  - 项目版本提升到 `0.1.7`。
  - README 安装说明更新为 `0.1.7`，并说明本轮包含多轮澄清补槽闭环。
  - 生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`，并复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.1.7.exe`。
- 扩展联网搜索 query 补槽：
  - `web.search missing=query` 后，用户下一句直接回复关键词即可继续执行 `/search`。
  - `web.search_summarize missing=query` 后，用户下一句直接回复关键词即可继续执行 `/search-summary`，保持 SearchRouter 先拿来源、LLMRouter 再基于来源总结。
  - query 澄清提示显示为“查询关键词”，并给出 `/search 关键词` 或 `/search-summary 关键词`。
- 准备 `0.1.8` 可安装测试包：
  - 项目版本提升到 `0.1.8`。
  - README 安装说明更新为 `0.1.8`，并说明本轮包含联网搜索 query 补槽。
  - 生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`，并复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.1.8.exe`。
- 扩展编号+标签联合补槽：
  - `document.tag_numbered_recent missing=result_index,tags` 后，用户下一句可直接回复“第二份 项目 Python”。
  - `result_index` 澄清展示为“编号”，`tags` 展示为“标签”，提示文案改为一句式补充示例。
  - 补槽解析会从标签文本中移除开头编号词，避免把“第二份”写成资料标签。
- 准备 `0.1.9` 可安装测试包：
  - 项目版本提升到 `0.1.9`。
  - README 安装说明更新为 `0.1.9`，并说明本轮包含编号+标签联合补槽。
  - 生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`，并复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.1.9.exe`。
- 扩展目录别名和经验内容补槽：
  - `directory.open_alias missing=alias` 后，用户下一句可回复“目录是项目”，Agent 清理为 `alias=项目` 并继续记录打开目录请求。
  - `experience.record missing=experience` 后，用户下一句可回复“经验是导入资料后先打标签”，Agent 清理为经验内容并继续写入经验记忆。
  - 澄清提示把 `alias` 展示为“目录别名”，把 `experience` 展示为“经验内容”，并提供一句式补充示例。
- 准备 `0.1.10` 可安装测试包：
  - 项目版本提升到 `0.1.10`。
  - README 安装说明更新为 `0.1.10`，并说明本轮包含目录别名和经验内容补槽。
  - 生成 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup.exe`，并复制为 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.1.10.exe`。
- 收口多轮澄清 v1 并准备 `0.2.0`：
  - `document.read_path missing=path` 后，用户可下一句回复“文件是 note.txt”，提示展示为“文件路径”并继续执行 `/read`。
  - `document.read_numbered_recent missing=result_index` 后，用户可回复“第二份”，提示示例不再使用搜索结果语境。
  - `document.tag_recent missing=tags` 后，用户可回复“标签是项目 Python”，继续给当前资料打标签。
  - `tag_group.preview_tagging missing=alias,tags` 后，用户可回复“项目 归档”，第一个词作为标签组，其余词作为新标签，避免把“项目”重复写入新增标签。
  - `experience.search` 和 `experience.advice` 缺少 `query` 时，提示展示为“经验关键词”，用户可回复“关键词是导入资料”继续执行经验搜索或经验建议。
  - 项目版本提升到 `0.2.0`，README 安装说明同步本轮覆盖范围。
- 收口外脑 provider 配置闭环 v1 并准备 `0.3.0`：
  - `LLMSettings` 新增 `adapter_provider`，`qwen`/`gemini` 保留为原始 provider，同时映射到 `openai-compatible` adapter。
  - `build_llm_router()` 对 alias 复用现有 `OpenAIResponsesProvider`，不新增厂商 SDK 或并行执行层。
  - `/llm-status` 与 `/llm-enable` 会同时显示 `Provider` 和 `Adapter`，便于确认 alias 已生效。
  - `/llm-config-example qwen` 和 `/llm-config-example gemini` 直接输出对应 alias 的 PowerShell 模板。
  - 项目版本提升到 `0.3.0`，README 和 v7 方案文档同步本轮范围。

## 验证摘要

- InnerBrain 多轮澄清 RED/GREEN：新增 3 个 Agent 测试先失败，覆盖导入路径补槽、桌面快捷方式名称补槽和取消补充；实现 pending 澄清状态后目标测试通过。
- 专项回归：澄清提示、补槽执行和取消补充 4 个测试通过；`tests.test_inner_brain tests.test_agent` 共 229 项通过。
- 版本打包 RED/GREEN：版本一致性测试先因 `pyproject.toml` 仍为 `0.1.6` 失败；同步项目元数据、README 和测试目标到 `0.1.7` 后通过。
- 全量验证：全量 `unittest` 423 项通过；源码桌面 smoke 和打包后 `JarvisLite.exe --smoke` 均通过。
- 安装器验证：`install.cmd` 包含 `DisplayVersion /d "0.1.7"`、覆盖安装提示和用户数据保留提示；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.1.7 installation finished`。
- query 补槽 RED/GREEN：新增 2 个 Agent 测试先失败，澄清文案只显示原始 `query` 且补齐后不能生成 `/search` 或 `/search-summary`；改为优先使用 `slots.query` 后目标测试通过。
- query 补槽回归：`tests.test_inner_brain tests.test_agent` 共 231 项通过。
- `0.1.8` 打包 RED/GREEN：版本一致性测试先因 `pyproject.toml` 仍为 `0.1.7` 失败；同步项目元数据、README 和测试目标到 `0.1.8` 后通过。
- `0.1.8` 安装器验证：打包后 `JarvisLite.exe --smoke` 通过；`install.cmd` 包含 `DisplayVersion /d "0.1.8"`、覆盖安装提示和用户数据保留提示；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.1.8 installation finished`。
- `0.1.8` 收尾验证：全量 `unittest` 425 项通过，源码桌面 smoke 和打包后 `JarvisLite.exe --smoke` 均通过。
- 编号+标签联合补槽 RED/GREEN：新增 1 个 Agent 测试先失败，澄清文案只显示原始 `result_index、tags` 且编号词会污染标签；改为中文缺槽名、一句式提示和已知 intent 下的标签文本清理后目标测试通过。
- 编号+标签联合补槽回归：`tests.test_inner_brain tests.test_agent` 共 232 项通过。
- `0.1.9` 版本 RED/GREEN：版本一致性测试先因 `pyproject.toml` 仍为 `0.1.8` 失败；同步项目元数据、README 和测试目标到 `0.1.9` 后通过。
- `0.1.9` 收尾验证：全量 `unittest` 426 项通过，源码桌面 smoke 通过，打包后 `JarvisLite.exe --smoke` 退出码 0。
- `0.1.9` 安装器验证：`install.cmd` 包含 `DisplayVersion /d "0.1.9"`、覆盖安装提示和用户数据保留提示；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.1.9 installation finished`；安装包生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.1.9.exe`。
- 目录别名和经验内容补槽 RED/GREEN：新增 2 个 Agent 测试先失败，澄清文案只显示原始 `alias`/`experience` 且不能清理“目录是”“经验是”前缀；补齐中文缺槽名、提示示例和补槽归一化后目标测试通过。
- 目录别名和经验内容补槽回归：`tests.test_inner_brain tests.test_agent` 共 234 项通过。
- `0.1.10` 版本 RED/GREEN：版本一致性测试先因 `pyproject.toml` 仍为 `0.1.9` 失败；同步项目元数据、README 和测试目标到 `0.1.10` 后通过。
- `0.1.10` 收尾验证：全量 `unittest` 428 项通过，源码桌面 smoke 通过，打包后 `JarvisLite.exe --smoke` 退出码 0。
- `0.1.10` 安装器验证：`install.cmd` 包含 `DisplayVersion /d "0.1.10"`、覆盖安装提示和用户数据保留提示；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.1.10 installation finished`；安装包生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.1.10.exe`。
- `0.2.0` 多轮澄清 v1 RED/GREEN：新增 6 个 Agent 测试先失败，覆盖文件路径、编号资料、当前资料标签、标签组+新标签、经验搜索关键词和经验建议关键词；补齐提示文案和已知 intent 下的标签组补槽拆分后目标测试通过。
- `0.2.0` 专项回归：`tests.test_inner_brain tests.test_agent` 共 240 项通过。
- `0.2.0` 版本 RED/GREEN：版本一致性测试先因 `pyproject.toml` 仍为 `0.1.10` 失败；同步项目元数据、README 和测试目标到 `0.2.0` 后通过。
- `0.2.0` 收尾验证：全量 `unittest` 434 项通过，源码桌面 smoke 通过，打包后 `JarvisLite.exe --smoke` 退出码 0。
- `0.2.0` 安装器验证：`install.cmd` 包含 `DisplayVersion /d "0.2.0"`、覆盖安装提示和用户数据保留提示；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.2.0 installation finished`；安装包生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.2.0.exe`。
- `0.3.0` provider alias RED/GREEN：新增 5 个 LLM/Agent 测试先失败，覆盖 qwen/gemini alias 构建、缺 base_url 诊断、配置模板和 `/llm-enable` 状态展示；实现 alias -> OpenAI-compatible adapter 后通过。
- `0.3.0` 专项回归：`tests.test_llm tests.test_agent` 共 251 项通过。
- `0.3.0` 版本 RED/GREEN：版本一致性测试先因 `pyproject.toml` 仍为 `0.2.0` 失败；同步项目元数据、README 和测试目标到 `0.3.0` 后通过。
- `0.3.0` 收尾验证：全量 `unittest` 438 项通过，源码桌面 smoke 通过，打包后 `JarvisLite.exe --smoke` 退出码 0。
- `0.3.0` 安装器验证：`install.cmd` 包含 `DisplayVersion /d "0.3.0"`、覆盖安装提示和用户数据保留提示；`JarvisLiteSetup.sed` 包含 `Jarvis Lite 0.3.0 installation finished`；安装包生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.3.0.exe`。
- 静态验证：`git diff --check` 退出 0（仅 LF/CRLF 工作区换行提示），Markdown 本地链接检查通过，敏感信息扫描通过，本地 LLM/Search 配置未被 Git 跟踪。
- 完整验证见 [../../verification/2026-05/2026-05-29.md](../../verification/2026-05/2026-05-29.md)。

## 下一步

- 继续扩大多轮澄清覆盖的缺失槽位类型，例如更多组合槽位、路径+标签、最近文件导入来源和目录整理别名。
- 继续基于真实使用日志缩小 `legacy_fallback` 范围。
- 评估更稳定的轻量分类器方案，例如字符 n-gram、embedding 相似度或小型分类器。
- 根据真实外脑配置反馈决定是否需要 Gemini/Qwen 原生 adapter；若兼容端点足够，优先继续打磨提示词、上下文和 smoke 诊断。
