# 2026-06-02 进度记录

> 日期：2026-06-02
> 执行者：Codex

## 当日目标

- 接续 `日志.txt` 中断的 `0.35.0` 任务，完成 InnerBrain 训练候选按编号写入本机 evaluation 样本。
- 保持评估和训练分离：只写 `data/inner-brain/evaluation/runtime.jsonl`，不写 runtime 训练样本，不刷新 InnerBrain，不移除候选。
- 继续推进 `0.36.0`，按 v39 后续补齐 InnerBrain 本机 evaluation JSONL 文件过滤。
- 继续推进 `0.37.0`，按 v40 后续补齐本机失败评估按 JSONL 文件分组。
- 继续推进 `0.38.0`，按 v41 后续补齐本机失败评估 Markdown 报告导出。
- 继续推进 `0.39.0`，按 v42 后续补齐本机失败评估原因汇总。
- 继续推进 `0.40.0`，按 v43 后续补齐本机失败评估类型汇总。
- 继续推进 `0.41.0`，按 v44 后续补齐本机失败评估期望意图汇总。
- 继续推进 `0.42.0`，按 v45 后续补齐本机失败评估意图混淆汇总。
- 继续推进 `0.43.0`，按 v46 后续补齐本机失败评估文件意图混淆交叉汇总。
- 继续推进 `0.44.0`，按 v47 后续补齐本机失败评估意图混淆修复建议分组。
- 继续推进 `0.45.0`，按 v48 后续补齐本机失败评估文件意图混淆修复建议分组。
- 继续推进 `0.46.0`，按 v49 后续补齐本机 evaluation 空样本引导。
- 继续推进 `0.47.0`，按 v51 后续补齐本机 evaluation 样本保存后的验证命令提示。
- 继续推进 `0.48.0`，按 v52 后续补齐本机失败评估报告导出后的处理命令提示。
- 继续推进 `0.49.0`，按 v53 后续补齐本机失败视图中的导出失败报告提示。
- 继续推进 `0.50.0`，按 v54 后续补齐本机失败视图中的文件聚焦提示。

## 完成事项

- 新增 `/inner-brain-eval-add-candidate 编号 => /命令`：
  - 复用 `/inner-brain-candidates` 的候选编号。
  - 复用教学目标命令白名单，把候选原文保存为本机命令评估样本。
  - 候选编号不存在时返回候选缺失提示，不写 evaluation 或 training 文件。
- 新增 `/inner-brain-eval-label-candidate 编号 => intent [slot=value ...]`：
  - 复用候选编号和 label slot 解析。
  - 把候选原文保存为本机 intent 评估样本。
  - 保存后候选仍保留，可继续用于显式 teach 或 label。
- `/inner-brain-candidates` 输出增加按编号保存评估样本提示。
- 新入口纳入路由观察命令，不污染最近路由历史。
- 项目版本提升到 `0.35.0`，并同步 README、PROJECT-PLAN、v39 方案、进度索引和验证记录。
- 新增 `0.36.0` `/inner-brain-eval-local-file 文件名`：
  - 按 `data/inner-brain/evaluation/*.jsonl` basename 过滤本机评估样本。
  - 用户省略 `.jsonl` 时自动补全扩展名。
  - 评估报告显示 `评估集：local_evaluation:<文件名>` 和 `评估文件：<文件名>`。
- 新增 `0.36.0` `/inner-brain-eval-local-file-failed 文件名`：
  - 只显示指定本机 JSONL 文件中的失败样本和显式修复建议。
  - 不写入 `data/inner-brain/training/runtime.jsonl`。
- `InnerBrainEvaluationCase` 增加只读 `source_file` 元数据，加载本机 JSONL 时记录来源文件名；评估 payload 和去重键保持不变。
- 新命令纳入路由观察命令，不污染最近路由历史。
- 项目版本提升到 `0.36.0`，并同步 README、PROJECT-PLAN、v40 方案、进度索引和验证记录。
- 新增 `0.37.0` 本机失败评估文件分组：
  - `InnerBrainEvaluationReport.failed_source_file_counts` 只统计失败样本的来源 JSONL 文件。
  - `/inner-brain-eval-local-failed` 在未指定单个文件时显示 `失败文件：`，只列出失败来源文件和失败条数。
  - 失败视图不再把通过文件混入文件分组，降低排查噪音。
- 项目版本提升到 `0.37.0`，并同步 README、PROJECT-PLAN、v41 方案、进度索引和验证记录。
- 新增 `0.38.0` 本机失败评估报告导出：
  - `export_inner_brain_evaluation_report()` 把失败评估结果写入 `word/inner-brain-evaluation-report.md`。
  - `/inner-brain-eval-local-report [文件名]` 可导出全部本机失败评估，或按指定 JSONL 文件导出失败项。
  - 报告包含评估结果、失败文件分组、失败样例、原因和显式修复建议。
- 项目版本提升到 `0.38.0`，并同步 README、PROJECT-PLAN、v42 方案、进度索引和验证记录。
- 新增 `0.39.0` 本机失败评估原因汇总：
  - `InnerBrainEvaluationReport.failed_reason_counts` 会按失败原因文本统计失败数量。
  - `describe_inner_brain_evaluation(..., failures_only=True)` 在失败文件分组后输出 `失败原因汇总：`。
  - `/inner-brain-eval-local-report [文件名]` 导出的 Markdown 报告会按失败原因列出数量和典型样本。
- 项目版本提升到 `0.39.0`，并同步 README、PROJECT-PLAN、v43 方案、进度索引和验证记录。
- 新增 `0.40.0` 本机失败评估类型汇总：
  - `InnerBrainEvaluationReport.failed_reason_category_counts` 会按意图、命令和策略失败类型统计失败数量。
  - `describe_inner_brain_evaluation(..., failures_only=True)` 在失败文件分组后输出 `失败类型汇总：`。
  - `/inner-brain-eval-local-report [文件名]` 导出的 Markdown 报告会按失败类型列出数量和典型样本。
- 项目版本提升到 `0.40.0`，并同步 README、PROJECT-PLAN 和 v44 方案。
- 新增 `0.41.0` 本机失败评估期望意图汇总：
  - `InnerBrainEvaluationReport.failed_expected_intent_counts` 会按失败样本的 `expected_intent` 统计失败数量。
  - `describe_inner_brain_evaluation(..., failures_only=True)` 在失败类型汇总后输出 `失败期望意图汇总：`。
  - `/inner-brain-eval-local-report [文件名]` 导出的 Markdown 报告会按失败期望意图列出数量和典型样本。
- 项目版本提升到 `0.41.0`，并同步 README、PROJECT-PLAN 和 v45 方案。
- 新增 `0.42.0` 本机失败评估意图混淆汇总：
  - `InnerBrainEvaluationReport.failed_intent_confusion_counts` 会按失败样本的 `expected_intent -> actual_intent` 统计失败数量。
  - `describe_inner_brain_evaluation(..., failures_only=True)` 在失败期望意图汇总后输出 `失败意图混淆汇总：`。
  - 只统计 `expected_intent != result.intent` 的失败样本，策略或命令失败但意图一致的样本不会进入混淆汇总。
  - `/inner-brain-eval-local-report [文件名]` 导出的 Markdown 报告会按失败意图混淆方向列出数量和典型样本。
- 项目版本提升到 `0.42.0`，并同步 README、PROJECT-PLAN 和 v46 方案。
- 新增 `0.43.0` 本机失败评估文件意图混淆汇总：
  - `InnerBrainEvaluationReport.failed_source_file_intent_confusion_counts` 会按失败样本的 `source_file + expected_intent -> actual_intent` 统计失败数量。
  - `describe_inner_brain_evaluation(..., failures_only=True)` 在失败意图混淆汇总后输出 `失败文件意图混淆汇总：`。
  - 只统计有 `source_file` 且 `expected_intent != result.intent` 的失败样本；指定单个文件过滤时不展示交叉汇总。
  - `/inner-brain-eval-local-report` 导出的 Markdown 报告会按失败文件和意图混淆方向列出数量和典型样本。
- 项目版本提升到 `0.43.0`，并同步 README、PROJECT-PLAN 和 v47 方案。
- 新增 `0.44.0` 本机失败评估意图混淆修复建议分组：
  - `describe_inner_brain_evaluation(..., failures_only=True)` 会从失败样本中筛选 `expected_intent != result.intent` 的意图混淆记录。
  - 失败视图和 `/inner-brain-eval-local-report [文件名]` 导出的 Markdown 报告新增 `失败意图混淆修复建议：`，按 `expected_intent -> actual_intent` 分组列出显式 `/inner-brain-teach` 或 `/inner-brain-label` 建议。
  - 保留现有平铺 `失败修复建议：`，策略或命令不匹配但意图一致的样本仍在平铺建议中展示。
  - 新逻辑只读本机评估样本，不写训练样本、不自动训练、不改变 evaluation JSONL payload。
- 项目版本提升到 `0.44.0`，并同步 README、PROJECT-PLAN 和 v48 方案。
- 新增 `0.45.0` 本机失败评估文件意图混淆修复建议分组：
  - `describe_inner_brain_evaluation(..., failures_only=True)` 会从失败样本中筛选 `source_file` 存在且 `expected_intent != result.intent` 的意图混淆记录。
  - 未指定单个文件时，失败视图和 `/inner-brain-eval-local-report` 导出的 Markdown 报告新增 `失败文件意图混淆修复建议：`，按来源文件和 `expected_intent -> actual_intent` 分组列出显式 `/inner-brain-teach` 或 `/inner-brain-label` 建议。
  - 指定单个文件导出时不展示文件级修复建议分组，避免重复列出同一个文件名；保留现有 `失败意图混淆修复建议：` 和平铺 `失败修复建议：`。
  - 新逻辑只读本机评估样本，不写训练样本、不自动训练、不改变 evaluation JSONL payload。
- 项目版本提升到 `0.45.0`，并同步 README、PROJECT-PLAN 和 v49 方案。
- 新增 `0.46.0` 本机 evaluation 空样本引导：
  - 当 `local_evaluation` 评估集没有任何本机 JSONL 样本时，`describe_inner_brain_evaluation()` 会输出 `本机评估样本：- 无`。
  - 空状态会列出 `/inner-brain-eval-add`、`/inner-brain-eval-label`、`/inner-brain-eval-add-candidate` 和 `/inner-brain-eval-label-candidate`，并说明这些命令只写本机 evaluation 样本、不自动训练。
  - `/inner-brain-eval-local` 和 `/inner-brain-eval-local-failed` 继续复用同一评估描述函数，不新增命令、不写 training runtime 样本、不改变 evaluation JSONL payload。
- 项目版本提升到 `0.46.0`，并同步 README、PROJECT-PLAN 和 v50 方案。
- 新增 `0.47.0` 本机 evaluation 样本保存后续验证提示：
  - `/inner-brain-eval-add`、`/inner-brain-eval-label`、`/inner-brain-eval-add-candidate` 和 `/inner-brain-eval-label-candidate` 统一复用保存反馈出口。
  - 保存反馈会提示 `/inner-brain-eval-local`、`/inner-brain-eval-local-failed`、`/inner-brain-eval-local-file runtime.jsonl` 和 `/inner-brain-eval-local-report`。
  - 新逻辑不新增命令、不自动运行评估、不写 training runtime 样本、不改变 evaluation JSONL payload 或保存路径。
- 项目版本提升到 `0.47.0`，并同步 README、PROJECT-PLAN 和 v51 方案。
- 新增 `0.48.0` 本机失败评估报告导出后续处理提示：
  - `/inner-brain-eval-local-report [文件名]` 导出报告后会输出 `后续处理：`。
  - 未指定文件时提示查看本机失败样本、按文件聚焦失败和补充 evaluation 样本；指定文件时提示复查当前文件失败样本、查看全部本机失败样本和补充 evaluation 样本。
  - 新逻辑不新增命令、不改变导出的 Markdown 报告内容、不写 training runtime 样本、不改变 evaluation JSONL payload 或保存路径。
- 项目版本提升到 `0.48.0`，并同步 README、PROJECT-PLAN 和 v52 方案。
- 新增 `0.49.0` 本机失败视图导出报告提示：
  - `/inner-brain-eval-local-failed` 有失败样本时会输出 `后续处理：`，提示导出全部本机失败报告和按文件导出失败报告。
  - `/inner-brain-eval-local-file-failed 文件名` 有失败样本时会提示导出当前文件失败报告和全部本机失败报告。
  - 本机 evaluation 样本为空或当前视图没有失败样本时不追加导出提示，保留原有空状态和无失败输出。
  - 新逻辑不新增命令、不改变导出的 Markdown 报告内容、不自动运行报告导出、不写 training runtime 样本、不改变 evaluation JSONL payload 或保存路径。
- 项目版本提升到 `0.49.0`，并同步 README、PROJECT-PLAN 和 v53 方案。
- 新增 `0.50.0` 本机失败视图文件聚焦提示：
  - `/inner-brain-eval-local-failed` 有失败样本时会在 `后续处理：` 中提示 `/inner-brain-eval-local-file-failed 文件名`。
  - `/inner-brain-eval-local-file-failed 文件名` 有失败样本时会提示 `/inner-brain-eval-local-failed`，便于回到全部本机失败样本。
  - 空样本或无失败样本时不追加文件聚焦提示。
  - 新逻辑不新增命令、不改变导出的 Markdown 报告内容、不自动运行报告导出、不写 training runtime 样本、不改变 evaluation JSONL payload 或保存路径。
- 项目版本提升到 `0.50.0`，并同步 README、PROJECT-PLAN 和 v54 方案。
- 新增 `0.51.0` 本机 evaluation 已处理样本只读清单：
  - 新增 `/inner-brain-eval-local-resolved [文件名]`，只读列出当前已经通过的本机 evaluation 样本。
  - 省略文件名时查看全部本机 evaluation 已处理样本；指定文件名时只查看该 JSONL 文件，文件名继续支持省略 `.jsonl`。
  - 输出 `已处理样例：`，没有当前已通过样本时显示 `- 无`。
  - 命令响应会提示回到 `/inner-brain-eval-local-failed` 或 `/inner-brain-eval-local-file-failed 文件名` 继续处理待处理失败样本。
  - 新逻辑只读执行评估，不自动训练、不写 training runtime 样本、不改变 evaluation JSONL payload 或保存路径。
- 项目版本提升到 `0.51.0`，并同步 README、PROJECT-PLAN 和 v55 方案。
- 新增 `0.52.0` 本机评估全量视图后续处理提示：
  - `/inner-brain-eval-local` 有本机 evaluation 样本时会追加 `后续处理：`，提示 `/inner-brain-eval-local-failed`、`/inner-brain-eval-local-resolved` 和 `/inner-brain-eval-local-file 文件名`。
  - `/inner-brain-eval-local-file 文件名` 有样本时会提示当前文件失败视图、当前文件已处理清单和返回全部本机评估。
  - 空本机 evaluation 样本不追加全量后续处理，继续保留添加样本引导。
  - 新逻辑只改变 Agent 响应文本，不自动训练、不写 training runtime 样本、不改变 evaluation JSONL payload 或报告导出内容。
- 项目版本提升到 `0.52.0`，并同步 README、PROJECT-PLAN 和 v56 方案。
- 新增 `0.53.0` 本机评估全量视图文件名候选提示：
  - `/inner-brain-eval-local` 有本机 evaluation 来源文件时，会在 `后续处理：` 后追加 `可聚焦文件：`。
  - 每个来源 JSONL 文件会展示样本数量和可复制的 `/inner-brain-eval-local-file 文件名` 命令。
  - 文件过滤视图、失败视图、已处理视图和报告导出不改行为。
  - 新逻辑只改变 Agent 响应文本，不自动训练、不写 training runtime 样本、不改变 evaluation JSONL payload 或报告导出内容。
- 项目版本提升到 `0.53.0`，并同步 README、PROJECT-PLAN 和 v57 方案。
- 新增 `0.54.0` 本机评估全量视图文件候选状态摘要：
  - `/inner-brain-eval-local` 的 `可聚焦文件：` 候选行继续展示来源 JSONL 文件名、总样本数和可复制的文件聚焦命令。
  - 每个候选文件行会追加 `通过 N 条，失败 N 条`，帮助优先处理有失败的文件。
  - 统计只在 Agent 响应层根据 `report.case_results` 现算，不新增 `InnerBrainEvaluationReport` 字段。
  - 文件过滤视图、失败视图、已处理视图和报告导出不改行为；不自动训练、不写 training runtime 样本。
- 项目版本提升到 `0.54.0`，并同步 README、PROJECT-PLAN 和 v58 方案。
- 新增 `0.55.0` 本机评估全量视图文件候选失败优先排序：
  - `/inner-brain-eval-local` 的 `可聚焦文件：` 候选列表按失败数量降序、总样本数降序、文件名升序排序。
  - 有失败样本的 JSONL 文件会排在纯通过文件前面，方便先进入待处理文件。
  - 候选行格式继续保留总数、通过数、失败数和 `/inner-brain-eval-local-file 文件名`。
  - 新逻辑只改变 Agent 响应文本顺序，不自动训练、不写 training runtime 样本、不改变 evaluation JSONL payload 或报告导出内容。
- 项目版本提升到 `0.55.0`，并同步 README、PROJECT-PLAN 和 v59 方案。
- 新增 `0.56.0` 本机失败视图失败文件汇总排序：
  - `/inner-brain-eval-local-failed` 的 `失败文件：` 分组按失败数量降序、文件名升序排序。
  - 未指定文件的本机失败 Markdown 报告复用同一排序，方便先处理失败贡献最高的 JSONL 文件。
  - 新逻辑只改变失败文件分组展示顺序，不自动训练、不写 training runtime 样本、不改变 evaluation JSONL payload、失败样例或修复建议。
- 项目版本提升到 `0.56.0`，并同步 README、PROJECT-PLAN 和 v60 方案。
- 新增 `0.57.0` 本机文件失败视图已处理入口：
  - `/inner-brain-eval-local-file-failed 文件名` 有失败样本时，会在 `后续处理：` 中提示 `/inner-brain-eval-local-resolved 当前文件名`。
  - 保留查看全部本机失败样本、导出当前文件失败报告和导出全部本机失败报告入口。
  - 新逻辑只改变 Agent 响应文本，不自动训练、不写 training runtime 样本、不改变 evaluation JSONL payload、失败样例、汇总分组或报告导出内容。
- 项目版本提升到 `0.57.0`，并同步 README、PROJECT-PLAN 和 v61 方案。
- 新增 `0.58.0` 本机已处理视图文件候选提示：
  - `/inner-brain-eval-local-resolved` 未指定文件且存在通过样本时，会在 `后续处理：` 中追加 `可查看文件：`。
  - 候选文件只统计当前通过样本，纯失败文件不会出现在已处理候选列表。
  - 候选排序为通过数量降序、文件名升序；不新增命令，不改变指定文件已处理视图。
- 项目版本提升到 `0.58.0`，并同步 README、PROJECT-PLAN 和 v62 方案。
- 新增 `0.59.0` 本机已处理视图文件候选状态摘要：
  - `/inner-brain-eval-local-resolved` 全量已处理视图的 `可查看文件：` 候选行会显示已处理数量和同文件待处理失败数量。
  - 候选文件仍只统计有通过样本的来源文件，纯失败文件不会出现在已处理候选列表。
  - 排序继续保持通过数量降序、文件名升序；不新增命令，不改变指定文件已处理视图。
- 项目版本提升到 `0.59.0`，并同步 README、PROJECT-PLAN 和 v63 方案。
- 新增 `0.60.0` 本机已处理视图文件候选待处理优先排序：
  - `/inner-brain-eval-local-resolved` 全量已处理视图的 `可查看文件：` 候选列表优先展示同文件待处理失败数量更多的来源文件。
  - 候选行继续显示已处理数量和待处理失败数量，纯失败文件仍不会进入已处理候选列表。
  - 排序键调整为待处理失败数量降序、已处理数量降序、文件名升序；不新增命令，不改变指定文件已处理视图。
- 项目版本提升到 `0.60.0`，并同步 README、PROJECT-PLAN 和 v64 方案。
- 新增 `0.61.0` 本机已处理视图文件候选待处理入口：
  - `/inner-brain-eval-local-resolved` 全量已处理视图的 `可查看文件：` 候选行在同文件待处理失败数量大于 0 时追加 `/inner-brain-eval-local-file-failed 当前文件名`。
  - 纯已处理文件不追加失败入口，避免引导到空失败视图。
  - 新逻辑只改变 Agent 响应文本，不新增命令、不自动训练、不写 training runtime 样本、不改变排序或 evaluation JSONL payload。
- 项目版本提升到 `0.61.0`，并同步 README、PROJECT-PLAN 和 v65 方案。
- 新增 `0.62.0` 本机评估全量视图文件候选待处理入口：
  - `/inner-brain-eval-local` 全量视图的 `可聚焦文件：` 候选行在同文件失败数量大于 0 时追加 `/inner-brain-eval-local-file-failed 当前文件名`。
  - 纯通过文件不追加失败入口，避免引导到空失败视图。
  - 新逻辑只改变 Agent 响应文本，不新增命令、不自动训练、不写 training runtime 样本、不改变排序、文件过滤视图或 evaluation JSONL payload。
- 项目版本提升到 `0.62.0`，并同步 README、PROJECT-PLAN 和 v66 方案。

## 验证摘要

- `0.35.0` RED/GREEN：新增 Agent 候选编号评估命令、候选编号评估标注、缺失候选不写入和版本一致性测试先失败；实现后目标测试 4 项通过。
- `0.35.0` 相邻回归 413 项通过，全量 `python -m unittest discover -s tests -v` 540 项通过。
- `0.35.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.35.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.35.0.exe`，大小 `57,122,816` 字节；安装脚本和完成弹窗均显示 `0.35.0`。
- `0.35.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.36.0` RED/GREEN：新增 InnerBrain 文件过滤、Agent 指定文件评估、Agent 指定文件失败视图和版本一致性测试先失败；实现后目标测试 4 项通过。
- `0.36.0` 相邻回归 416 项通过，全量 `python -m unittest discover -s tests -v` 543 项通过。
- `0.36.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.36.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.36.0.exe`，大小 `57,122,816` 字节；安装脚本和完成弹窗均显示 `0.36.0`。
- `0.36.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.37.0` RED/GREEN：新增 InnerBrain 失败文件分组、Agent 本机失败文件分组和版本一致性测试先失败；实现后目标测试 3 项通过。
- `0.37.0` 相邻回归 418 项通过，全量 `python -m unittest discover -s tests -v` 545 项通过。
- `0.37.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.37.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.37.0.exe`，大小 `57,126,912` 字节；安装脚本和完成弹窗均显示 `0.37.0`。
- `0.37.0` 收尾目标测试复跑 3 项通过，静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.38.0` RED/GREEN：新增 InnerBrain 报告导出、Agent 本机失败报告导出、Agent 指定文件报告导出和版本一致性测试先失败；实现后目标测试 4 项通过。
- `0.38.0` InnerBrain + Agent 相邻回归 327 项通过。
- `0.38.0` 全量 `python -m unittest discover -s tests -v` 548 项通过。
- `0.38.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.38.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.38.0.exe`，大小 `57,126,912` 字节；安装脚本和完成弹窗均显示 `0.38.0`。
- `0.38.0` 收尾目标测试复跑 4 项通过，静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.39.0` RED/GREEN：新增 InnerBrain 失败原因汇总、InnerBrain 报告导出包含原因汇总、Agent 本机失败报告包含原因汇总和版本一致性测试先失败；实现后目标测试 4 项通过。
- `0.39.0` InnerBrain + Agent 相邻回归首次因更新检测 fixture 断言仍为 `0.38.1` 失败 1 项；同步断言为 `0.39.1` 后复跑 328 项通过。
- `0.39.0` 全量 `python -m unittest discover -s tests -v` 549 项通过。
- `0.39.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.39.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.39.0.exe`，大小 `57,126,912` 字节；安装脚本和完成弹窗均显示 `0.39.0`。
- `0.39.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.40.0` RED/GREEN：新增 InnerBrain 失败类型汇总、InnerBrain 报告导出包含类型汇总、Agent 本机失败报告包含类型汇总和版本一致性测试先失败；实现后目标测试 4 项通过。
- `0.40.0` InnerBrain + Agent 相邻回归 329 项通过。
- `0.40.0` 全量 `python -m unittest discover -s tests -v` 550 项通过。
- `0.40.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.40.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.40.0.exe`，大小 `57,126,912` 字节；安装脚本和完成弹窗均显示 `0.40.0`。
- `0.40.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.41.0` RED/GREEN：新增 InnerBrain 失败期望意图汇总、InnerBrain 报告导出包含期望意图汇总、Agent 本机失败报告包含期望意图汇总和版本一致性测试先失败；实现后目标测试 4 项通过。
- `0.41.0` InnerBrain + Agent 相邻回归 330 项通过。
- `0.41.0` 全量 `python -m unittest discover -s tests -v` 551 项通过。
- `0.41.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.41.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.41.0.exe`，大小 `57,126,912` 字节；安装脚本和完成弹窗均显示 `0.41.0`。
- `0.41.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.42.0` RED/GREEN：新增 InnerBrain 失败意图混淆汇总、InnerBrain 报告导出包含混淆汇总、Agent 本机失败报告包含混淆汇总和版本一致性测试先失败；实现后目标测试 4 项通过。
- `0.42.0` InnerBrain + Agent 相邻回归 331 项通过。
- `0.42.0` 全量 `python -m unittest discover -s tests -v` 552 项通过。
- `0.42.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.42.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.42.0.exe`，大小 `57,126,912` 字节；安装脚本和完成弹窗均显示 `0.42.0`。
- `0.42.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.43.0` RED/GREEN：新增 InnerBrain 失败文件意图混淆汇总、InnerBrain 报告导出包含文件混淆汇总、Agent 本机失败报告包含文件混淆汇总和版本一致性测试先失败；实现后目标测试 4 项通过。
- `0.43.0` InnerBrain + Agent 相邻回归 332 项通过。
- `0.43.0` 全量 `python -m unittest discover -s tests -v` 553 项通过。
- `0.43.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.43.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.43.0.exe`，大小 `57,131,008` 字节；安装脚本和完成弹窗均显示 `0.43.0`。
- `0.43.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.44.0` RED/GREEN：新增 InnerBrain 意图混淆修复建议分组、InnerBrain 报告导出包含分组建议、Agent 本机失败报告包含分组建议和版本一致性测试先失败；实现后目标测试 4 项通过。
- `0.44.0` InnerBrain + Agent 相邻回归 333 项通过。
- `0.44.0` 全量 `python -m unittest discover -s tests -v` 554 项通过。
- `0.44.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.44.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.44.0.exe`，大小 `57,131,008` 字节；安装脚本和完成弹窗均显示 `0.44.0`。
- `0.44.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.45.0` RED/GREEN：新增 InnerBrain 文件意图混淆修复建议分组、InnerBrain 报告导出包含文件分组建议、Agent 本机失败报告包含文件分组建议和版本一致性测试先失败；补强单文件过滤负向断言后目标测试 5 项通过。
- `0.45.0` InnerBrain + Agent 相邻回归 334 项通过。
- `0.45.0` 全量 `python -m unittest discover -s tests -v` 555 项通过。
- `0.45.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.45.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.45.0.exe`，大小 `57,131,008` 字节；安装脚本和完成弹窗均显示 `0.45.0`。
- `0.45.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.46.0` RED/GREEN：新增 InnerBrain 本机 evaluation 空样本引导、Agent 本机失败空样本命令和版本一致性测试先失败；实现后目标测试 3 项通过。
- `0.46.0` InnerBrain + Agent 相邻回归 336 项通过。
- `0.46.0` 全量 `python -m unittest discover -s tests -v` 557 项通过。
- `0.46.0` 实际空本机评估命令 `/inner-brain-eval-local-failed` 输出本机评估样本为空、四个 evaluation 样本添加入口和不自动训练说明。
- `0.46.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.46.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.46.0.exe`，大小 `57,131,008` 字节；安装脚本和完成弹窗均显示 `0.46.0`。
- `0.46.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.47.0` RED/GREEN：新增 Agent 本机 evaluation 保存反馈后续验证提示和版本一致性测试先失败；实现后目标测试 3 项通过。
- `0.47.0` InnerBrain + Agent 相邻回归 336 项通过。
- `0.47.0` 全量 `python -m unittest discover -s tests -v` 557 项通过。
- `0.47.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.47.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.47.0.exe`，大小 `57,131,008` 字节；安装脚本和完成弹窗均显示 `0.47.0`。
- `0.47.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.48.0` RED/GREEN：新增 Agent 本机失败报告导出后续处理提示和版本一致性测试先失败；实现后目标测试 3 项通过。
- `0.48.0` InnerBrain + Agent 相邻回归 336 项通过。
- `0.48.0` 全量 `python -m unittest discover -s tests -v` 557 项通过。
- `0.48.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.48.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.48.0.exe`，大小 `57,131,008` 字节；安装脚本和完成弹窗均显示 `0.48.0`。
- `0.48.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.49.0` RED/GREEN：新增 Agent 本机失败视图导出报告提示、空样本负向断言和版本一致性测试先失败；实现后目标测试 4 项通过。
- `0.49.0` InnerBrain + Agent 相邻回归 336 项通过。
- `0.49.0` 全量 `python -m unittest discover -s tests -v` 557 项通过。
- `0.49.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.49.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.49.0.exe`，大小 `57,131,008` 字节；安装脚本和完成弹窗均显示 `0.49.0`。
- `0.49.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.50.0` RED/GREEN：新增 Agent 本机失败视图文件聚焦提示、文件失败视图查看全部提示、空样本负向断言和版本一致性测试先失败；实现后目标测试 4 项通过。
- `0.50.0` InnerBrain + Agent 相邻回归 336 项通过。
- `0.50.0` 全量 `python -m unittest discover -s tests -v` 557 项通过。
- `0.50.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.50.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.50.0.exe`，大小 `57,126,912` 字节；安装脚本和完成弹窗均显示 `0.50.0`。
- `0.50.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.51.0` RED/GREEN：新增 InnerBrain 已处理样本只读描述、Agent 本机已处理样本清单、文件过滤、空清单和版本一致性测试先失败；实现后目标测试 6 项通过。
- `0.51.0` InnerBrain + Agent 相邻回归 341 项通过。
- `0.51.0` 全量 `python -m unittest discover -s tests -v` 562 项通过。
- `0.51.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.51.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.51.0.exe`，大小 `57,131,008` 字节；安装脚本和完成弹窗均显示 `0.51.0`。
- `0.51.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.52.0` RED/GREEN：新增 Agent 本机全量评估后续处理提示、文件全量评估后续处理提示、空本机样本负向断言和版本一致性测试先失败；实现后目标测试 4 项通过。
- `0.52.0` InnerBrain + Agent 相邻回归 341 项通过。
- `0.52.0` 全量 `python -m unittest discover -s tests -v` 562 项通过。
- `0.52.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.52.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.52.0.exe`，大小 `57,131,008` 字节；安装脚本和完成弹窗均显示 `0.52.0`。
- `0.52.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.53.0` RED/GREEN：新增 Agent 本机全量评估文件名候选提示和版本一致性测试先失败；实现后目标测试 3 项通过。
- `0.53.0` InnerBrain + Agent 相邻回归 341 项通过。
- `0.53.0` 全量 `python -m unittest discover -s tests -v` 562 项通过。
- `0.53.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.53.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.53.0.exe`，大小 `57,131,008` 字节；安装脚本和完成弹窗均显示 `0.53.0`。
- `0.53.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.54.0` RED/GREEN：新增 Agent 本机全量评估文件候选状态摘要和版本一致性测试先失败；实现后目标测试 3 项通过。
- `0.54.0` InnerBrain + Agent 相邻回归 341 项通过。
- `0.54.0` 全量 `python -m unittest discover -s tests -v` 562 项通过。
- `0.54.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.54.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.54.0.exe`，大小 `57,131,008` 字节；安装脚本和完成弹窗均显示 `0.54.0`。
- `0.54.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.55.0` RED/GREEN：新增 Agent 本机全量评估文件候选失败优先排序和版本一致性测试先失败；实现后目标测试 3 项通过。
- `0.55.0` InnerBrain + Agent 相邻回归 341 项通过。
- `0.55.0` 全量 `python -m unittest discover -s tests -v` 562 项通过。
- `0.55.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.55.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.55.0.exe`，大小 `57,131,008` 字节；安装脚本和完成弹窗均显示 `0.55.0`。
- `0.55.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.56.0` RED/GREEN：新增 InnerBrain 本机失败文件汇总排序、Agent 本机失败文件汇总排序和版本一致性测试先失败；实现后目标测试 3 项通过。
- `0.56.0` InnerBrain + Agent 相邻回归 343 项通过。
- `0.56.0` 全量 `python -m unittest discover -s tests -v` 564 项通过。
- `0.56.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.56.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.56.0.exe`，大小 `57,135,104` 字节；安装脚本和完成弹窗均显示 `0.56.0`。
- `0.56.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.57.0` RED/GREEN：新增 Agent 当前文件失败视图已处理入口和版本一致性测试先失败；实现后目标测试 2 项通过。
- `0.57.0` Agent + ProjectMetadata 相邻回归 302 项通过。
- `0.57.0` 全量 `python -m unittest discover -s tests -v` 564 项通过。
- `0.57.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.57.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.57.0.exe`，大小 `57,131,008` 字节；安装脚本和完成弹窗均显示 `0.57.0`。
- `0.57.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.58.0` RED/GREEN：新增 Agent 全量已处理视图文件候选和版本一致性测试先失败；实现后目标测试 2 项通过。
- `0.58.0` Agent + ProjectMetadata 相邻回归 302 项通过。
- `0.58.0` 全量 `python -m unittest discover -s tests -v` 564 项通过。
- `0.58.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.58.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.58.0.exe`，大小 `57,131,008` 字节；安装脚本和完成弹窗均显示 `0.58.0`。
- `0.58.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.59.0` RED/GREEN：新增 Agent 全量已处理视图文件候选状态摘要和版本一致性测试先失败；实现后目标测试 2 项通过。
- `0.59.0` Agent + ProjectMetadata 相邻回归 302 项通过。
- `0.59.0` 全量 `python -m unittest discover -s tests -v` 564 项通过。
- `0.59.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.59.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.59.0.exe`，大小 `57,131,008` 字节；安装脚本和完成弹窗均显示 `0.59.0`。
- `0.59.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.60.0` RED/GREEN：新增 Agent 全量已处理视图文件候选待处理优先排序和版本一致性测试先失败；实现后目标测试 2 项通过。
- `0.60.0` Agent + ProjectMetadata 相邻回归 302 项通过。
- `0.60.0` 全量 `python -m unittest discover -s tests -v` 564 项通过。
- `0.60.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.60.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.60.0.exe`，大小 `57,131,008` 字节；安装脚本和完成弹窗均显示 `0.60.0`。
- `0.60.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.61.0` RED/GREEN：新增 Agent 全量已处理视图文件候选待处理入口和版本一致性测试先失败；实现后目标测试 2 项通过。
- `0.61.0` Agent + ProjectMetadata 相邻回归 302 项通过。
- `0.61.0` 全量 `python -m unittest discover -s tests -v` 564 项通过。
- `0.61.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.61.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.61.0.exe`，大小 `57,131,008` 字节；安装脚本和完成弹窗均显示 `0.61.0`。
- `0.61.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.62.0` RED/GREEN：新增 Agent 本机评估全量视图文件候选待处理入口和版本一致性测试先失败；实现后目标测试 2 项通过。
- `0.62.0` Agent + ProjectMetadata 相邻回归 302 项通过。
- `0.62.0` 全量 `python -m unittest discover -s tests -v` 564 项通过。
- `0.62.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.62.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.62.0.exe`，大小 `57,131,008` 字节；安装脚本和完成弹窗均显示 `0.62.0`。
- `0.62.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.63.0` RED/GREEN：新增 InnerBrain 本机失败视图失败文件分组聚焦入口、Agent 本机失败视图失败文件分组聚焦入口、失败文件分组排序完整行和版本一致性测试先失败；实现后目标测试 5 项通过。
- `0.63.0` InnerBrain + Agent + ProjectMetadata 相邻回归 347 项通过。
- `0.63.0` 全量 `python -m unittest discover -s tests -v` 564 项通过。
- `0.63.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.63.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.63.0.exe`，大小 `57,135,104` 字节；安装脚本和完成弹窗均显示 `0.63.0`。
- `0.63.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.64.0` RED/GREEN：新增 InnerBrain 本机失败视图失败文件分组报告入口、Agent 本机失败视图失败文件分组报告入口、失败文件分组排序完整行和版本一致性测试先失败；实现后目标测试 5 项通过。
- `0.64.0` InnerBrain + Agent + ProjectMetadata 相邻回归 347 项通过。
- `0.64.0` 全量 `python -m unittest discover -s tests -v` 564 项通过。
- `0.64.0` 源码桌面 smoke 输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`；打包后窗口版 exe smoke 退出码 0。
- `0.64.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.64.0.exe`，大小 `57,135,104` 字节；安装脚本和完成弹窗均显示 `0.64.0`。
- `0.64.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。
- `0.65.0` RED/GREEN：新增 Agent 本机评估全量视图文件候选报告入口和版本一致性测试先失败；实现后目标测试 2 项通过。
- `0.65.0` Agent + ProjectMetadata 相邻回归 302 项通过。
- `0.65.0` 全量 `python -m unittest discover -s tests -v` 564 项通过。
- `0.65.0` 源码桌面 smoke 与打包后 exe smoke 均输出 `Jarvis Lite 桌面助手` 与 `desktopPetWindow`。
- `0.65.0` 安装包已生成到 `E:\oyzj\ai\jarvis-lite-dist\JarvisLiteSetup-0.65.0.exe`，大小 `57,135,104` 字节；安装脚本和完成弹窗均显示 `0.65.0`。
- `0.65.0` 静态检查、Markdown 本地链接检查、敏感信息差异扫描和本地配置跟踪检查均通过。

## 下一步

- 用户沉淀真实本机 evaluation 样本后，再根据失败报告中的高频期望意图、意图混淆方向、来源文件交叉混淆、文件级分组修复建议、失败类型和失败原因，优先补 seed/runtime 样本、槽位补全或轻量分类器评分。
- 若治理流程继续变长，再考虑在失败、已处理和文件聚焦视图中追加批量处理入口或更多文件级优先级提示。
