# 2026-06-03 进度记录

> 日期：2026-06-03
> 执行者：Codex

## 当日目标

- 继续 InnerBrain 本机 evaluation 治理闭环，把保存样本后的报告入口从全局报告收紧为当前 `runtime.jsonl` 文件报告。
- 保持 evaluation 与 training 分离：只改变反馈文案，不写 `data/inner-brain/training/runtime.jsonl`。

## 完成事项

- 新增 `0.69.0` 本机 evaluation 保存反馈按文件报告入口：
  - `/inner-brain-eval-add`、`/inner-brain-eval-label`、`/inner-brain-eval-add-candidate` 和 `/inner-brain-eval-label-candidate` 继续统一写入 `data/inner-brain/evaluation/runtime.jsonl`。
  - 保存反馈保留 `/inner-brain-eval-local`、`/inner-brain-eval-local-failed` 和 `/inner-brain-eval-local-file runtime.jsonl`。
  - 保存反馈的报告入口改为 `/inner-brain-eval-local-report runtime.jsonl`。
- 项目版本提升到 `0.69.0`，并同步 README、PROJECT-PLAN、v73 方案、进度索引和验证记录。
- 新增 `0.70.0` 本机失败报告导出反馈当前文件已处理入口：
  - `/inner-brain-eval-local-report failed-log.jsonl` 导出指定文件失败报告后，反馈会提示 `/inner-brain-eval-local-resolved failed-log.jsonl`。
  - 全量报告导出反馈保持原样；新入口只服务指定文件报告导出后的同文件对照。
  - 项目版本提升到 `0.70.0`，并同步 README、PROJECT-PLAN、v74 方案、进度索引和验证记录。
- 新增 `0.71.0` 本机失败报告导出反馈当前文件总览入口：
  - `/inner-brain-eval-local-report failed-log.jsonl` 导出指定文件失败报告后，反馈会提示 `/inner-brain-eval-local-file failed-log.jsonl`。
  - 全量报告导出反馈保持原样；新入口只服务指定文件报告导出后的同文件总览回跳。
  - 项目版本提升到 `0.71.0`，并同步 README、PROJECT-PLAN、v75 方案、进度索引和验证记录。
- 新增 `0.72.0` 本机文件失败视图当前文件总览入口：
  - `/inner-brain-eval-local-file-failed failed-log.jsonl` 聚焦指定文件失败样本后，反馈会提示 `/inner-brain-eval-local-file failed-log.jsonl`。
  - 全量失败视图反馈保持原样；新入口只服务指定文件失败视图后的同文件总览回跳。
  - 项目版本提升到 `0.72.0`，并同步 README、PROJECT-PLAN、v76 方案、进度索引和验证记录。
- 新增 `0.73.0` 本机已处理指定文件视图当前文件总览入口：
  - `/inner-brain-eval-local-resolved real-log.jsonl` 查看指定文件已处理样本后，反馈会提示 `/inner-brain-eval-local-file real-log.jsonl`。
  - 全量已处理视图反馈保持原样；新入口只服务指定文件已处理视图后的同文件总览回跳。
  - 项目版本提升到 `0.73.0`，并同步 README、PROJECT-PLAN、v77 方案、进度索引和验证记录。

## 验证摘要

- RED：本机 evaluation 四条保存入口反馈断言和版本一致性测试先失败。
- GREEN：同一目标测试已通过。
- `0.69.0`、`0.70.0`、`0.71.0`、`0.72.0` 和 `0.73.0` 的全量回归、安装包构建和打包后 smoke 结果记录在 [../../verification/2026-06/2026-06-03.md](../../verification/2026-06/2026-06-03.md)。

## 下一步

- 继续观察真实 `runtime.jsonl` 失败报告中的高频失败类型、期望意图和文件级混淆方向，再决定是否补 seed/runtime 样本或继续优化文件级治理提示。
