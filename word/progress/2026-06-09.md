# 2026-06-09 进度

> 日期：2026-06-09
> 执行者：Codex

## 当日目标

- 按 Jarvis Lite 1.0 正式版节奏继续推进偏好应用链路。
- 完成 `0.140.0` 偏好应用确认记录与撤销第一阶段。
- 完成 `0.141.0` 偏好进入普通回复上下文第一阶段。
- 完成 `0.142.0` 偏好格式化本地回答第一阶段。
- 完成 `0.143.0` 偏好应用撤销提示第一阶段。
- 完成 `0.144.0` 偏好本地回答附注范围第一阶段。
- 保持偏好应用链路的审计边界：最近有效确认记录可进入普通 LLM fallback 上下文、本地知识库命中回答附注和长期记忆兜底回答附注，但不接入 LLM 命令白名单、SearchRouter、InnerBrain、路由或桌面执行决策。

## 完成事项

- 复核 `.codex/` 与 `word/` 记录，确认上一阶段停在 `0.139.0` 偏好应用确认命令第一阶段。
- 创建 Python 3.13 虚拟环境并安装桌面打包依赖：`.\.venv\Scripts\python.exe -m pip install -e ".[desktop-build]"`。
- 新增 v145 上下文扫描 `.codex/context-scan-v145-preference-apply-audit.json` 和正式计划 `word/plans/2026-06-09-v145-preference-apply-audit-plan.md`。
- RED：偏好应用确认历史、无启用/冲突拒绝不写历史、撤销记录不影响偏好启用状态、Agent `/preference-apply-history` 和 `/preference-apply-undo`、配置管家入口、LLM 白名单边界和版本一致性测试先失败。
- GREEN：`/preference-apply-confirm [输入文本]` 成功后写入运行态确认记录，记录包含 `prefapp-...` 确认 ID、输入文本、偏好稳定 ID、偏好文本和状态。
- 新增 `/preference-apply-history` 查看最近确认记录。
- 新增 `/preference-apply-undo 编号或ID` 只把确认记录标记为已撤销；不停用偏好、不删除偏好、不回滚已经展示的输出。
- 修复 `JarvisAgent._runtime_context()` 保存运行态上下文时覆盖 helper 写入字段的问题，保留已持久化的 `recent_preference_applications`。
- 版本提升到 `0.140.0`，同步 `pyproject.toml`、`src/jarvis_lite/__init__.py` 和项目元数据测试。
- 同步 README、PROJECT-PLAN、计划索引、文档索引、每日进度入口和验证入口。
- 断线恢复后复核 `日志.txt`，确认上次中断在命令行 smoke 的错误导入；本轮改用真实公开 API 复跑。
- 追加修复同秒重复确认的确认 ID 碰撞：连续两次相同输入、相同启用偏好、相同时间戳确认时会保留两条不同 `prefapp-...` 历史记录。
- 生成 `0.140.0` Windows 安装包和版本化副本 `E:\ai\jarvis-lite-dist\JarvisLiteSetup-0.140.0.exe`。

## 验证摘要

- 环境基线：`tests.test_project_metadata -v` 和 `jarvis_lite.desktop.app --smoke` 通过。
- 目标 RED 已确认，失败点符合预期：缺少 helper、Agent 命令未知、版本不匹配。
- 目标 GREEN：`tests.test_preferences`、Agent 新命令测试、LLM 白名单边界和版本一致性测试通过。
- 相邻回归：`tests.test_preferences tests.test_memory_config_candidates tests.test_memory_config_manager tests.test_agent tests.test_llm tests.test_project_metadata -v` 通过，`Ran 453 tests`，`OK`。
- 追加同秒确认 ID 碰撞 RED/GREEN：单测先失败为历史仅 1 条，修复后目标集合 `438` 项通过。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，`Ran 770 tests`，`OK`。
- 命令行 smoke：`preference-apply-audit-smoke OK`。
- 源码桌面 smoke 和打包后 smoke 均通过，输出包含 `desktopPetWindow`，打包后无 stderr、无残留 `JarvisLite` 进程。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 666 项通过；严格真实密钥形态扫描 25 个公开变更/新增文件无命中。

## 0.141.0 偏好进入普通回复上下文第一阶段

### 完成事项

- 新增 v146 上下文扫描 `.codex/context-scan-v146-preference-reply-context.json` 和正式计划 `word/plans/2026-06-09-v146-preference-reply-context-plan.md`。
- RED：偏好确认上下文 helper、撤销后上下文失效、启用集合变化后旧确认失效、`/llm-context-preview` 展示有效偏好确认上下文、普通 LLM fallback 接收上下文、LLM provider instructions 仍排除偏好管理命令和版本一致性测试先失败。
- GREEN：新增 `describe_preference_reply_context()`，只选择最近一条状态为 `confirmed`、且当前已启用偏好 ID 和文本与确认记录完全一致、且无明显冲突的确认记录。
- 将有效偏好确认上下文接入 `JarvisAgent._llm_context_lines()`，因此 `/llm-context-preview` 和普通 LLM fallback 使用同一上下文来源。
- 撤销确认、停用偏好、删除偏好或启用集合变化后，旧确认不再进入普通 LLM fallback 上下文。
- 保持边界：不把偏好管理命令加入 LLM provider command 白名单，不改变 SearchRouter、InnerBrain、命令路由、授权层或桌面执行决策。
- 版本提升到 `0.141.0`，同步 `pyproject.toml`、`src/jarvis_lite/__init__.py` 和项目元数据测试。

### 验证摘要

- 目标 GREEN：`tests.test_preferences`、Agent LLM 上下文测试、LLM 白名单边界和版本一致性测试通过。
- 相邻回归：`tests.test_preferences tests.test_memory_config_candidates tests.test_memory_config_manager tests.test_agent tests.test_llm tests.test_project_metadata -v` 通过，`Ran 458 tests`，`OK`。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，`Ran 774 tests`，`OK`。
- 命令行 smoke：`preference-reply-context-smoke OK`。
- 源码桌面 smoke 和打包后 smoke 均通过，输出包含 `desktopPetWindow`，打包后无 stderr、无残留 `JarvisLite` 进程。
- Windows 安装包：生成 `E:\ai\jarvis-lite-dist\JarvisLiteSetup-0.141.0.exe`，大小 `59,711,488` 字节，版本资源为 `0.141.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 671 项通过；严格真实密钥形态扫描 14 个公开变更/新增文件无命中。

## 0.142.0 偏好格式化本地回答第一阶段

### 完成事项

- 新增 v147 上下文扫描 `.codex/context-scan-v147-preference-local-answer-format.json` 和正式计划 `word/plans/2026-06-09-v147-preference-local-answer-format-plan.md`。
- RED：本地回答偏好附注 helper、知识库 `/ask` 回答附注、长期记忆兜底回答附注、启用集合变化失效、LLM 白名单边界和版本一致性测试先失败。
- GREEN：新增 `describe_preference_local_answer_note()`，复用 v146 的有效确认记录判定，只在最新确认记录仍为 `confirmed`、当前已启用偏好 ID/文本与确认记录完全一致、且无明显冲突时返回附注。
- 将本地回答附注接入 `JarvisAgent._answer_from_data()` 和长期记忆兜底返回；保持原有知识库命中来源、匹配排序、命中原因和长期记忆摘要主体不变。
- 撤销确认、停用偏好、删除偏好、启用集合变化或产生冲突后，本地回答不再展示旧确认附注。
- 保持边界：不新增 slash command，不把偏好管理命令或附注文案加入 LLM provider command 白名单，不改变 SearchRouter、InnerBrain、命令路由、授权层或桌面执行决策。
- 版本提升到 `0.142.0`，同步 `pyproject.toml`、`src/jarvis_lite/__init__.py` 和项目元数据测试。

### 验证摘要

- 目标 RED 已确认，失败点符合预期：缺少 `describe_preference_local_answer_note()`，本地知识库/长期记忆回答没有附注，版本仍为 `0.141.0`。
- 目标 GREEN：本地回答附注、Agent 两条本地回答路径、LLM 白名单边界和版本一致性测试 5 项通过。
- 相邻回归：`tests.test_preferences tests.test_agent tests.test_llm tests.test_project_metadata -v` 通过，`Ran 445 tests`，`OK`。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，`Ran 777 tests`，`OK`。
- 命令行 smoke：`preference-local-answer-format-smoke OK`。
- 源码桌面 smoke 和打包后 smoke 均通过，输出包含 `desktopPetWindow`，打包后无 stderr、无残留 `JarvisLite` 进程。
- Windows 安装包：生成 `E:\ai\jarvis-lite-dist\JarvisLiteSetup-0.142.0.exe`，大小 `59,715,584` 字节，版本资源为 `0.142.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 674 项通过；严格真实密钥形态扫描 722 个文本文件无命中；README BOM 为 `EF-BB-BF`。

## 0.143.0 偏好应用撤销提示第一阶段

### 完成事项

- 新增 v148 上下文扫描 `.codex/context-scan-v148-preference-undo-hints.json` 和正式计划 `word/plans/2026-06-09-v148-preference-undo-hints-plan.md`。
- RED：确认输出精确撤销提示、本地知识库命中回答附注、长期记忆兜底附注、普通 LLM fallback context 不泄漏撤销命令、版本一致性测试先失败。
- GREEN：新增内部格式化 helper，并接入确认输出、本地知识库命中回答附注和长期记忆兜底附注。
- 保持边界：普通 LLM fallback context 不展示 `/preference-apply-undo prefapp-...`，偏好管理命令继续不进入 LLM provider instructions、SearchRouter、InnerBrain、路由或桌面执行决策。
- 版本提升到 `0.143.0`，同步 `pyproject.toml`、`src/jarvis_lite/__init__.py` 和项目元数据测试。

### 验证摘要

- 目标 GREEN：`tests.test_preferences`、Agent 确认输出与历史命令测试、LLM 白名单边界和版本一致性测试 11 项通过。
- 相邻回归：`tests.test_preferences tests.test_agent tests.test_llm tests.test_project_metadata -v` 通过，`Ran 445 tests`，`OK`。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，`Ran 777 tests`，`OK`。
- 命令行 smoke：`preference-undo-hints-smoke OK`，确认输出、本地 `/ask` 附注和长期记忆兜底附注均展示 `撤销确认：/preference-apply-undo prefapp-...`，普通 LLM fallback context 不包含该命令。
- 源码桌面 smoke：`.\.venv\Scripts\python.exe -m jarvis_lite.desktop.app --smoke` 退出码 0，输出 `Jarvis Lite 桌面助手` 和 `desktopPetWindow`。
- Windows 安装包：生成基础 `E:\ai\jarvis-lite-dist\JarvisLiteSetup.exe`，并复制版本化安装包 `E:\ai\jarvis-lite-dist\JarvisLiteSetup-0.143.0.exe`，大小 `59,715,584` 字节，时间戳 `2026/6/9 21:49:59`；安装脚本、SED、`JarvisLite.version.txt` 和 `JarvisLite.exe` 版本资源均为 `0.143.0`。
- 打包后 smoke：`Start-Process -Wait -PassThru -RedirectStandardOutput -RedirectStandardError` 执行 `E:\ai\jarvis-lite-dist\desktop-exe\JarvisLite.exe --smoke`，退出码 0，stdout 包含 `desktopPetWindow`，stderr 为空，无残留 `JarvisLite` 进程。
- 静态检查：`git diff --check` 退出码 0，仅 LF/CRLF 提示；Markdown 本地链接 677 项通过，覆盖 423 个 Markdown 文件；`config/llm.local.json`、`config/search.local.json`、`word/inner-brain-evaluation-report.md` 不存在；README BOM 为 `EF-BB-BF`。

## 0.144.0 偏好本地回答附注范围第一阶段

### 完成事项

- 新增 v149 上下文扫描 `.codex/context-scan-v149-preference-local-answer-scope.json` 和正式计划 `word/plans/2026-06-09-v149-preference-local-answer-scope-plan.md`。
- RED：本地回答附注回答类型范围、未知回答类型不生成附注、Agent `/ask` 知识库回答类型标签、长期记忆兜底回答类型标签、普通 LLM fallback context 不泄漏本地回答标签和版本一致性测试先失败。
- GREEN：`describe_preference_local_answer_note(paths, answer_type)` 增加回答类型 allowlist，仅允许 `knowledge` 和 `memory` 生成附注；附注显示 `回答类型：本地知识库回答` 或 `回答类型：长期记忆兜底回答`。
- Agent 本地知识库命中路径传入 `knowledge`，长期记忆兜底路径传入 `memory`。
- 保持边界：普通 LLM fallback context 不展示本地回答类型标签或 `/preference-apply-undo prefapp-...`；偏好管理命令继续不进入 LLM provider instructions、SearchRouter、InnerBrain、路由或桌面执行决策。
- 版本提升到 `0.144.0`，同步 `pyproject.toml`、`src/jarvis_lite/__init__.py` 和项目元数据测试。
- 按 `DOCUMENTATION.md` 收紧 README 和根 `verification.md`，保留入口摘要，把完整验证明细放入 `verification/2026-06/2026-06-09.md`。

### 验证摘要

- 目标 RED 已确认，失败点符合预期：helper 旧签名不接受 answer type、本地回答缺少回答类型标签、版本仍为 `0.143.0`。
- 目标 GREEN：6 项通过。
- 相邻回归：`tests.test_preferences tests.test_agent tests.test_llm tests.test_project_metadata -v` 通过，`Ran 446 tests`，`OK`；首次因更新测试夹具仍为 `0.143.1` 失败，提升到 `0.144.1` 后复跑通过。
- 全量回归：`.\.venv\Scripts\python.exe -m unittest discover -s tests -v`，`Ran 778 tests`，`OK`。
- 命令行 smoke：`preference-local-answer-scope-smoke OK`。
- 源码桌面 smoke 和打包后 smoke 均通过，打包后 stdout 包含 `desktopPetWindow`，stderr 为空，无残留 `JarvisLite` 进程。
- Windows 安装包：生成基础 `E:\ai\jarvis-lite-dist\JarvisLiteSetup.exe`，并复制版本化安装包 `E:\ai\jarvis-lite-dist\JarvisLiteSetup-0.144.0.exe`，大小 `59,715,584` 字节，时间戳 `2026/6/9 22:44:36`；安装脚本、SED、`JarvisLite.version.txt` 和 `JarvisLite.exe` 版本资源均为 `0.144.0`。

## 下一步

- 后续评估是否需要用户可配置的偏好回答类型开关，默认仍不从普通聊天或 LLM 白名单自动写入、启停或确认偏好。
