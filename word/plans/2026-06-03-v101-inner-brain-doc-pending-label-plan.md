# v101：InnerBrain 本机失败视图文档待处理失败标签方案

> 日期：2026-06-03
> 执行者：Codex
> 说明：本文承接 v100 本机报告处理边界待处理失败标签，明确 `0.97.0` 的公开文档待处理失败标签收口。

## 目标

`0.97.0` 把 README 概要段和 `word/PROJECT-PLAN.md` 能力描述中仍残留的泛化“失败样本/只看失败”表述收紧为“待处理失败样本”。这样公开文档与 `/help`、运行日志、报告标题、报告反馈和报告处理边界保持同一套本机待处理失败语义。

## 边界

- 不修改运行时代码、命令集合、报告正文或报告导出路径。
- 不修改本机 evaluation JSONL payload、训练样本写入或排序逻辑。
- 不新增真实本机 evaluation 样本。

## 实现要点

- 在 `tests/test_project_metadata.py` 增加公开文档一致性测试，断言 README 与 PROJECT-PLAN 使用“待处理失败”标签并排除旧表述。
- 更新 README 概要段和 PROJECT-PLAN 能力描述中的本机失败视图文案。
- 项目版本提升到 `0.97.0`，更新更新清单测试夹具到 `0.97.1`。

## 验证

- RED：文档一致性测试和版本一致性测试先失败，证明当前公开文档仍有旧表述。
- GREEN：实现后目标测试通过。
- 回归：ProjectMetadata + Agent 相邻回归、全量 `unittest`、源码桌面 smoke、安装包构建和打包后 exe smoke。
