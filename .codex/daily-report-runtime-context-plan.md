# 日报运行态上下文联动计划

> 日期：2026-05-22  
> 执行者：Codex

## 目标

让 `/daily-report` 生成的日报引用最近运行态上下文，包括最近资料、最近目录、最近搜索结果和最近建议。

## 范围

- 新增日报章节：`## 最近上下文`。
- 读取 `runtime_context.py` 已有的项目外上下文文件。
- 没有上下文时写明暂无最近上下文。
- 不改变上下文持久化格式，不改变 Agent 最近上下文状态命令。

## 实施步骤

1. 写失败测试：先保存运行态上下文，再生成日报，断言日报包含最近上下文。
2. 在 `automation.py` 中加载并格式化运行态上下文。
3. 跑 Automation、Agent 和全量验证。
4. 更新 README、进度文档、方案文档、验证记录和 `.codex` 本地记录。
