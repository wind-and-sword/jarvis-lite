# 0.116.0 Chrome 低风险工作流第一阶段审查报告

> 日期：2026-06-03
> 审查者：Codex
> 审查对象：`/chrome-workflow-status`、`/chrome-open`、`/chrome-search`、`0.116.0` 文档和验证记录

## 评分

- 技术维度：94/100
- 战略维度：94/100
- 综合评分：94/100
- 建议：通过

## 核对结果

- 需求字段完整性：通过。目标、范围、非目标、文件计划、验证方式已写入 v121 计划。
- 接口契约：通过。`describe_chrome_workflow_status(paths)`、`normalize_chrome_url(raw_url)`、`build_chrome_search_url(query)`、`open_chrome_url(paths, raw_url, executor=...)`、`describe_chrome_open()` 和 `describe_chrome_search()` 均有测试覆盖。
- 授权边界：通过。`/chrome-open` 与 `/chrome-search` 已纳入桌面动作集合；LLM 白名单只增加 `/chrome-workflow-status`，不允许 LLM 直接建议打开网页命令。
- 测试覆盖：通过。目标 RED/GREEN、相邻回归、全量回归、命令行 smoke、源码桌面 smoke、安装包构建、打包后 smoke 和静态检查均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v121 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.116.0`。

## 风险与处理

- 真实 `/chrome-open` 与 `/chrome-search` 会启动本机浏览器，默认 smoke 未执行真实动作；该风险由执行器注入测试覆盖启动参数契约，并在验证记录中标注跳过原因。
- 当前只构造 Google 搜索 URL，后续如果需要用户可配置搜索引擎，应作为单独阶段接入配置管家或本地配置文件。
- 当前不读取网页、不点击页面、不保存资料；后续网页提取与保存资料应依赖截图/OCR、联网搜索或明确的浏览器自动化方案分阶段推进。

## 证据

- 目标 RED：19 项先失败，失败原因符合预期。
- 目标 GREEN：26 项通过。
- 相邻回归：417 项通过。
- 全量回归：653 项通过。
- 打包：`JarvisLiteSetup-0.116.0.exe` 生成，大小 `60,403,712` 字节；版本资源为 `0.116.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 552 项通过；严格真实密钥形态扫描 22 个公开变更文件无命中；README BOM 为 `EF-BB-BF`。

# 0.117.0 Clash Verge 低风险工作流第一阶段审查报告

> 日期：2026-06-03
> 执行者：Codex

## 结论

- 建议：通过。
- 综合评分：94/100。
- 技术评分：95/100。
- 战略评分：93/100。

## 核对清单

- 需求字段完整性：通过。目标、范围、非目标、文件计划、验证方式已写入 v122 计划。
- 接口契约：通过。`describe_clash_workflow_status(paths)`、`open_clash_verge(paths, executor=...)`、`describe_clash_open()` 和 `describe_clash_focus()` 均有测试覆盖。
- 授权边界：通过。`/clash-open` 与 `/clash-focus` 已纳入桌面动作集合；LLM 白名单只增加 `/clash-workflow-status`，不允许 LLM 直接建议打开或聚焦代理面板。
- 测试覆盖：通过。目标 RED/GREEN、相邻回归、全量回归、命令行 smoke、源码桌面 smoke、安装包构建、打包后 smoke 和静态检查均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v122 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.117.0`。

## 风险与处理

- 真实 `/clash-open` 与 `/clash-focus` 会启动或切换本机应用，默认 smoke 未执行真实动作；该风险由执行器注入和窗口快照注入测试覆盖启动与聚焦契约，并在验证记录中标注跳过原因。
- 当前不切换节点、不开关系统代理、不修改配置；后续如要支持代理状态变更，必须作为单独阶段引入更明确的状态读取、确认和失败复盘。

## 证据

- 目标 RED：20 项先失败，失败原因符合预期。
- 目标 GREEN：25 项通过。
- 相邻回归：429 项通过。
- 全量回归：666 项通过。
- 打包：`JarvisLiteSetup-0.117.0.exe` 生成，大小 `60,403,712` 字节；版本资源为 `0.117.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 554 项通过；严格真实密钥形态扫描 22 个公开变更文件无命中；README BOM 为 `EF-BB-BF`。

# 0.118.0 QQ/微信准备式工作流第一阶段审查报告

> 日期：2026-06-03
> 执行者：Codex

## 结论

- 建议：通过。
- 综合评分：94/100。
- 技术评分：95/100。
- 战略评分：93/100。

## 核对清单

- 需求字段完整性：通过。目标、范围、非目标、文件计划、验证方式已写入 v123 计划。
- 接口契约：通过。消息工作流状态、打开、聚焦和准备未发送草稿均有单元测试覆盖；草稿命令显式要求 `联系人 => 消息`，空字段会拒绝。
- 授权边界：通过。`/qq-open`、`/qq-focus`、`/wechat-open` 与 `/wechat-focus` 已纳入桌面动作集合；准备草稿命令不执行桌面动作，只返回本地未发送草稿；LLM 白名单只增加 `/messaging-workflow-status`。
- 测试覆盖：通过。目标 RED/GREEN、相邻回归、全量回归、命令行 smoke、源码桌面 smoke、安装包构建、打包后 smoke 和静态检查均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v123 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.118.0`。

## 风险与处理

- 真实 QQ/微信打开和聚焦会启动或切换本机应用，默认 smoke 未执行真实动作；该风险由执行器和窗口快照注入测试覆盖契约，并在验证记录中标注跳过原因。
- 当前不查找真实联系人、不点击、不输入到聊天窗口、不读取聊天内容、不发送消息；后续如要进入真实联系人定位或输入阶段，必须单独设计确认边界和失败复盘。

## 证据

- 目标 RED：目标命令先失败，失败原因符合预期。
- 目标 GREEN：29 项通过。
- 相邻回归：440 项通过。
- 全量回归：683 项通过。
- 打包：`JarvisLiteSetup-0.118.0.exe` 生成，大小 `60,411,904` 字节；版本资源为 `0.118.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 652 项通过；严格密钥形态扫描无命中；README BOM 为 `EF-BB-BF`。

# 0.119.0 IDEA 项目状态第一阶段审查报告

> 日期：2026-06-03
> 执行者：Codex

## 结论

- 建议：通过。
- 综合评分：94/100。
- 技术评分：95/100。
- 战略评分：93/100。

## 核对清单

- 需求字段完整性：通过。目标、范围、非目标、文件计划、验证方式已写入 v124 计划。
- 接口契约：通过。IDEA 状态、打开、聚焦、打开项目和只读项目状态均有单元测试覆盖；打开项目要求显式存在的本地目录。
- 授权边界：通过。`/idea-open`、`/idea-focus` 和 `/idea-open-project` 已纳入桌面动作集合；`/idea-project-status` 只读检查本地目录状态；LLM 白名单只增加 `/idea-workflow-status`。
- 测试覆盖：通过。目标 RED/GREEN、相邻回归、全量回归、命令行 smoke、源码桌面 smoke、安装包构建、打包后 smoke 均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v124 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.119.0`。

## 风险与处理

- 真实 `/idea-open`、`/idea-focus` 与 `/idea-open-project` 会启动或切换本机应用，默认 smoke 未执行真实动作；该风险由执行器和窗口快照注入测试覆盖契约，并在验证记录中标注跳过原因。
- 当前只读项目状态只检查目录标记，不读取 IDE 内部状态；后续如果要运行测试、打开终端或读取 IDE 窗口内容，应作为单独阶段引入确认和失败复盘。

## 证据

- 目标 RED：目标命令先失败，失败原因符合预期。
- 目标 GREEN：24 项通过。
- 相邻回归：451 项通过。
- 全量回归：702 项通过。
- 打包：`JarvisLiteSetup-0.119.0.exe` 生成，大小 `60,416,000` 字节；版本资源为 `0.119.0`。

# 0.120.0 任务状态与失败复盘第一阶段审查报告

> 日期：2026-06-03
> 执行者：Codex

## 结论

- 建议：通过。
- 综合评分：94/100。
- 技术评分：95/100。
- 战略评分：93/100。

## 核对清单

- 需求字段完整性：通过。目标、范围、非目标、文件计划、验证方式已写入 v125 计划。
- 接口契约：通过。任务状态、开始、步骤推进、失败复盘、恢复、完成和取消均有单元测试覆盖；命令要求显式 slash command。
- 持久化边界：通过。当前任务与最近失败记录复用项目外 `jarvis-lite-runtime/agent-context.json`，可跨 Agent 重启恢复。
- 执行边界：通过。本阶段只记录任务状态和复盘占位，不自动截图、不自动 OCR、不自动重新执行外部动作；LLM 白名单只增加 `/task-status`。
- 测试覆盖：通过。目标 RED/GREEN、相邻回归、全量回归、命令行 smoke、源码桌面 smoke、安装包构建、打包后 smoke 和静态检查均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v125 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.120.0`。

## 风险与处理

- 当前失败复盘的屏幕/OCR 字段只是占位，未采集真实屏幕上下文；已在命令输出、计划和验证记录中明确边界，后续截图/OCR 接入应作为单独阶段推进。
- 任务恢复只恢复状态，不重放任何外部动作；该行为符合第一阶段边界，避免误启动、误切换或误输入。

## 证据

- 目标 RED：目标命令先失败，失败原因符合预期。
- 目标 GREEN：9 项通过。
- 相邻回归：425 项通过。
- 全量回归：710 项通过。
- 打包：`JarvisLiteSetup-0.120.0.exe` 生成，大小 `60,424,192` 字节；版本资源为 `0.120.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 562 项通过；严格密钥形态扫描 15 个公开变更文件无命中；README BOM 为 `EF-BB-BF`。

# 0.121.0 任务失败截图 OCR 复盘第一阶段审查报告

> 日期：2026-06-03
> 执行者：Codex

## 结论

- 建议：通过。
- 综合评分：94/100。
- 技术评分：95/100。
- 战略评分：93/100。

## 核对清单

- 需求字段完整性：通过。目标、范围、非目标、文件计划、验证方式已写入 v126 计划。
- 接口契约：通过。`record_task_failure_with_screen_ocr()` 要求显式失败原因和已有当前任务，支持注入截图器与 OCR 识别器，返回截图路径、尺寸、OCR 文本或 OCR 失败诊断。
- 持久化边界：通过。截图/OCR 上下文写入已有运行态最近失败复盘，未新增持久化结构。
- 执行边界：通过。本阶段只在显式 `/task-fail-capture` 下采集失败上下文，不自动重新执行外部动作、不点击、不输入；LLM 白名单不开放 `/task-fail-capture`。
- 测试覆盖：通过。目标 RED/GREEN、相邻回归、全量回归、命令行 smoke、源码桌面 smoke、安装包构建、打包后 smoke 和静态检查均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v126 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.121.0`。

## 风险与处理

- 真实 `/task-fail-capture` 会保存当前屏幕截图，可能采集敏感屏幕内容；本轮不执行真实截图 smoke，改用 capturer 和 recognizer 注入测试覆盖采集、OCR 成功和 OCR 失败诊断契约。
- OCR 文本可能较长；状态展示使用压缩后的屏幕/OCR 上下文，运行态保留完整失败复盘内容，便于后续人工判断是否固化。

## 证据

- 目标 RED：目标命令先失败，失败原因符合预期。
- 目标 GREEN：9 项通过。
- 相邻回归：435 项通过。
- 全量回归：713 项通过。
- 打包：`JarvisLiteSetup-0.121.0.exe` 生成，大小 `60,424,192` 字节；版本资源为 `0.121.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 565 项通过；严格密钥形态扫描 12 个公开变更文件无命中；README BOM 为 `EF-BB-BF`。

# 0.122.0 记忆与配置候选池第一阶段审查报告

> 日期：2026-06-03
> 执行者：Codex

## 结论

- 建议：通过。
- 综合评分：94/100。
- 技术评分：95/100。
- 战略评分：93/100。

## 核对清单

- 需求字段完整性：通过。目标、范围、非目标、候选类型、文件计划和验收标准已写入 v127 计划。
- 接口契约：通过。候选列表、显式添加、重复合并计数、运行态恢复和忽略候选均有单元测试覆盖；Agent 命令参数错误有明确提示。
- 持久化边界：通过。候选池复用项目外 `jarvis-lite-runtime/agent-context.json`，不写 `memory/profile.md`、经验记忆、目录、应用配置、联系人或授权规则。
- 执行边界：通过。本阶段只允许显式 slash command；候选写入和忽略命令不进入 LLM 白名单。
- 测试覆盖：通过。目标 RED/GREEN、相邻回归、全量回归、命令行 smoke、源码桌面 smoke、安装包构建、打包后 smoke 和静态检查均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v127 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.122.0`。

## 风险与处理

- 运行态路径以项目根父目录为锚点；测试夹具已改用 `temp_dir/jarvis-lite` 隔离，避免候选在不同临时项目间串扰。
- 打包后 GUI exe 使用 `runw.exe`，当前终端未收到 stdout；以退出码 0、源码桌面 smoke stdout 和无残留进程作为打包后 smoke 证据。
- 候选池还没有固化/编辑/删除真实长期配置的命令；后续阶段应保持显式确认和可撤销边界。

## 证据

- 目标 RED：目标命令先失败，失败原因符合预期。
- 目标 GREEN：9 项通过。
- 相邻回归：411 项通过。
- 全量回归：718 项通过。
- 打包：`JarvisLiteSetup-0.122.0.exe` 生成，大小 `60,432,384` 字节；版本资源为 `0.122.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 569 项通过；严格真实密钥形态扫描 16 个公开变更文件无命中。

# 0.123.0 记忆与配置候选固化第一阶段审查报告

> 日期：2026-06-03
> 执行者：Codex

## 结论

- 建议：通过。
- 综合评分：94/100。
- 技术评分：95/100。
- 战略评分：93/100。

## 核对清单

- 需求字段完整性：通过。目标、非目标、实施步骤和验收标准已写入 v128 计划。
- 接口契约：通过。`/config-candidate-apply 编号`、候选固化函数、目录候选格式和 unsupported 类型边界均有测试覆盖。
- 持久化边界：通过。低风险候选只复用既有 `memory/profile.md`、`memory/experiences.md` 和 `memory/directories.json`，不新增长期配置结构。
- 执行边界：通过。固化命令只允许显式 slash command，不加入 LLM 白名单，不从普通聊天自动触发。
- 测试覆盖：通过。目标 RED/GREEN、相邻回归、全量回归、临时项目命令 smoke、源码桌面 smoke、安装包构建、打包后 smoke 和静态检查均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v128 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.123.0`。

## 风险与处理

- 联系人别名、授权规则、应用路径和偏好仍未固化；当前按边界说明保持 active，后续应先补确认和撤销闭环再落盘。
- 目录候选只接受明确 `别名 => 路径` 或 `别名 路径`，并复用现有目录存在性校验，避免猜测用户意图。
- 0.122.0 与 0.123.0 当前均为本地提交待推送；GitHub HTTPS 推送通道本轮出现 TLS/443 超时，需要后续重试。

## 证据

- 目标 RED：目标命令先失败，失败原因符合预期。
- 目标 GREEN：11 项通过。
- 相邻回归：417 项通过。
- 全量回归：724 项通过。
- 打包：`JarvisLiteSetup-0.123.0.exe` 生成，大小 `60,436,480` 字节；版本资源为 `0.123.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 415 项通过；严格真实密钥形态扫描 15 个公开变更文件无命中。

# 0.124.0 任务状态自动采集第一阶段审查报告

> 日期：2026-06-03
> 执行者：Codex

## 结论

- 建议：通过。
- 综合评分：94/100。
- 技术评分：95/100。
- 战略评分：93/100。

## 核对清单

- 需求字段完整性：通过。目标、非目标、实施步骤和验收标准已写入 v129 计划。
- 接口契约：通过。任务事件持久化、状态展示、失败复盘继承和 Agent 自动采集均有测试覆盖。
- 持久化边界：通过。复用 `jarvis-lite-runtime/agent-context.json`，只扩展当前任务和失败记录的可序列化字段。
- 执行边界：通过。只采集路由事件，不自动截图、不自动 OCR、不重放外部动作，不把任务管理命令自身写入自动采集段。
- 测试覆盖：通过。目标 RED/GREEN、相邻回归、全量回归、临时项目 smoke、源码桌面 smoke、安装包构建、打包后 smoke 和静态检查均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v129 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.124.0`。

## 风险与处理

- 本阶段只采集路由事件，不采集命令执行结果全文；后续需要在不大规模破坏 `handle()` 返回路径的前提下补执行结果摘要。
- 自动采集只在当前任务 `running` 时写入，失败任务恢复前不会继续追加事件，避免复盘被失败后的观察命令污染。
- `/task-fail` 自身仍作为路由摘要出现，但不会进入“自动采集上下文”段，测试已覆盖该边界。

## 证据

- 目标 RED：目标命令先失败，失败原因符合预期。
- 目标 GREEN：3 项通过。
- 相邻回归：417 项通过。
- 全量回归：726 项通过。
- 打包：`JarvisLiteSetup-0.124.0.exe` 生成，大小 `60,436,480` 字节；版本资源为 `0.124.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 419 项通过；严格真实密钥形态扫描 17 个公开变更文件无命中。

# 0.125.0 任务执行结果摘要采集第一阶段审查报告

> 日期：2026-06-03
> 执行者：Codex

## 结论

- 建议：通过。
- 综合评分：94/100。
- 技术评分：95/100。
- 战略评分：93/100。

## 核对清单

- 需求字段完整性：通过。目标、非目标、实施步骤和验收标准已写入 v130 计划。
- 接口契约：通过。`record_task_event_result(paths, prompt, result)` 只在当前任务 running 且存在最近匹配事件时写回结果摘要。
- 执行边界：通过。只覆盖显式 slash command 返回摘要，不截图、不 OCR、不重放外部动作，不扩大自然语言自动执行范围。
- 持久化边界：通过。复用 `recent_events`，仅替换匹配事件的 `summary` 并更新时间。
- 测试覆盖：通过。目标 RED/GREEN、相邻回归、全量回归、临时项目 smoke、源码桌面 smoke、安装包构建、打包后 smoke 和静态检查均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v130 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.125.0`。

## 风险与处理

- 本阶段不覆盖普通自然语言和所有 `handle()` 返回路径；这是刻意收窄，避免为了结果采集重构高风险路由主干。
- 结果摘要会压缩到短文本，长输出不会完整进入失败复盘；后续如需更完整 transcript，应设计单独的显式调试日志引用，而不是塞入任务事件。
- 任务已经失败后不会写回 `/task-fail` 的返回结果，避免失败后的复盘命令污染原始失败上下文。

## 证据

- 目标 RED：目标命令先失败，失败原因符合预期。
- 目标 GREEN：3 项通过。
- 相邻回归：418 项通过。
- 全量回归：727 项通过。
- 打包：`JarvisLiteSetup-0.125.0.exe` 生成，大小 `60,436,480` 字节；版本资源为 `0.125.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 583 项通过；严格真实密钥形态扫描 17 个公开变更文件无命中。

# 0.126.0 任务失败复盘行动建议第一阶段审查报告

> 日期：2026-06-03
> 执行者：Codex

## 结论

- 建议：通过。
- 综合评分：94/100。
- 技术评分：95/100。
- 战略评分：93/100。

## 核对清单

- 需求字段完整性：通过。目标、非目标、实施步骤和验收标准已写入 v131 计划。
- 接口契约：通过。普通失败动态下一步建议、截图/OCR 失败不重复建议、Agent 重启恢复和版本一致性均有测试覆盖。
- 执行边界：通过。本阶段只改变失败复盘建议文案，不自动截图、不自动 OCR、不重放外部动作，不扩大自然语言自动执行范围。
- 持久化边界：通过。复用最近失败记录的 `next_step` 字段，不新增持久化结构；已有屏幕上下文用于区分是否提示补充截图/OCR。
- 测试覆盖：通过。目标 RED/GREEN、相邻回归、全量回归、临时项目 smoke、源码桌面 smoke、安装包构建、打包后 smoke 和静态检查均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v131 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.126.0`。

## 风险与处理

- `/task-status` 会展示历史失败记录，因此 smoke 不能在全部历史文本中断言没有 `/task-fail-capture`；已改为检查最新截图/OCR 失败记录的 `next_step`，避免把旧普通失败记录误判为回归。
- 当前建议只提示显式补充截图/OCR，不自动采集屏幕，符合现有任务复盘边界。
- 真实 `/task-fail-capture` 会保存当前屏幕截图，本轮继续使用 fake capturer/OCR 验证采集分支，避免不必要的真实屏幕采集。

## 证据

- 目标 RED：目标命令先失败，失败原因符合预期。
- 目标 GREEN：4 项通过。
- 相邻回归：418 项通过。
- 全量回归：727 项通过。
- 打包：`JarvisLiteSetup-0.126.0.exe` 生成，大小 `60,436,480` 字节；版本资源为 `0.126.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 588 项通过；严格真实密钥形态扫描 16 个公开变更文件无命中。

# 0.127.0 任务失败复盘样本建议第一阶段审查报告

> 日期：2026-06-03
> 执行者：Codex

## 结论

- 建议：通过。
- 综合评分：95/100。
- 技术评分：95/100。
- 战略评分：94/100。

## 核对清单

- 需求字段完整性：通过。目标、非目标、实施步骤和验收标准已写入 v132 计划。
- 接口契约：通过。命令事件建议只来自 `RuntimeTaskEventContext` 中 `route == "command"` 且命令以 `/` 开头的事件。
- 执行边界：通过。本阶段只展示可复制建议，不自动写入 evaluation、不训练、不重放命令、不点击、不输入。
- 持久化边界：通过。复用既有最近任务事件和最近失败记录，不新增持久化结构。
- 测试覆盖：通过。目标 RED/GREEN、状态边界补充 RED/GREEN、相邻回归、全量回归、临时项目 smoke、源码桌面 smoke、安装包构建、打包后 smoke 和静态检查均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v132 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.127.0`。

## 风险与处理

- 样本建议会包含原始显式命令输入中的路径；这是用户本地复制样本所需信息，当前不自动上传、不自动写入 evaluation。
- 当前只支持命令事件，不从 OCR 或非命令事件推断 intent，避免把失败复盘变成隐式训练。
- 临时 smoke 暴露 `/task-status` 最近日志缺少样本建议边界，已补测试并修复，失败复盘和状态页的边界语义一致。

## 证据

- 目标 RED：目标命令先失败，失败原因符合预期；状态边界缺口补充 RED 后也先失败。
- 目标 GREEN：5 项通过。
- 相邻回归：418 项通过。
- 全量回归：727 项通过。
- 打包：`JarvisLiteSetup-0.127.0.exe` 生成，大小 `60,440,576` 字节；版本资源为 `0.127.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 593 项通过；严格真实密钥形态扫描 16 个公开变更文件无命中。

# 0.128.0 记忆与配置候选恢复第一阶段审查报告

> 日期：2026-06-03
> 执行者：Codex

## 结论

- 建议：通过。
- 综合评分：95/100。
- 技术评分：95/100。
- 战略评分：94/100。

## 核对清单

- 需求字段完整性：通过。目标、非目标、实施步骤和验收标准已写入 v133 计划。
- 接口契约：通过。`/config-candidate-history` 只读展示历史候选，`/config-candidate-restore 编号` 只把历史候选状态恢复为 `active`。
- 执行边界：通过。本阶段不自动删除已固化内容，不写高风险配置，不加入 LLM 白名单。
- 持久化边界：通过。复用运行态候选列表和 `status` 字段，不变更长期记忆、经验记忆和常用目录存储格式。
- 测试覆盖：通过。目标 RED/GREEN、相邻回归、全量回归、临时项目 smoke、源码桌面 smoke、安装包构建、打包后 smoke 和静态检查均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v133 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.128.0`。

## 风险与处理

- 恢复已固化候选后，活跃候选可再次被固化；底层长期记忆和常用目录已有去重逻辑，临时 smoke 已验证已写入内容不会被恢复动作删除。
- 历史编号独立于活跃候选编号；输出中明确要求先看 `/config-candidate-history` 再恢复，避免把活跃候选编号误用于恢复。
- 当前仍不覆盖联系人、授权规则或应用配置的长期写入；这是本阶段的明确非目标，后续高风险候选应单独做确认与撤销闭环。

## 证据

- 目标 RED：目标命令先失败，失败原因符合预期。
- 目标 GREEN：11 项通过。
- 相邻回归：421 项通过。
- 全量回归：730 项通过。
- 打包：`JarvisLiteSetup-0.128.0.exe` 生成，大小 `60,440,576` 字节；版本资源为 `0.128.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 598 项通过；严格真实密钥形态扫描 18 个公开变更文件无命中。

# 0.129.0 高风险记忆与配置候选确认草稿第一阶段审查报告

> 日期：2026-06-03
> 执行者：Codex

## 结论

- 建议：通过。
- 综合评分：95/100。
- 技术评分：95/100。
- 战略评分：94/100。

## 核对清单

- 需求字段完整性：通过。目标、非目标、实施步骤和验收标准已写入 v134 计划。
- 接口契约：通过。高风险候选执行 `/config-candidate-apply 编号` 只返回确认草稿和撤销入口，不改变候选状态。
- 执行边界：通过。本阶段不创建联系人、授权、应用别名或偏好长期配置文件，不新增 LLM 白名单命令。
- 持久化边界：通过。复用运行态候选列表，不新增高风险长期存储结构。
- 测试覆盖：通过。目标 RED/GREEN、相邻回归、全量回归、临时项目 smoke、源码桌面 smoke、安装包构建、打包后 smoke 和静态检查均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v134 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.129.0`。

## 风险与处理

- 当前“确认草稿”不执行真实固化；这是刻意收窄，避免在缺少撤销存储和审计模型时写入联系人或授权规则。
- 撤销入口使用当前活跃候选编号；候选在草稿后保持 active，因此编号与 `/config-candidates` 保持一致。
- 后续如果实现真实高风险固化，需要单独设计长期存储、来源、时间、用途和可撤销入口。

## 证据

- 目标 RED：目标命令先失败，失败原因符合预期。
- 目标 GREEN：12 项通过。
- 相邻回归：422 项通过。
- 全量回归：731 项通过。
- 打包：`JarvisLiteSetup-0.129.0.exe` 生成，大小 `60,440,576` 字节；版本资源为 `0.129.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 603 项通过；严格真实密钥形态扫描 16 个公开变更文件无命中。

# 0.130.0 任务失败复盘窗口与授权摘要第一阶段审查报告

> 日期：2026-06-04
> 执行者：Codex

## 结论

- 建议：通过。
- 综合评分：95/100。
- 技术评分：95/100。
- 战略评分：95/100。

## 核对清单

- 需求字段完整性：通过。目标、非目标、实施步骤和验收标准已写入 v135 计划。
- 接口契约：通过。`/task-fail` 与 `/task-fail-capture` 在失败复盘中写入 `window_context`，`/task-status` 最近失败记录展示窗口、路由和授权摘要。
- 执行边界：通过。本阶段只读采集当前窗口摘要，不自动截图普通失败，不切换窗口、不点击、不输入、不自动重放外部动作。
- 持久化边界：通过。运行态失败记录新增可选 `window_context` 字段，旧记录缺失该字段时按空字符串读取。
- 测试覆盖：通过。目标 RED/GREEN、相邻回归、全量回归、命令行 smoke、源码桌面 smoke、安装包构建、打包后 smoke 和静态检查均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v135 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.130.0`。

## 风险与处理

- 当前窗口摘要依赖 Windows 前台窗口 API；不可用平台或无前台窗口时返回可读降级文案，单测覆盖格式化路径。
- 窗口标题可能包含用户当前本地应用信息；该上下文只写入本机运行态失败复盘，用于恢复任务判断，不上传、不训练。
- `/task-fail-capture` 现在同时携带截图/OCR 上下文与窗口摘要；两者仍必须由显式命令触发，不改变自动执行边界。

## 证据

- 目标 RED：目标命令先失败，失败原因符合预期。
- 目标 GREEN：5 项通过；当前会话复验 `Ran 5 tests in 0.203s`，`OK`。
- 相邻回归：429 项通过。
- 全量回归：734 项通过。
- 打包：`JarvisLiteSetup-0.130.0.exe` 生成，大小 `60,440,576` 字节；版本资源为 `0.130.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 612 项通过；严格真实密钥形态扫描 20 个公开变更/新增文件无命中。

# 0.131.0 联系人别名确认固化与撤销第一阶段审查报告

> 日期：2026-06-04
> 执行者：Codex

## 结论

- 建议：通过。
- 综合评分：95/100。
- 技术评分：95/100。
- 战略评分：95/100。

## 核对清单

- 需求字段完整性：通过。目标、非目标、实施步骤和验收标准已写入 v136 计划。
- 接口契约：通过。`/config-candidate-confirm 编号` 只按活跃候选编号确认联系人别名，`/config-candidate-undo 编号` 只按历史候选编号撤销已固化联系人别名。
- 执行边界：通过。本阶段不查找真实联系人、不点击、不输入、不发送消息；确认/撤销命令不进入 LLM 白名单。
- 持久化边界：通过。联系人别名仅写入 `config/contacts.local.json`，撤销保留空配置文件并恢复候选为活跃；授权规则、应用别名和偏好不真实固化。
- 测试覆盖：通过。目标 RED/GREEN、相邻回归、全量回归、命令行 smoke、源码桌面 smoke、安装包构建、打包后 smoke 和静态检查均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v136 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.131.0`。

## 风险与处理

- 联系人别名只是用户给出的文本映射，当前不会验证真实联系人存在，避免误触发 QQ/微信动作。
- 撤销按历史编号执行，候选撤销后会回到活跃列表；如果后续新增其他联系人配置字段，需要继续保持可撤销来源和显式确认边界。
- 高风险候选确认能力仍只覆盖联系人别名；应用别名和授权规则需要单独设计存储、审计和回滚语义。

## 证据

- 目标 RED：目标命令先失败，失败原因符合预期。
- 目标 GREEN：12 项通过；当前会话复验 `Ran 12 tests in 0.429s`，`OK`。
- 相邻回归：428 项通过。
- 全量回归：739 项通过。
- 打包：`JarvisLiteSetup-0.131.0.exe` 生成，大小 `60,448,768` 字节；版本资源为 `0.131.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 616 项通过；严格真实密钥形态扫描 21 个公开变更/新增文件无命中。

# 0.132.0 应用别名确认固化与撤销第一阶段审查报告

> 日期：2026-06-04
> 执行者：Codex

## 结论

- 建议：通过。
- 综合评分：95/100。
- 技术评分：95/100。
- 战略评分：95/100。

## 核对清单

- 需求字段完整性：通过。目标、非目标、实施步骤和验收标准已写入 v137 计划。
- 接口契约：通过。`/config-candidate-confirm 编号` 只按活跃候选编号确认应用别名，`/config-candidate-undo 编号` 只按历史候选编号撤销已固化应用别名。
- 执行边界：通过。本阶段不启动应用、不切换窗口、不点击、不输入；确认/撤销命令不进入 LLM 白名单。
- 持久化边界：通过。应用别名仅写入 `config/apps.local.json` 的 `apps.<app_id>.aliases`；撤销只删除对应别名并保留 path 等其他覆盖。
- 测试覆盖：通过。目标 RED/GREEN、相邻回归、全量回归、命令行 smoke、源码桌面 smoke、安装包构建、打包后 smoke 和静态检查均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v137 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.132.0`。

## 风险与处理

- 应用别名仅绑定到已登记应用，不新增应用路径和启动策略；真实启动仍由 `/app-launch` 的既有显式命令边界控制。
- 撤销按历史编号执行，候选撤销后回到活跃列表；已增加 JSON 结构回归断言，避免撤销时污染根节点或删除 path 覆盖。
- 授权规则和偏好候选仍未真实确认固化，后续需要单独设计存储、审计和撤销语义。

## 证据

- 目标 RED：目标命令先失败，失败原因符合预期。
- 目标 GREEN：6 项通过。
- 相邻回归：437 项通过。
- 全量回归：743 项通过。
- 打包：`JarvisLiteSetup-0.132.0.exe` 生成，大小 `60,448,768` 字节；版本资源为 `0.132.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 620 项通过；严格真实密钥形态扫描 19 个公开变更/新增文件无命中。

# 0.133.0 授权规则确认固化与撤销第一阶段审查报告

> 日期：2026-06-05
> 执行者：Codex

## 结论

- 建议：通过。
- 综合评分：95/100。
- 技术评分：95/100。
- 战略评分：95/100。

## 核对清单

- 需求字段完整性：通过。目标、非目标、实施步骤和验收标准已写入 v138 计划。
- 接口契约：通过。`/config-candidate-confirm 编号` 只按活跃候选编号确认授权规则，`/config-candidate-undo 编号` 只按历史候选编号撤销已固化授权规则。
- 执行边界：通过。本阶段只持久化和展示授权规则，不改变 `authorize_intent_execution()` 决策；确认/撤销命令不进入 LLM 白名单。
- 持久化边界：通过。授权规则仅写入 `config/authorization.local.json` 的 `rules` 列表；撤销只删除对应规则并保留空配置文件。
- 测试覆盖：通过。目标 RED/GREEN、相邻回归、全量回归、命令行 smoke、源码桌面 smoke、安装包构建、打包后 smoke 和静态检查均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v138 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.133.0`。

## 风险与处理

- 授权规则当前只是用户可审计的本地文本规则，不参与自动放行或阻断；避免在规则语义未结构化前改变执行授权边界。
- 撤销按历史编号执行，候选撤销后回到活跃列表；规则匹配按规则文本 casefold 去重和删除，后续若新增结构化规则 ID，需要补充迁移与显示语义。
- 偏好候选仍未真实确认固化，后续需要单独设计存储、审计和撤销语义。

## 证据

- 目标 RED：目标命令先失败，失败原因符合预期。
- 目标 GREEN：27 项通过。
- 相邻回归：440 项通过。
- 全量回归：746 项通过。
- 打包：`JarvisLiteSetup-0.133.0.exe` 生成，大小 `60,452,864` 字节；版本资源为 `0.133.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 630 项通过；严格真实密钥形态扫描 23 个公开变更/新增文件无命中。

# 0.134.0 偏好确认固化与撤销第一阶段审查报告

> 日期：2026-06-05
> 执行者：Codex

## 结论

- 建议：通过。
- 综合评分：95/100。
- 技术评分：95/100。
- 战略评分：95/100。

## 核对清单

- 需求字段完整性：通过。目标、非目标、实施步骤和验收标准已写入 v139 计划。
- 接口契约：通过。`/config-candidate-confirm 编号` 只按活跃候选编号确认偏好，`/config-candidate-undo 编号` 只按历史候选编号撤销已固化偏好。
- 执行边界：通过。本阶段只持久化和展示偏好，不改变回复风格、LLM prompt、路由或执行决策；确认/撤销命令不进入 LLM 白名单。
- 持久化边界：通过。偏好仅写入 `config/preferences.local.json` 的 `preferences` 列表；撤销只删除对应偏好并保留空配置文件。
- 测试覆盖：通过。目标 RED/GREEN、相邻回归、全量回归、命令行 smoke、源码桌面 smoke、安装包构建、打包后 smoke、文档后元数据复验和静态检查均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v139 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.134.0`。

## 风险与处理

- 已保存偏好当前只是用户可审计的本地文本偏好，不参与自动回复风格、LLM prompt 或执行决策；后续若启用偏好，需要单独设计显式启用/停用和预览语义。
- 撤销按历史编号执行，候选撤销后回到活跃列表；偏好匹配按偏好文本 casefold 去重和删除，后续若新增结构化偏好 ID，需要补充迁移与显示语义。
- 确认/撤销命令仍不进入 LLM 白名单，普通聊天和外脑 fallback 不会自动写入偏好。

## 证据

- 目标 RED：目标命令先失败，失败原因符合预期。
- 目标 GREEN：20 项通过。
- 相邻回归：434 项通过。
- 全量回归：750 项通过。
- 打包：`JarvisLiteSetup-0.134.0.exe` 生成，大小 `60,461,056` 字节；版本资源为 `0.134.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 308 项通过；严格真实密钥形态扫描 20 个公开变更/新增文件无命中。

# 0.135.0 偏好显式启用与停用第一阶段审查报告

> 日期：2026-06-05
> 执行者：Codex

## 结论

- 建议：通过。
- 综合评分：95/100。
- 技术评分：95/100。
- 战略评分：95/100。

## 核对清单

- 需求字段完整性：通过。目标、非目标、实施步骤和验收标准已写入 v140 计划。
- 接口契约：通过。`/preference-status` 只读展示本地偏好状态；`/preference-enable 编号` 和 `/preference-disable 编号` 只按 1 基编号切换已保存偏好的 `enabled` 字段。
- 执行边界：通过。本阶段只记录启用状态，不改变回复风格、LLM prompt、路由或执行决策；偏好管理命令不进入 LLM 白名单。
- 持久化边界：通过。偏好启用状态仅写入 `config/preferences.local.json` 的 `enabled` 字段；旧记录缺失该字段时按未启用读取。
- 测试覆盖：通过。目标 RED/GREEN、相邻回归、全量回归、命令行 smoke、源码桌面 smoke、安装包构建、打包后 smoke 和静态检查均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v140 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.135.0`。

## 风险与处理

- 已启用偏好当前不参与自动回复或 LLM prompt，避免在优先级、冲突和适用范围未设计前改变对话行为。
- 启停按状态列表编号执行，后续若偏好可编辑或排序，需要补充稳定 ID 或冲突提示。
- 偏好启停命令仍不进入 LLM 白名单，普通聊天和外脑 fallback 不会自动启用、停用或应用偏好。

## 证据

- 目标 RED：目标命令先失败，失败原因符合预期。
- 目标 GREEN：10 项通过。
- 相邻回归：437 项通过。
- 全量回归：753 项通过。
- 文档后元数据复验：7 项通过。
- 打包：`JarvisLiteSetup-0.135.0.exe` 生成，大小 `60,461,056` 字节；版本资源为 `0.135.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 494 项通过；严格真实密钥形态扫描 20 个公开变更/新增文件无命中。

# 0.136.0 偏好应用预览第一阶段审查报告

> 日期：2026-06-05
> 执行者：Codex

## 结论

- 建议：通过。
- 综合评分：95/100。
- 技术评分：95/100。
- 战略评分：95/100。

## 核对清单

- 需求字段完整性：通过。目标、非目标、实施步骤和验收标准已写入 v141 计划。
- 接口契约：通过。`/preference-preview [输入文本]` 只读展示已启用偏好和应用草案，无输入时展示通用预览，有输入时展示预览输入。
- 执行边界：通过。本阶段只做显式预览，不改变回复风格、LLM prompt、路由或执行决策；偏好预览命令不进入 LLM 白名单。
- 持久化边界：通过。预览只读取 `config/preferences.local.json` 中 `enabled=true` 的偏好，不写入偏好状态或其他运行态决策。
- 测试覆盖：通过。目标 RED/GREEN、相邻回归、全量回归、命令行 smoke、源码桌面 smoke、安装包构建、打包后 smoke、文档后元数据复验和静态检查均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v141 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.136.0`。

## 风险与处理

- 偏好预览当前只是人工可审计草案，不自动参与回复或 LLM prompt，避免在冲突、优先级和适用范围未设计前改变对话行为。
- 预览按保存顺序展示已启用偏好；后续若允许编辑、排序或分组，需要补充稳定 ID、冲突提示和真正应用前确认。
- `/preference-preview` 仍不进入 LLM 白名单，普通聊天和外脑 fallback 不会自动预览、启用或应用偏好。

## 证据

- 目标 RED：目标命令先失败，失败原因符合预期。
- 目标 GREEN：11 项通过。
- 相邻回归：440 项通过。
- 全量回归：756 项通过。
- 文档后元数据复验：7 项通过。
- 打包：`JarvisLiteSetup-0.136.0.exe` 生成，大小 `60,465,152` 字节；版本资源为 `0.136.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 498 项通过；严格真实密钥形态扫描 20 个公开变更/新增文件无命中。

# 0.137.0 偏好稳定 ID 与冲突提示第一阶段审查报告

> 日期：2026-06-05
> 执行者：Codex

## 结论

- 建议：通过。
- 综合评分：95/100。
- 技术评分：95/100。
- 战略评分：95/100。

## 核对清单

- 需求字段完整性：通过。目标、非目标、实施步骤和验收标准已写入 v142 计划。
- 接口契约：通过。偏好引用支持 1 基编号或稳定 ID；状态和预览输出展示 ID；冲突提示仅基于已启用偏好。
- 执行边界：通过。本阶段只做显式启停、预览和冲突提示，不改变回复风格、LLM prompt、路由或执行决策；偏好管理命令不进入 LLM 白名单。
- 兼容性：通过。旧 `preferences.local.json` 缺少 `id` 时按偏好文本生成确定性 ID；按编号启停保留。
- 测试覆盖：通过。目标 RED/GREEN、相邻回归、全量回归、命令行 smoke、源码桌面 smoke、安装包构建和打包后 smoke 均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v142 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.137.0`。

## 风险与处理

- 冲突检测只覆盖明显关键词对，属于提示层，不是偏好语义解析；复杂偏好冲突仍需后续真正应用前确认策略处理。
- ID 基于偏好文本生成；当前没有偏好编辑命令，因此稳定性满足本阶段需求。后续若支持编辑，需要明确保留旧 ID 还是生成新 ID。

## 证据

- 目标 RED：目标命令先失败，失败原因符合预期。
- 目标 GREEN：13 项通过。
- 相邻回归：442 项通过。
- 全量回归：758 项通过。
- 文档后元数据复验：7 项通过。
- 打包：`JarvisLiteSetup-0.137.0.exe` 生成，大小 `60,465,152` 字节；版本资源为 `0.137.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 646 项通过；严格真实密钥形态扫描 15 个公开变更/新增文件无命中。

# 0.138.0 偏好应用确认草稿第一阶段审查报告

> 日期：2026-06-05
> 执行者：Codex

## 结论

- 建议：通过。
- 综合评分：95/100。
- 技术评分：95/100。
- 战略评分：95/100。

## 核对清单

- 需求字段完整性：通过。目标、非目标、实施步骤和验收标准已写入 v143 计划。
- 接口契约：通过。`/preference-apply-draft [输入文本]` 生成待确认偏好应用草稿，输出输入、已启用偏好、稳定 ID、冲突提示和确认边界。
- 执行边界：通过。本阶段只做显式草稿，不真正应用偏好，不改变回复风格、LLM prompt、路由或执行决策；偏好应用草稿命令不进入 LLM 白名单。
- 持久化边界：通过。草稿只读取 `config/preferences.local.json` 中 `enabled=true` 的偏好，不写入偏好状态、pending 状态或其他运行态决策。
- 测试覆盖：通过。目标 RED/GREEN、相邻回归、全量回归、命令行 smoke、源码桌面 smoke、安装包构建、打包后 smoke 和静态检查均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v143 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.138.0`。

## 风险与处理

- 当前草稿不生成可执行确认命令，避免用户误以为偏好已经真正应用；下一阶段需要单独设计确认命令和应用范围。
- 草稿仍按已启用偏好保存顺序展示，冲突提示只覆盖明显关键词对；复杂冲突继续留给人工确认。
- `/preference-apply-draft` 不进入 LLM 白名单，普通聊天和外脑 fallback 不会自动启用、预览或应用偏好。

## 证据

- 目标 RED：目标命令先失败，失败原因符合预期。
- 目标 GREEN：429 项通过。
- 相邻回归：445 项通过。
- 全量回归：761 项通过。
- 命令行 smoke：`preference-apply-draft-smoke OK`。
- 打包：`JarvisLiteSetup-0.138.0.exe` 生成，大小 `60,465,152` 字节；版本资源为 `0.138.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 650 项通过；严格真实密钥形态扫描 16 个公开变更/新增文件无命中。

# 0.139.0 偏好应用确认命令第一阶段审查报告

> 日期：2026-06-05
> 执行者：Codex

## 结论

- 建议：通过。
- 综合评分：95/100。
- 技术评分：95/100。
- 战略评分：95/100。

## 核对清单

- 需求字段完整性：通过。目标、非目标、实施步骤和验收标准已写入 v144 计划。
- 接口契约：通过。`/preference-apply-confirm [输入文本]` 只确认已启用偏好应用到本次命令输出；无启用偏好或存在明显冲突时拒绝确认。
- 执行边界：通过。本阶段不改变普通回复、LLM prompt、SearchRouter、InnerBrain、路由或执行决策；偏好应用确认命令不进入 LLM 白名单。
- 持久化边界：通过。确认命令只读取 `config/preferences.local.json` 中 `enabled=true` 的偏好，不写入偏好状态、pending 状态或其他运行态决策。
- 测试覆盖：通过。RED/GREEN、相邻回归、全量回归、命令行 smoke、源码桌面 smoke、安装包构建、打包后 smoke 和静态检查均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v144 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.139.0`。

## 风险与处理

- 当前确认仍是显式命令输出范围内的确认，不是普通对话自动应用；后续若需要影响真实回复，必须单独设计可审计的应用链路和回滚语义。
- 冲突检测仍只覆盖明显关键词对；存在冲突时拒绝确认，避免在优先级未定义前自动裁决。
- `/preference-apply-confirm` 不进入 LLM 白名单，普通聊天和外脑 fallback 不会自动启用、预览或确认偏好。

## 证据

- 目标 RED：目标命令先失败，失败原因符合预期。
- 目标 GREEN：同一目标命令通过；接手后相邻回归 449 项通过。
- 全量回归：765 项通过。
- 命令行 smoke：`preference-apply-confirm smoke OK`。
- 打包：`JarvisLiteSetup-0.139.0.exe` 生成，大小 `60,469,248` 字节；版本资源为 `0.139.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 654 项通过；严格真实密钥形态扫描 17 个公开变更/新增文件无命中。

# 0.140.0 偏好应用确认记录与撤销第一阶段审查报告

> 日期：2026-06-09
> 执行者：Codex

## 结论

- 建议：通过。
- 综合评分：96/100。
- 技术评分：96/100。
- 战略评分：95/100。

## 核对清单

- 需求字段完整性：通过。目标、非目标、实施步骤和验收标准已写入 v145 计划。
- 接口契约：通过。`/preference-apply-confirm [输入文本]` 成功后记录运行态确认历史；`/preference-apply-history` 查看最近确认记录；`/preference-apply-undo 编号或ID` 只撤销确认记录。
- 执行边界：通过。当前阶段不改变普通聊天、LLM prompt、SearchRouter、InnerBrain、路由或执行决策；历史和撤销命令不进入 LLM 白名单。
- 持久化边界：通过。确认记录写入运行态 `recent_preference_applications`；撤销只把确认记录标记为 `undone`，不停用偏好、不删除偏好、不回滚已经展示的输出。
- 缺陷处理：通过。断线恢复后复核 `日志.txt`，修正 command-line smoke 使用的不存在 API；追加同秒重复确认 ID 碰撞回归测试并修复。
- 测试覆盖：通过。目标 RED/GREEN、相邻回归、全量回归、命令行 smoke、源码桌面 smoke、安装包构建、打包后 smoke 和静态检查均已记录。
- 文档同步：通过。README、PROJECT-PLAN、v145 计划、计划索引、文档索引、进度和验证记录均已同步到 `0.140.0`。

## 风险与处理

- 撤销语义当前仅覆盖确认记录状态，不影响已经展示过的确认输出；该边界已在命令输出、计划和验证记录中明确。
- 同秒重复确认在相同输入和相同启用偏好下原本会生成相同 `prefapp-*` ID；已新增回归测试并在 ID 碰撞时追加序号参与哈希，保留每次确认历史。
- 偏好仍未自动进入普通回复链路；下一阶段如果要真实影响回复，需要单独设计应用链路、审计和冲突优先级。

## 证据

- 目标 RED：目标命令先失败，失败原因符合预期。
- 目标 GREEN：相邻回归 453 项通过。
- 追加 RED/GREEN：`test_confirmed_preference_application_keeps_same_second_duplicate_history` 先失败后通过。
- 目标复验：438 项通过。
- 全量回归：770 项通过。
- 断线恢复最终复验：770 项通过。
- 命令行 smoke：`preference-apply-audit-smoke OK`。
- 打包：`JarvisLiteSetup-0.140.0.exe` 生成，大小 `59,715,584` 字节；版本资源为 `0.140.0`。
- 静态检查：`git diff --check` 退出码 0；Markdown 本地链接 666 项通过；严格真实密钥形态扫描 25 个公开变更/新增文件无命中。
