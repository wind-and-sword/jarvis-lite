# 0.26.0 桌面候选选择绑定执行计划

> 日期：2026-06-01
> 执行者：Codex

## 目标

把 `/inner-brain-candidates` 返回的编号候选直接绑定到桌面模板填充区：候选下拉框展示当前候选，选择某条候选后同步编号，随后“填教学”或“填标注”仍只写入输入框。

## 边界

- 不新增后端命令。
- 不新增自然语言正则。
- 不自动发送、不自动训练。
- 不提交 `.codex/` 本地留痕。

## TDD 步骤

1. 在 `tests/test_desktop_widgets.py` 新增桌面候选选择测试：
   - 有候选时下拉框展示候选文本。
   - 选择第 2 条候选会同步编号为 2。
   - 点击“填标注”使用第 2 条候选编号填入模板。
   - 空候选时下拉框显示空状态并禁用。
2. 把 `tests/test_project_metadata.py` 的发布版本预期提升到 `0.26.0`，先跑目标测试确认 RED。
3. 在 `src/jarvis_lite/desktop/widgets.py` 实现候选解析、下拉框填充、选择与编号双向同步。
4. 更新 `pyproject.toml`、`src/jarvis_lite/__init__.py` 和 `tests/test_agent.py` 更新清单夹具版本。
5. 跑目标 GREEN、相邻回归、全量测试、桌面 smoke、安装包构建和静态检查。
6. 更新 README、方案、验证、进度和索引文档。
7. 提交并推送 `0.26.0`。
